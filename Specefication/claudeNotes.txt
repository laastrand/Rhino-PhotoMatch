
hi claude!
Hi! How can I help you today?

create these folders:
RhinoPhotoMatch/
├── Commands/
├── Core/
├── UI/
└── Resources/
Let me create these folders in the current working directory.

Bash Create RhinoPhotoMatch folder structure
IN
mkdir -p c:/SRC/RHINO/VIEW/RhinoPhotoMatch/Commands c:/SRC/RHINO/VIEW/RhinoPhotoMatch/Core c:/SRC/RHINO/VIEW/RhinoPhotoMatch/UI c:/SRC/RHINO/VIEW/RhinoPhotoMatch/Resources

OUT
0
Done. Folders created at RhinoPhotoMatch/.

I want to buld a plug in to rhino the specification in the file \VIEWSpecefication\RhinoPhotoMatch_Spec.md
The user wants to build a Rhino plugin based on a specification file. Let me read that file first.

Let me read the specification file.

Read RhinoPhotoMatch_Spec.md
Globpattern: "**/*PhotoMatch*"
Found 1 file
Read RhinoPhotoMatch_Spec.md
This is a well-structured specification for a Rhino plugin called RhinoPhotoMatch. The user wants to build this plugin. Let me understand the scope:

The spec describes a Rhino 8 C# plugin for photo matching/camera calibration. The features are:

Plugin Scaffold - minimal Rhino 8 plugin structure

Photo Import & Picture Plane - import photos as picture frames

Camera-Linked Picture Planes - named camera + picture plane pairs

Camera Calibration (PnP) - reference point-based calibration using OpenCV SolvePnP

Camera Calibration (Vanishing Points) - vanishing line-based calibration

Overlay & Alignment Feedback - wireframe overlay display conduit

View Lock - lock calibrated viewport

Session Save & Restore - JSON serialization to Rhino doc strings

Dockable Panel UI - Eto.Forms panel

The tech stack uses C# with RhinoCommon for Rhino 8, OpenCvSharp4 and Math.NET Numerics for computation, Eto.Forms for the UI, and System.Text.Json for serialization.

Now I need to scaffold the project structure. I'll create a .csproj file targeting .NET 7.0 since that's what Rhino 8 uses, set up the RhinoCommon references, and build out the main plugin entry point that Rhino will load. Setting up the project file with the correct target framework and NuGet dependencies for Rhino 8...

The plugin class inherits from PlugIn and implements the OnLoad method to initialize the plugin when Rhino loads it. Now I'm wondering whether a .rhp file is actually needed for deployment.

Actually, the .rhp file is just the compiled DLL renamed with a .rhp extension during the build process, so I don't need to create it manually. Looking at the scaffold requirements, I need to set up the .csproj file and the plugin class itself. The spec mentions adding NuGet references for OpenCvSharp4 and Math.NET Numerics as part of the scaffold tasks, but there's a constraint saying not to reference external packages yet. I think the intent is to start with just RhinoCommon and add those other dependencies in later steps when they're actually needed. Now I'm deciding which core files to create for the scaffold—the solution file, project file, and main plugin class—and I need to make sure I'm using the right configuration for Rhino 8, including the proper plugin GUID and target framework. I should start with the plugin scaffold since that's the foundation for everything else. For a Rhino 8 plugin, I need to target net7.0-windows, reference the RhinoCommon NuGet package, structure it as a Class Library with the proper GUID attribute, and include the OpenCV and Math.NET Numerics dependencies for the image processing features.

Writing the project files... Setting up the project file with the necessary NuGet package dependencies for RhinoCommon, OpenCV, and MathNet.Numerics, then starting the plugin entry point class with the required assembly attributes. Finishing up the plugin class definition, then I'm thinking through the project structure—I should create a solution file and scaffold out the main files I'll need, starting with the plugin class and working through the necessary namespaces and GUIDs. Finishing the solution file structure with build configurations for both Debug and Release modes, using the same project GUID throughout.

I've read the full spec. It covers 8 features. I'll start with the Plugin Scaffold — the prerequisite for everything else — creating the .sln, .csproj, and plugin entry point.

The user wants me to use the folders already created in RhinoPhotoMatch/. Let me create the scaffold files in the proper locations. I'll create:

RhinoPhotoMatch.sln - solution file at root
RhinoPhotoMatch/RhinoPhotoMatch.csproj - project file
RhinoPhotoMatch/RhinoPhotoMatchPlugin.cs - plugin entry point in the RhinoPhotoMatch folder (not in a subfolder since it's the root plugin file)
The folders already created are:

RhinoPhotoMatch/Commands/

RhinoPhotoMatch/Core/

RhinoPhotoMatch/UI/

RhinoPhotoMatch/Resources/

So the plugin entry point goes directly in RhinoPhotoMatch/, and the other files will go in their respective folders as we build features. Now I need to generate the GUIDs for the solution and project files to set up the .sln and .csproj structure.

Let me create the scaffold files. I'll create:

Solution file
.csproj
RhinoPhotoMatchPlugin.cs (in RhinoPhotoMatch/ root)
The user said "use folders in RhinoPhotoMatch" - meaning put files inside the already-created RhinoPhotoMatch/ folder structure.

Update Todos

Create solution file (.sln)

Create RhinoPhotoMatch.csproj

Create RhinoPhotoMatchPlugin.cs entry point
Write RhinoPhotoMatch.sln
20 lines

Microsoft Visual Studio Solution File, Format Version 12.00
# Visual Studio Version 17
VisualStudioVersion = 17.0.31903.59
MinimumVisualStudioVersion = 10.0.40219.1
Project("{FAE04EC0-301F-11D3-BF4B-00C04F79EFBC}") = "RhinoPhotoMatch", "RhinoPhotoMatch\RhinoPhotoMatch.csproj", "{B1C2D3E4-F5A6-7890-BCDE-F01234567891}"
EndProject
Global
	GlobalSection(SolutionConfigurationPlatforms) = preSolution
		Debug|Any CPU = Debug|Any CPU
		Release|Any CPU = Release|Any CPU
	EndGlobalSection
	GlobalSection(ProjectConfigurationPlatforms) = postSolution
		{B1C2D3E4-F5A6-7890-BCDE-F01234567891}.Debug|Any CPU.ActiveCfg = Debug|Any CPU
		{B1C2D3E4-F5A6-7890-BCDE-F01234567891}.Debug|Any CPU.Build.0 = Debug|Any CPU
		{B1C2D3E4-F5A6-7890-BCDE-F01234567891}.Release|Any CPU.ActiveCfg = Release|Any CPU
		{B1C2D3E4-F5A6-7890-BCDE-F01234567891}.Release|Any CPU.Build.0 = Release|Any CPU
	EndGlobalSection
EndGlobal
Update Todos

Create solution file (.sln)

Create RhinoPhotoMatch.csproj

Create RhinoPhotoMatchPlugin.cs entry point
Write RhinoPhotoMatch.csproj
27 lines
<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net7.0-windows</TargetFramework>
    <AssemblyName>RhinoPhotoMatch</AssemblyName>
    <RootNamespace>RhinoPhotoMatch</RootNamespace>
    <OutputType>Library</OutputType>
    <AppendTargetFrameworkToOutputPath>false</AppendTargetFrameworkToOutputPath>
    <AppendRuntimeIdentifierToOutputPath>false</AppendRuntimeIdentifierToOutputPath>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <!-- Rename output to .rhp so Rhino recognises it as a plugin -->
    <TargetExt>.rhp</TargetExt>
  </PropertyGroup>

  <ItemGroup>
    <!-- RhinoCommon: exclude runtime assets — Rhino provides them at load time -->
    <PackageReference Include="RhinoCommon" Version="8.*" ExcludeAssets="runtime" />
    <!-- Computer vision -->
    <PackageReference Include="OpenCvSharp4" Version="4.9.0.*" />
    <PackageReference Include="OpenCvSharp4.runtime.win" Version="4.9.0.*" />
    <!-- Linear algebra -->
    <PackageReference Include="MathNet.Numerics" Version="5.*" />
  </ItemGroup>

</Project>
Update Todos

Create solution file (.sln)

Create RhinoPhotoMatch.csproj

Create RhinoPhotoMatchPlugin.cs entry point
Write RhinoPhotoMatchPlugin.cs
36 lines
using System;
using System.Collections.Generic;
using Rhino;
using Rhino.PlugIns;

[assembly: System.Runtime.InteropServices.Guid("A8E3F2C1-D4B7-4A5E-9C3F-B2D8E1A6C9F4")]

namespace RhinoPhotoMatch
{
    public class RhinoPhotoMatchPlugin : PlugIn
    {
        public RhinoPhotoMatchPlugin()
        {
            Instance = this;
        }

        public static RhinoPhotoMatchPlugin Instance { get; private set; } = null!;

        public override PlugInLoadTime LoadTime => PlugInLoadTime.AtStartup;

        // Active picture plane / camera pairs for this session
        // Populated by PhotoPlaneRegistry (Core/)
        public List<object> PhotoPlanePairs { get; } = new();

        // 3D–2D reference point pairs for camera calibration
        // Populated by SetReferencePointsCommand (Commands/)
        public List<(Rhino.Geometry.Point3d WorldPoint, Rhino.Geometry.Point2d ImagePoint)> ReferencePairs { get; } = new();

        protected override LoadReturnCode OnLoad(ref string errorMessage)
        {
            RhinoApp.WriteLine("RhinoPhotoMatch loaded.");
            return LoadReturnCode.Success;
        }
    }
}
Update Todos

Create solution file (.sln)

Create RhinoPhotoMatch.csproj

Create RhinoPhotoMatchPlugin.cs entry point
Scaffold is ready. Here's what was created:


RhinoPhotoMatch.sln
RhinoPhotoMatch/
├── RhinoPhotoMatch.csproj       ← targets net7.0-windows, .rhp output
├── RhinoPhotoMatchPlugin.cs     ← plugin entry point + session state
├── Commands/
├── Core/
├── UI/
└── Resources/
Key decisions:

TargetExt is .rhp so the build output is directly loadable by Rhino
RhinoCommon has ExcludeAssets="runtime" — Rhino provides the runtime at load time
OpenCvSharp4.runtime.win brings the native OpenCV DLLs into the build output
Plugin holds ReferencePairs and PhotoPlanePairs as session state — the Core/Commands classes will write into these
Next step would be implementing the Photo Import & Picture Plane feature (Commands/ImportPhotoCommand.cs + Core/PicturePlaneManager.cs). Want to continue?

yes
The user wants to continue with the Photo Import & Picture Plane feature. Let me create:

Commands/ImportPhotoCommand.cs - file picker dialog, places picture plane
Core/PicturePlaneManager.cs - encapsulates picture frame creation and retrieval
But wait - the spec also says in the "Camera-Linked Picture Planes" feature that we should NOT use Rhino's built-in PictureFrame object type, and instead use a flat mesh/surface with a custom material. However, that's Feature 3, and Feature 2 (Photo Import & Picture Plane) says to use Rhino.DocObjects.PictureFrame.

Looking at this more carefully, Feature 3 explicitly forbids using Rhino's PictureFrame and requires a mesh with custom material instead, which is the more complete approach. Since the user is building toward Feature 3 anyway, I should implement Feature 2 using that mesh-based approach from the start to avoid refactoring later.

I'll start by creating the ImportPhotoCommand class and a PicturePlaneManager to handle the mesh creation and material setup. I need to figure out the right Rhino API for adding a picture frame to the document. The method takes a plane, image path, and some boolean flags for self-illumination and embedding, along with width and height parameters. Let me work through the exact signature and how to calculate the proper dimensions while maintaining the aspect ratio.

Writing the ImportPhotoCommand... Now I'm calculating the aspect ratio and setting up the picture frame dimensions, then adding it to the document through the picture plane manager. I'm storing the frame ID and image path in the plugin instance, logging the successful import with the image dimensions, and redrawing the viewport to display the result.

Building the PicturePlaneManager utility...

Now I'm thinking through how to handle state management for the photo plane features. The spec indicates that Feature 3 will use a PhotoPlaneRegistry to track PhotoPlanePair records, so for Feature 2 I'll keep it simple with just basic plugin-level state to store the picture frame ID. I'm also considering whether to use System.Drawing.Common or Eto.Drawing.Bitmap for image handling in the .NET 7 Windows context.

Since Eto is already available in the Rhino environment, I'll leverage Eto.Drawing.Bitmap to read image dimensions directly rather than parsing headers manually. I also need to update the plugin to persist the PictureFrameId and PhotoImagePath values, and I'm starting to think through how the file dialog integration will work with Eto.Forms.

For the file dialog, I should use Eto.Forms.OpenFileDialog since that's what the spec requires, though Rhino does have its own dialog options available. Now I'm considering how doc.Objects.AddPictureFrame will fit into the workflow.

The method takes a plane, bitmap file path, and dimensions in model units—I'll start with a 1.0 unit width and let users scale as needed. I also need to update the plugin class to store the picture frame ID and handle reading the existing file to add the new properties, though I realize I initially used List<object> for PhotoPlanePairs which I should reconsider.

For now, I'll keep Feature 2 focused with just PictureFrameId and PhotoImagePath in the plugin, knowing that Feature 3 will introduce PhotoPlaneRegistry and refactor the state management approach more comprehensively. My plan is to update the plugin file, then create the import command and picture plane manager classes.

Update Todos

Create Core/PicturePlaneManager.cs

Create Commands/ImportPhotoCommand.cs

Update RhinoPhotoMatchPlugin.cs with photo state properties
Write PicturePlaneManager.cs
36 lines
using System;
using Rhino;
using Rhino.DocObjects;
using Rhino.Geometry;

namespace RhinoPhotoMatch.Core
{
    /// <summary>
    /// Creates and retrieves picture frame objects in the Rhino document.
    /// Placed at the world XY origin, scaled to preserve the photo aspect ratio.
    /// </summary>
    public static class PicturePlaneManager
    {
        /// <summary>
        /// Adds a self-illuminated picture frame at the world origin.
        /// Width and height are in model units — caller is responsible for aspect ratio.
        /// </summary>
        public static Guid AddPictureFrame(RhinoDoc doc, string imagePath, double width, double height)
        {
            return doc.Objects.AddPictureFrame(
                Plane.WorldXY,
                imagePath,
                selfIllumination: true,
                embedBitmap: false,
                width: width,
                height: height);
        }

        /// <summary>Returns the picture frame object, or null if the id is not found.</summary>
        public static RhinoObject? GetPictureFrame(RhinoDoc doc, Guid id)
        {
            return id != Guid.Empty ? doc.Objects.FindId(id) : null;
        }
    }
}
Update Todos

Create Core/PicturePlaneManager.cs

Create Commands/ImportPhotoCommand.cs

Update RhinoPhotoMatchPlugin.cs with photo state properties
Write ImportPhotoCommand.cs
80 lines
using System;
using System.IO;
using Eto.Forms;
using Rhino;
using Rhino.Commands;
using RhinoPhotoMatch.Core;

namespace RhinoPhotoMatch.Commands
{
    /// <summary>
    /// PMImportPhoto — lets the user pick a JPG or PNG file and places it as a
    /// picture frame at the world origin scaled to the correct aspect ratio.
    /// The object ID is stored in the plugin instance for use by other commands.
    /// </summary>
    public class ImportPhotoCommand : Command
    {
        public override string EnglishName => "PMImportPhoto";

        protected override Rhino.Commands.Result RunCommand(RhinoDoc doc, RunMode mode)
        {
            using var dialog = new OpenFileDialog
            {
                Title = "Select Photo (JPG or PNG)",
                Filters =
                {
                    new FileFilter("Image files", ".jpg", ".jpeg", ".png"),
                    new FileFilter("All files", ".*")
                },
                CurrentFilterIndex = 0
            };

            if (dialog.ShowDialog(null) != DialogResult.Ok)
                return Rhino.Commands.Result.Cancel;

            string imagePath = dialog.FileName;
            string ext = Path.GetExtension(imagePath).ToLowerInvariant();

            if (ext != ".jpg" && ext != ".jpeg" && ext != ".png")
            {
                RhinoApp.WriteLine($"PMImportPhoto: unsupported format \"{ext}\". Only JPG and PNG are supported.");
                return Rhino.Commands.Result.Failure;
            }

            // Read pixel dimensions via Eto (available in all Rhino 8 installs)
            int pixelWidth, pixelHeight;
            try
            {
                using var bmp = new Eto.Drawing.Bitmap(imagePath);
                pixelWidth  = bmp.Width;
                pixelHeight = bmp.Height;
            }
            catch (Exception ex)
            {
                RhinoApp.WriteLine($"PMImportPhoto: could not read image — {ex.Message}");
                return Rhino.Commands.Result.Failure;
            }

            // Place frame at world origin; height = 1 model unit, width preserves aspect ratio
            double frameHeight = 1.0;
            double frameWidth  = frameHeight * ((double)pixelWidth / pixelHeight);

            var id = PicturePlaneManager.AddPictureFrame(doc, imagePath, frameWidth, frameHeight);
            if (id == Guid.Empty)
            {
                RhinoApp.WriteLine("PMImportPhoto: failed to add picture frame to document.");
                return Rhino.Commands.Result.Failure;
            }

            var plugin = RhinoPhotoMatchPlugin.Instance;
            plugin.PictureFrameId  = id;
            plugin.PhotoImagePath  = imagePath;

            RhinoApp.WriteLine($"PMImportPhoto: loaded \"{Path.GetFileName(imagePath)}\" ({pixelWidth} x {pixelHeight} px)");
            doc.Views.Redraw();

            return Rhino.Commands.Result.Success;
        }
    }
}
Update Todos

Create Core/PicturePlaneManager.cs

Create Commands/ImportPhotoCommand.cs

Update RhinoPhotoMatchPlugin.cs with photo state properties
Now update the plugin to expose the two new state properties:

Edit RhinoPhotoMatchPlugin.cs
Added 10 lines
Update Todos

Create Core/PicturePlaneManager.cs

Create Commands/ImportPhotoCommand.cs

Update RhinoPhotoMatchPlugin.cs with photo state properties
Feature 2 is done. Files added/changed:

File	What it does
Core/PicturePlaneManager.cs	Creates/retrieves picture frame objects
Commands/ImportPhotoCommand.cs	PMImportPhoto command — file picker, validation, frame placement
RhinoPhotoMatchPlugin.cs	Added PictureFrameId + PhotoImagePath state
The frame is placed at world XY origin with height = 1 model unit and width scaled to the photo's aspect ratio.

Ready to continue with Feature 3: Camera-Linked Picture Planes (PhotoPlaneRegistry, named camera pairs, PMAddPhotoPlane / PMRemovePhotoPlane / PMRelinkPhoto)? Note that Feature 3 also changes the picture plane from a PictureFrame object to a flat mesh with a custom material so alpha transparency works in rendered viewports.

how do we install in rhino?
The user wants to know how to install/load the plugin in Rhino. Let me explain the process for Rhino 8.

There are two ways:

Option A — Drag & Drop (quickest for testing)
Build the project in Visual Studio (Ctrl+Shift+B)
Find the output file: RhinoPhotoMatch/bin/Debug/RhinoPhotoMatch.rhp
Drag the .rhp file directly onto an open Rhino 8 viewport
Rhino will ask to load it — click Yes
Option B — Install permanently via Rhino Options
Build the project
In Rhino: Tools → Options → Plug-ins → Install...
Browse to RhinoPhotoMatch.rhp and open it
Restart Rhino if prompted
Option C — Debug from Visual Studio (best for development)
Add a post-build event to copy the .rhp to Rhino's plugin folder and launch Rhino as the debugger target:

In the .csproj, add:

<PropertyGroup Condition="'$(Configuration)'=='Debug'">
  <StartAction>Program</StartAction>
  <StartProgram>C:\Program Files\Rhino 8\System\Rhino.exe</StartProgram>
  <StartArguments>/nosplash</StartArguments>
</PropertyGroup>
In Project → Properties → Debug, set the executable to Rhino.exe
Press F5 — Visual Studio launches Rhino with the plugin loaded and breakpoints work
Verifying it loaded
In the Rhino command line type:


PMImportPhoto
If the file dialog appears, the plugin is loaded correctly. You should also see RhinoPhotoMatch loaded. in the command history on startup.

Want me to add the debug launch configuration to the .csproj?

can you buld it for me to test
The user wants me to build the project. I'll use the Bash tool to run dotnet build in the project directory.

Bash Build RhinoPhotoMatch plugin
IN
cd "c:/SRC/RHINO/VIEW/RhinoPhotoMatch" && dotnet build 2>&1

OUT
Exit code 155
The command could not be loaded, possibly because:
  * You intended to execute a .NET application:
      The application 'build' does not exist or is not a managed .dll or .exe.
  * You intended to execute a .NET SDK command:
      No .NET SDKs were found.

Download a .NET SDK:
https://aka.ms/dotnet/download

Learn about SDK resolution:
https://aka.ms/dotnet/sdk-not-found
Bash Find available build tools
IN
where msbuild 2>&1; where dotnet 2>&1; ls "/c/Program Files/dotnet/" 2>&1 | head -20; ls "/c/Program Files/Microsoft Visual Studio/" 2>&1 | head -10

OUT
INFO: Could not find files for the given pattern(s).
C:\Program Files\dotnet\dotnet.exe
LICENSE.txt
ThirdPartyNotices.txt
dotnet.exe
host
shared
swidtag
ls: cannot access '/c/Program Files/Microsoft Visual Studio/': No such file or directory
Bash Check installed .NET SDK versions
IN
ls "/c/Program Files/dotnet/sdk/" 2>&1

OUT
Exit code 2
ls: cannot access '/c/Program Files/dotnet/sdk/': No such file or directory
Bash List installed .NET SDKs
IN
"C:/Program Files/dotnet/dotnet.exe" --list-sdks 2>&1

OUT
0
No .NET SDKs are installed. Let me check if there's a Visual Studio installation somewhere else.

Bash Look for Visual Studio or Rhino installations
IN
ls "C:/Program Files/" 2>&1 | grep -i "visual\|rhino\|dotnet\|build"

OUT
MSBuild
Rhino 8
dotnet
Bash Check MSBuild installation
IN
ls "C:/Program Files/MSBuild/" 2>&1

OUT
Microsoft
Bash Find MSBuild.exe
IN
find "C:/Program Files/MSBuild" -name "MSBuild.exe" 2>&1

OUT
The user doesn't want to proceed with this tool use. The tool use was rejected (eg. if it was a file edit, the new_string was NOT written to the file). STOP what you are doing and wait for the user to tell you how to proceed.
there is a build error "C:\SRC\RHINO\VIEW\RhinoPhotoMatch\Commands\ImportPhotoCommand.cs(15,39): error CS0104: 'Command' is an ambiguous reference between 'Rhino.Commands.Command' and 'Eto.Forms.Command' [C:\SRC\RHINO\VIEW\RhinoPhotoMatch\RhinoPhotoMatch.csproj]"
Edit ImportPhotoCommand.cs
Modified
Also remove the redundant using Rhino.Commands; since we're now fully qualifying it, and drop the duplicate Rhino.Commands.Result qualifications that are already consistent — actually let me just remove the ambiguous using instead so the code stays clean:

Edit ImportPhotoCommand.cs
Removed 1 line
That's it. The fix is qualifying the base class as Rhino.Commands.Command explicitly, since Eto.Forms also defines a Command type and the two namespaces clashed. Try building again.

more errors ""C:\SRC\RHINO\VIEW\RhinoPhotoMatch\Commands\ImportPhotoCommand.cs(18,75): error CS0246: The type or namespace name 'RunMode' could not be found (are you missing a using directive or an assembly reference?) [C:\SRC\RHINO\VIEW\RhinoPhotoMatch\RhinoPhotoMatch.csproj]
C:\SRC\RHINO\VIEW\RhinoPhotoMatch\Commands\ImportPhotoCommand.cs(14,18): error CS0534: 'ImportPhotoCommand' does not implement inherited abstract member 'Command.RunCommand(RhinoDoc, RunMode)' [C:\SRC\RHINO\VIEW\RhinoPhotoMatch\RhinoPhotoMatch.csproj]
The issue is that RunMode is in the Rhino.Commands namespace, which we removed. Let me add it back or qualify RunMode fully.

Edit ImportPhotoCommand.cs
Added 2 lines
The alias using EtoCommand = Eto.Forms.Command; is flagged as unnecessary since we're not using it. Let me remove it and instead just keep Rhino.Commands using, since the base class is now fully qualified as Rhino.Commands.Command. The RunMode type is also in Rhino.Commands, so adding back using Rhino.Commands; should fix both errors.

Wait, let me re-check what the current file looks like after my edit. The file now has:

using Rhino.Commands; (re-added)

using EtoCommand = Eto.Forms.Command; (alias, flagged as unnecessary)

Base class: Rhino.Commands.Command (fully qualified)

Since the base class is fully qualified, having both using Rhino.Commands; and using Eto.Forms; won't create ambiguity—the fully qualified name takes precedence, so the unnecessary alias can be safely removed.

Edit ImportPhotoCommand.cs
Removed 1 line
The hint says "Using directive is unnecessary." but it's a hint, not an error. The build errors should be fixed now since we have using Rhino.Commands; which provides RunMode. The hint might be about using Eto.Forms; or some other using. Let me read the current file to see its state.

The hint is cosmetic (severity: Hint, not Error) — the build errors about RunMode should now be resolved. Try building again and let me know if there are any remaining errors.

C:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\PicturePlaneManager.cs(20,32): error CS7036: There is no argument given that corresponds to the required parameter 'asMesh' of 'ObjectTable.AddPictureFrame(Plane, string, bool, double, double, bool, bool)' [C:\SRC\RHINO\VIEW\RhinoPhotoMatch\RhinoPhotoMatch.csproj]
The AddPictureFrame method signature is different from what I assumed. The error says:
ObjectTable.AddPictureFrame(Plane, string, bool, double, double, bool, bool)

So the actual signature is:


AddPictureFrame(Plane plane, string bitmapFileName, bool selfIllumination, double width, double height, bool embedBitmap, bool asMesh)
Let me fix the call in PicturePlaneManager.cs.

The actual Rhino 8 signature is AddPictureFrame(Plane, string, bool selfIllumination, double width, double height, bool embedBitmap, bool asMesh). Let me fix it:

Edit PicturePlaneManager.cs
Added 1 line
Try building again.

C:\SRC\RHINO\VIEW\RhinoPhotoMatch\Commands\ImportPhotoCommand.cs(65,13): error CS8803: Top-level statements must precede namespace and type declarations. [C:\SRC\RHINO\VIEW\RhinoPhotoMatch\RhinoPhotoMatch.csproj]
Looking at the system reminder, it seems the linter/user modified the file and the code after the RunCommand method's closing braces is now outside the class/namespace - it appears the code got split. Lines 59-62 show:


            return Rhino.Commands.Result.Success;
        }
    }
}

            // Place frame at world origin; height = 1 model unit, width preserves aspect ratio
            double frameHeight = 1.0;
The code that should be inside RunCommand ended up after the closing braces. I need to read the full file and rewrite it.

The file got corrupted — code ended up outside the class. Let me read the current state and rewrite it:

Read ImportPhotoCommand.cs
Write ImportPhotoCommand.cs
80 lines
using System;
using System.IO;
using Eto.Forms;
using Rhino;
using Rhino.Commands;
using RhinoPhotoMatch.Core;

namespace RhinoPhotoMatch.Commands
{
    /// <summary>
    /// PMImportPhoto — lets the user pick a JPG or PNG file and places it as a
    /// picture frame at the world origin scaled to the correct aspect ratio.
    /// The object ID is stored in the plugin instance for use by other commands.
    /// </summary>
    public class ImportPhotoCommand : Rhino.Commands.Command
    {
        public override string EnglishName => "PMImportPhoto";

        protected override Rhino.Commands.Result RunCommand(RhinoDoc doc, RunMode mode)
        {
            using var dialog = new OpenFileDialog
            {
                Title = "Select Photo (JPG or PNG)",
                Filters =
                {
                    new FileFilter("Image files", ".jpg", ".jpeg", ".png"),
                    new FileFilter("All files", ".*")
                },
                CurrentFilterIndex = 0
            };

            if (dialog.ShowDialog(null) != DialogResult.Ok)
                return Rhino.Commands.Result.Cancel;

            string imagePath = dialog.FileName;
            string ext = Path.GetExtension(imagePath).ToLowerInvariant();

            if (ext != ".jpg" && ext != ".jpeg" && ext != ".png")
            {
                RhinoApp.WriteLine($"PMImportPhoto: unsupported format \"{ext}\". Only JPG and PNG are supported.");
                return Rhino.Commands.Result.Failure;
            }

            // Read pixel dimensions via Eto (available in all Rhino 8 installs)
            int pixelWidth, pixelHeight;
            try
            {
                using var bmp = new Eto.Drawing.Bitmap(imagePath);
                pixelWidth  = bmp.Width;
                pixelHeight = bmp.Height;
            }
            catch (Exception ex)
            {
                RhinoApp.WriteLine($"PMImportPhoto: could not read image — {ex.Message}");
                return Rhino.Commands.Result.Failure;
            }

            // Place frame at world origin; height = 1 model unit, width preserves aspect ratio
            double frameHeight = 1.0;
            double frameWidth  = frameHeight * ((double)pixelWidth / pixelHeight);

            var id = PicturePlaneManager.AddPictureFrame(doc, imagePath, frameWidth, frameHeight);
            if (id == Guid.Empty)
            {
                RhinoApp.WriteLine("PMImportPhoto: failed to add picture frame to document.");
                return Rhino.Commands.Result.Failure;
            }

            var plugin = RhinoPhotoMatchPlugin.Instance;
            plugin.PictureFrameId = id;
            plugin.PhotoImagePath = imagePath;

            RhinoApp.WriteLine($"PMImportPhoto: loaded \"{Path.GetFileName(imagePath)}\" ({pixelWidth} x {pixelHeight} px)");
            doc.Views.Redraw();

            return Rhino.Commands.Result.Success;
        }
    }
}
File restored. Try building again.

give me a list of all the commands in the plugin and what they do
Based on the spec, here are all commands the plugin will have:

Implemented

Command	File	What it does
PMImportPhoto	Commands/ImportPhotoCommand.cs	Opens a JPG/PNG file picker, places it as a picture frame at world origin scaled to aspect ratio
Not yet implemented

Command	Feature	What it does
PMAddPhotoPlane	Camera-Linked Planes	Adds a new picture plane / named camera pair (can be called multiple times)
PMRemovePhotoPlane	Camera-Linked Planes	Removes a pair, warns before deleting both objects
PMRelinkPhoto	Camera-Linked Planes	Re-points the material texture to a new file path and refreshes the viewport
PMSetReferencePoints	Calibration (PnP)	Guides user through picking matched 3D model points and 2D photo points
PMCalibrate	Calibration (PnP)	Runs SolvePnP on the stored point pairs, applies result to the named viewport
PMSetVanishingLines	Calibration (Vanishing)	Lets user draw line pairs on the photo to derive camera orientation via vanishing points
PMToggleOverlay	Overlay	Turns the wireframe projection overlay on/off in the calibrated viewport
PMLockView	View Lock	Toggles camera movement lock on the calibrated viewport
PMSaveSession	Session	Serializes calibration state to JSON stored in the Rhino document
PMLoadSession	Session	Restores calibration state from the document, prompts to relink if photo has moved
Want to continue building? The next logical step is Feature 3: Camera-Linked Picture Planes (PMAddPhotoPlane, PMRemovePhotoPlane, PMRelinkPhoto + PhotoPlaneRegistry).

PMImport works, lets continue building features
The user confirmed PMImportPhoto works and wants to continue with Feature 3: Camera-Linked Picture Planes. Let me plan what needs to be built:

Core/PhotoPlanePair.cs - record type holding pair data

Core/PhotoPlaneRegistry.cs - tracks all pairs, manages naming

Update Core/PicturePlaneManager.cs - add flat mesh + material creation (replacing PictureFrame approach)

Update Commands/ImportPhotoCommand.cs - integrate with registry, create named view

Commands/AddPhotoPlaneCommand.cs - PMAddPhotoPlane

Commands/RemovePhotoPlaneCommand.cs - PMRemovePhotoPlane

Commands/RelinkPhotoCommand.cs - PMRelinkPhoto

The core approach shifts from PictureFrame to a flat mesh with custom material for proper alpha transparency, paired with a named camera view. I'll auto-increment names like PM_Photo_01, PM_Photo_02 and let users optionally rename after creation. For the mesh itself, I need a flat plane at the world origin sized to match the photo's aspect ratio.

Setting up the material involves applying the photo as a diffuse texture, using the same image as a transparency map for PNGs, and enabling self-illumination. I'll use RhinoDoc.NamedViews.Add() to create the paired camera view, and the material API to add textures and set properties like diffuse color.

I should also update ImportPhotoCommand to integrate with the new PhotoPlaneRegistry system rather than working independently.

Looking at Feature 2 versus Feature 3, I realize there's a conflict: Feature 2 uses PictureFrame (which the user confirmed is working), but Feature 3 explicitly requires a flat mesh with custom material instead to ensure alpha transparency renders correctly. Since Feature 3 is what we're implementing now, I need to refactor PicturePlaneManager to create a mesh-based approach rather than relying on PictureFrame.

Defining the data structures... Writing the implementation files...

For the named view, I'll grab the active viewport's info as a starting point and add it to the document's named views collection with the pair identifier as the name. Then for the removal logic, I need to handle deleting the mesh object, cleaning up its material, and removing the associated named view from the registry.

For the other commands, PMRemovePhotoPlane will present the user with a list of existing pairs to choose from, warn them before deletion, and remove everything. PMRelinkPhoto will let them select a pair, pick a new image file, update the material's texture, and refresh the viewport.

The registry will live in memory for now as part of the plugin instance, though the spec mentions it should eventually integrate with the session save/restore feature. I'm leaning toward making PhotoPlanePair a mutable class rather than a record so I can update properties like the image path when relinking without creating new instances.

Now I'm mapping out the file structure: the data class, the registry itself, updates to the manager for mesh and material creation, modifications to the import command, and separate command files for adding, removing, and relinking photo planes.

I'm realizing that PMAddPhotoPlane and PMImportPhoto overlap significantly—both handle importing photos and creating plane-camera pairs. Looking at the spec, PMImportPhoto is the basic import while PMAddPhotoPlane is the full-featured version that can be called multiple times. I should update ImportPhotoCommand to support the new registry-based approach and have both commands leverage it. I'm settling on a clean implementation plan: I'll create the core classes for managing photo plane pairs, build out the command structure for adding and removing them, and update the existing import logic to integrate with the new registry system.

Now I'm writing the mesh creation code... Setting up the material properties to make it self-illuminated by configuring the emission color to white and removing reflectivity and shine, though I'm reconsidering whether there's a more direct self-illumination property available in the Rhino API that I should be using instead. Setting the material source to use the object's material and assigning the material index, either when creating the mesh or modifying it afterward. For the named view, I need to create it after placing the picture plane and name both the view and plane with a matching identifier like PM_Photo_01. Now I'll write out all the necessary files.

For the photo plane registry, I'm building a system that lives in the plugin instance to track pairs of photo planes and named views, with methods to create, find, and remove them while auto-generating incremented names. For the remove command, I'll use Rhino's input system to let the user select which pair to delete from a list of available options.

Writing the registry and command code... I'll consolidate the confirmation flow for removal and then clarify the relationship between the import and add commands—they'll share the same registry logic, with PMImportPhoto being the quick version and PMAddPhotoPlane offering a clearer interface. Now I'm ready to start implementing the code.

Defining the data structures... I'm seeing the rest of the PhotoPlaneRegistry class with the FindByName method, and now I'm thinking about how to restructure this. I should move the mesh and material creation logic into PicturePlaneManager as lower-level helpers, while PhotoPlaneRegistry handles the orchestration and tracking of photo plane pairs.

For the plugin class itself, I'm replacing the old PhotoPlanePairs list with a PhotoPlaneRegistry instance that becomes the single source of truth. The PictureFrameId and PhotoImagePath fields from the earlier single-photo feature are now obsolete with this registry approach, though I should consider whether to keep them for backward compatibility.

Actually, since Feature 3 fundamentally shifts from picture frames to meshes, I'll clean this up properly and remove those old properties entirely—the Registry will handle all the state management going forward. Now I need to outline which files to create and update.

Writing the implementation code...

Feature 3 is the biggest one — it replaces the PictureFrame approach with flat mesh + material (for alpha support), adds named cameras, and introduces the registry. Let me plan and build it all.

Update Todos

Create Core/PhotoPlanePair.cs data class

Create Core/PhotoPlaneRegistry.cs

Update Core/PicturePlaneManager.cs — flat mesh + material methods

Update Commands/ImportPhotoCommand.cs — integrate registry

Create Commands/AddPhotoPlaneCommand.cs

Create Commands/RemovePhotoPlaneCommand.cs

Create Commands/RelinkPhotoCommand.cs

Update RhinoPhotoMatchPlugin.cs — swap to Registry
Write PhotoPlanePair.cs
35 lines
using System;

namespace RhinoPhotoMatch.Core
{
    /// <summary>
    /// Holds all data for a single picture plane / named camera pair.
    /// One pair is created each time PMAddPhotoPlane (or PMImportPhoto) is run.
    /// </summary>
    public class PhotoPlanePair
    {
        public PhotoPlanePair(string name, Guid meshObjectId, string imagePath, int materialIndex)
        {
            Name          = name;
            MeshObjectId  = meshObjectId;
            ImagePath     = imagePath;
            MaterialIndex = materialIndex;
        }

        /// <summary>Shared identifier for the mesh and its linked named view, e.g. PM_Photo_01.</summary>
        public string Name { get; set; }

        /// <summary>Object ID of the flat mesh in the Rhino document.</summary>
        public Guid MeshObjectId { get; set; }

        /// <summary>Absolute path of the source image file.</summary>
        public string ImagePath { get; set; }

        /// <summary>Index into RhinoDoc.Materials for this plane's dedicated material.</summary>
        public int MaterialIndex { get; set; }

        /// <summary>Named view name mirrors Name — kept explicit for clarity.</summary>
        public string NamedViewName => Name;
    }
}
Update Todos

Create Core/PhotoPlanePair.cs data class

Create Core/PhotoPlaneRegistry.cs

Update Core/PicturePlaneManager.cs — flat mesh + material methods

Update Commands/ImportPhotoCommand.cs — integrate registry

Create Commands/AddPhotoPlaneCommand.cs

Create Commands/RemovePhotoPlaneCommand.cs

Create Commands/RelinkPhotoCommand.cs

Update RhinoPhotoMatchPlugin.cs — swap to Registry
Write PhotoPlaneRegistry.cs
139 lines
using System;
using System.Collections.Generic;
using System.IO;
using System.Linq;
using Rhino;
using Rhino.DocObjects;
using Rhino.Geometry;

namespace RhinoPhotoMatch.Core
{
    /// <summary>
    /// Tracks all picture plane / named camera pairs for the current session.
    /// This is the single source of truth — commands read and write through here.
    /// </summary>
    public class PhotoPlaneRegistry
    {
        private readonly List<PhotoPlanePair> _pairs = new();

        public IReadOnlyList<PhotoPlanePair> Pairs => _pairs;

        // ------------------------------------------------------------------ //
        //  Create
        // ------------------------------------------------------------------ //

        /// <summary>
        /// Adds a new photo plane + named camera pair using the supplied image.
        /// Returns null and logs to the command line on failure.
        /// </summary>
        public PhotoPlanePair? CreatePair(RhinoDoc doc, string imagePath, string name)
        {
            // 1. Read image dimensions
            int pw, ph;
            try
            {
                using var bmp = new Eto.Drawing.Bitmap(imagePath);
                pw = bmp.Width;
                ph = bmp.Height;
            }
            catch (Exception ex)
            {
                RhinoApp.WriteLine($"PhotoPlaneRegistry: cannot read image — {ex.Message}");
                return null;
            }

            double frameHeight = 1.0;
            double frameWidth  = frameHeight * ((double)pw / ph);

            // 2. Create material (each plane gets its own instance)
            int matIdx = PicturePlaneManager.CreatePhotoMaterial(doc, imagePath);
            if (matIdx < 0)
            {
                RhinoApp.WriteLine("PhotoPlaneRegistry: failed to create material.");
                return null;
            }

            // 3. Add flat mesh with that material
            var meshId = PicturePlaneManager.AddPhotoPlane(doc, frameWidth, frameHeight, matIdx);
            if (meshId == Guid.Empty)
            {
                RhinoApp.WriteLine("PhotoPlaneRegistry: failed to add mesh to document.");
                return null;
            }

            // 4. Capture current viewport as the starting named view (camera)
            var vpInfo = new ViewportInfo(doc.Views.ActiveView.ActiveViewport);
            doc.NamedViews.Add(name, vpInfo);

            var pair = new PhotoPlanePair(name, meshId, imagePath, matIdx);
            _pairs.Add(pair);

            RhinoApp.WriteLine($"Photo plane \"{name}\" created — {pw} x {ph} px  |  \"{Path.GetFileName(imagePath)}\"");
            return pair;
        }

        // ------------------------------------------------------------------ //
        //  Remove
        // ------------------------------------------------------------------ //

        public void RemovePair(RhinoDoc doc, PhotoPlanePair pair)
        {
            // Delete mesh object
            doc.Objects.Delete(pair.MeshObjectId, quiet: true);

            // Delete material (set to unused first to avoid Rhino warnings)
            if (pair.MaterialIndex >= 0 && pair.MaterialIndex < doc.Materials.Count)
                doc.Materials.DeleteAt(pair.MaterialIndex);

            // Remove named view
            int nvIdx = doc.NamedViews.FindByName(pair.NamedViewName);
            if (nvIdx >= 0)
                doc.NamedViews.Delete(nvIdx);

            _pairs.Remove(pair);
            RhinoApp.WriteLine($"Photo plane \"{pair.Name}\" removed.");
        }

        // ------------------------------------------------------------------ //
        //  Relink
        // ------------------------------------------------------------------ //

        public void RelinkPhoto(RhinoDoc doc, PhotoPlanePair pair, string newImagePath)
        {
            var mat = doc.Materials[pair.MaterialIndex];
            mat.SetBitmapTexture(newImagePath);

            string ext = Path.GetExtension(newImagePath).ToLowerInvariant();
            if (ext == ".png")
                mat.SetTransparencyTexture(newImagePath);
            else
                mat.SetTransparencyTexture(string.Empty);

            mat.CommitChanges();
            pair.ImagePath = newImagePath;

            doc.Views.Redraw();
            RhinoApp.WriteLine($"\"{pair.Name}\" relinked to \"{Path.GetFileName(newImagePath)}\".");
        }

        // ------------------------------------------------------------------ //
        //  Helpers
        // ------------------------------------------------------------------ //

        public PhotoPlanePair? FindByName(string name) =>
            _pairs.FirstOrDefault(p => p.Name.Equals(name, StringComparison.OrdinalIgnoreCase));

        /// <summary>Generates the next available auto-name, e.g. PM_Photo_01.</summary>
        public string GenerateAutoName()
        {
            for (int i = 1; i < 100; i++)
            {
                string candidate = $"PM_Photo_{i:D2}";
                if (_pairs.All(p => !p.Name.Equals(candidate, StringComparison.OrdinalIgnoreCase)))
                    return candidate;
            }
            return $"PM_Photo_{Guid.NewGuid():N}";
        }
    }
}
Update Todos

Create Core/PhotoPlanePair.cs data class

Create Core/PhotoPlaneRegistry.cs

Update Core/PicturePlaneManager.cs — flat mesh + material methods

Update Commands/ImportPhotoCommand.cs — integrate registry

Create Commands/AddPhotoPlaneCommand.cs

Create Commands/RemovePhotoPlaneCommand.cs

Create Commands/RelinkPhotoCommand.cs

Update RhinoPhotoMatchPlugin.cs — swap to Registry
Now rewrite PicturePlaneManager to produce flat meshes + materials instead of PictureFrames:

Write PicturePlaneManager.cs
104 lines
using System;
using System.Drawing;
using System.IO;
using Rhino;
using Rhino.DocObjects;
using Rhino.Geometry;

namespace RhinoPhotoMatch.Core
{
    /// <summary>
    /// Low-level helpers for creating photo plane meshes and their dedicated materials.
    /// Each photo plane is a flat quad mesh with a unique material — NOT a Rhino PictureFrame —
    /// so that alpha-transparent PNG cutouts work correctly in rendered viewports.
    /// </summary>
    public static class PicturePlaneManager
    {
        // ------------------------------------------------------------------ //
        //  Mesh
        // ------------------------------------------------------------------ //

        /// <summary>
        /// Adds a flat quad mesh at the world XY origin with UV coordinates mapped
        /// to fill the texture, and assigns the given material to it.
        /// Returns Guid.Empty on failure.
        /// </summary>
        public static Guid AddPhotoPlane(RhinoDoc doc, double width, double height, int materialIndex)
        {
            var mesh = BuildQuadMesh(width, height);

            var attr = new ObjectAttributes
            {
                MaterialSource = ObjectMaterialSource.MaterialFromObject,
                MaterialIndex  = materialIndex,
                Name           = "PhotoPlane"
            };

            return doc.Objects.AddMesh(mesh, attr);
        }

        private static Mesh BuildQuadMesh(double width, double height)
        {
            var mesh = new Mesh();

            // Vertices — bottom-left origin, lying flat in XY plane
            mesh.Vertices.Add(0,     0,      0);  // 0: bottom-left
            mesh.Vertices.Add(width, 0,      0);  // 1: bottom-right
            mesh.Vertices.Add(width, height, 0);  // 2: top-right
            mesh.Vertices.Add(0,     height, 0);  // 3: top-left

            // UV coordinates matching the corners above
            mesh.TextureCoordinates.Add(0, 0);
            mesh.TextureCoordinates.Add(1, 0);
            mesh.TextureCoordinates.Add(1, 1);
            mesh.TextureCoordinates.Add(0, 1);

            mesh.Faces.AddFace(0, 1, 2, 3);
            mesh.Normals.ComputeNormals();
            mesh.Compact();

            return mesh;
        }

        // ------------------------------------------------------------------ //
        //  Material
        // ------------------------------------------------------------------ //

        /// <summary>
        /// Creates a new self-illuminated material with the photo as the diffuse texture.
        /// For PNG files the same texture is also used as the transparency map.
        /// Returns the material index in RhinoDoc.Materials, or -1 on failure.
        /// </summary>
        public static int CreatePhotoMaterial(RhinoDoc doc, string imagePath)
        {
            var material = new Material
            {
                Name             = $"PM_{Path.GetFileNameWithoutExtension(imagePath)}",
                DiffuseColor     = Color.White,
                // Full emission makes the surface self-illuminated (unaffected by scene lighting)
                EmissionColor    = Color.White,
                Reflectivity     = 0,
                Shine            = 0,
                Transparency     = 0
            };

            material.SetBitmapTexture(imagePath);

            string ext = Path.GetExtension(imagePath).ToLowerInvariant();
            if (ext == ".png")
                material.SetTransparencyTexture(imagePath);

            int index = doc.Materials.Add(material);
            return index;
        }

        // ------------------------------------------------------------------ //
        //  Retrieval
        // ------------------------------------------------------------------ //

        /// <summary>Returns the photo plane mesh object, or null if not found.</summary>
        public static RhinoObject? GetPhotoPlane(RhinoDoc doc, Guid id) =>
            id != Guid.Empty ? doc.Objects.FindId(id) : null;
    }
}
Update Todos

Create Core/PhotoPlanePair.cs data class

Create Core/PhotoPlaneRegistry.cs

Update Core/PicturePlaneManager.cs — flat mesh + material methods

Update Commands/ImportPhotoCommand.cs — integrate registry

Create Commands/AddPhotoPlaneCommand.cs

Create Commands/RemovePhotoPlaneCommand.cs

Create Commands/RelinkPhotoCommand.cs

Update RhinoPhotoMatchPlugin.cs — swap to Registry
Write ImportPhotoCommand.cs
24 lines
using System.IO;
using Eto.Forms;
using Rhino;
using Rhino.Commands;
using Rhino.Input;
using RhinoPhotoMatch.Core;

namespace RhinoPhotoMatch.Commands
{
    /// <summary>
    /// PMImportPhoto — file picker shortcut that delegates to PhotoPlaneRegistry.CreatePair.
    /// Equivalent to PMAddPhotoPlane; kept for discoverability.
    /// </summary>
    public class ImportPhotoCommand : Rhino.Commands.Command
    {
        public override string EnglishName => "PMImportPhoto";

        protected override Result RunCommand(RhinoDoc doc, RunMode mode)
        {
            return AddPhotoPlaneCommand.RunShared(doc);
        }
    }
}
Update Todos

Create Core/PhotoPlanePair.cs data class

Create Core/PhotoPlaneRegistry.cs

Update Core/PicturePlaneManager.cs — flat mesh + material methods

Update Commands/ImportPhotoCommand.cs — integrate registry

Create Commands/AddPhotoPlaneCommand.cs

Create Commands/RemovePhotoPlaneCommand.cs

Create Commands/RelinkPhotoCommand.cs

Update RhinoPhotoMatchPlugin.cs — swap to Registry
Write AddPhotoPlaneCommand.cs
75 lines
using System.IO;
using Eto.Forms;
using Rhino;
using Rhino.Commands;
using Rhino.Input;
using RhinoPhotoMatch.Core;

namespace RhinoPhotoMatch.Commands
{
    /// <summary>
    /// PMAddPhotoPlane — picks a JPG or PNG, creates a flat mesh photo plane and a linked
    /// named camera. Can be run multiple times to add multiple planes to the same document.
    /// </summary>
    public class AddPhotoPlaneCommand : Rhino.Commands.Command
    {
        public override string EnglishName => "PMAddPhotoPlane";

        protected override Result RunCommand(RhinoDoc doc, RunMode mode) => RunShared(doc);

        /// <summary>Shared logic used by both PMAddPhotoPlane and PMImportPhoto.</summary>
        internal static Result RunShared(RhinoDoc doc)
        {
            // 1. File picker
            using var dialog = new OpenFileDialog
            {
                Title = "Select Photo (JPG or PNG)",
                Filters =
                {
                    new FileFilter("Image files", ".jpg", ".jpeg", ".png"),
                    new FileFilter("All files", ".*")
                },
                CurrentFilterIndex = 0
            };

            if (dialog.ShowDialog(null) != DialogResult.Ok)
                return Result.Cancel;

            string imagePath = dialog.FileName;
            string ext = Path.GetExtension(imagePath).ToLowerInvariant();

            if (ext != ".jpg" && ext != ".jpeg" && ext != ".png")
            {
                RhinoApp.WriteLine($"PMAddPhotoPlane: unsupported format \"{ext}\". Only JPG and PNG are supported.");
                return Result.Failure;
            }

            // 2. Auto-generate a name; let user optionally rename it
            var registry = RhinoPhotoMatchPlugin.Instance.Registry;
            string autoName = registry.GenerateAutoName();

            string name = autoName;
            if (RhinoGet.GetString($"Name for this photo plane <{autoName}>", true, ref name) == Result.Cancel)
                return Result.Cancel;

            if (string.IsNullOrWhiteSpace(name))
                name = autoName;

            // Ensure the name is unique
            if (registry.FindByName(name) != null)
            {
                RhinoApp.WriteLine($"PMAddPhotoPlane: a photo plane named \"{name}\" already exists.");
                return Result.Failure;
            }

            // 3. Create pair via registry
            var pair = registry.CreatePair(doc, imagePath, name);
            if (pair == null)
                return Result.Failure;

            doc.Views.Redraw();
            return Result.Success;
        }
    }
}
Update Todos

Create Core/PhotoPlanePair.cs data class

Create Core/PhotoPlaneRegistry.cs

Update Core/PicturePlaneManager.cs — flat mesh + material methods

Update Commands/ImportPhotoCommand.cs — integrate registry

Create Commands/AddPhotoPlaneCommand.cs

Create Commands/RemovePhotoPlaneCommand.cs

Create Commands/RelinkPhotoCommand.cs

Update RhinoPhotoMatchPlugin.cs — swap to Registry
Write RemovePhotoPlaneCommand.cs
62 lines
using Rhino;
using Rhino.Commands;
using Rhino.Input;
using Rhino.Input.Custom;
using RhinoPhotoMatch.Core;

namespace RhinoPhotoMatch.Commands
{
    /// <summary>
    /// PMRemovePhotoPlane — lets the user pick a photo plane by name and deletes both
    /// the mesh and its linked named camera after a confirmation prompt.
    /// </summary>
    public class RemovePhotoPlaneCommand : Rhino.Commands.Command
    {
        public override string EnglishName => "PMRemovePhotoPlane";

        protected override Result RunCommand(RhinoDoc doc, RunMode mode)
        {
            var registry = RhinoPhotoMatchPlugin.Instance.Registry;

            if (registry.Pairs.Count == 0)
            {
                RhinoApp.WriteLine("PMRemovePhotoPlane: no photo planes in this document.");
                return Result.Nothing;
            }

            // Build option list from registered pair names
            var go = new GetOption();
            go.SetCommandPrompt("Select photo plane to remove");

            foreach (var pair in registry.Pairs)
                go.AddOption(pair.Name.Replace(" ", "_")); // option tokens can't have spaces

            go.Get();
            if (go.CommandResult() != Result.Success)
                return go.CommandResult();

            int idx = go.Option().Index - 1; // GetOption indices are 1-based
            if (idx < 0 || idx >= registry.Pairs.Count)
                return Result.Failure;

            var target = registry.Pairs[idx];

            // Confirmation
            string confirm = "No";
            RhinoApp.WriteLine($"This will permanently delete \"{target.Name}\" and its linked camera.");
            if (RhinoGet.GetString("Continue? (Yes/No)", false, ref confirm) != Result.Success)
                return Result.Cancel;

            if (!confirm.StartsWith("Y", System.StringComparison.OrdinalIgnoreCase))
            {
                RhinoApp.WriteLine("PMRemovePhotoPlane: cancelled.");
                return Result.Cancel;
            }

            registry.RemovePair(doc, target);
            doc.Views.Redraw();
            return Result.Success;
        }
    }
}
Update Todos

Create Core/PhotoPlanePair.cs data class

Create Core/PhotoPlaneRegistry.cs

Update Core/PicturePlaneManager.cs — flat mesh + material methods

Update Commands/ImportPhotoCommand.cs — integrate registry

Create Commands/AddPhotoPlaneCommand.cs

Create Commands/RemovePhotoPlaneCommand.cs

Create Commands/RelinkPhotoCommand.cs

Update RhinoPhotoMatchPlugin.cs — swap to Registry
Write RelinkPhotoCommand.cs
79 lines
using System.IO;
using Eto.Forms;
using Rhino;
using Rhino.Commands;
using Rhino.Input.Custom;
using RhinoPhotoMatch.Core;

namespace RhinoPhotoMatch.Commands
{
    /// <summary>
    /// PMRelinkPhoto — re-points a photo plane's material texture to a new file path.
    /// Use this after editing the photo externally (e.g. removing background in Photoshop).
    /// </summary>
    public class RelinkPhotoCommand : Rhino.Commands.Command
    {
        public override string EnglishName => "PMRelinkPhoto";

        protected override Result RunCommand(RhinoDoc doc, RunMode mode)
        {
            var registry = RhinoPhotoMatchPlugin.Instance.Registry;

            if (registry.Pairs.Count == 0)
            {
                RhinoApp.WriteLine("PMRelinkPhoto: no photo planes in this document.");
                return Result.Nothing;
            }

            // 1. Pick which plane to relink
            var go = new GetOption();
            go.SetCommandPrompt("Select photo plane to relink");

            foreach (var pair in registry.Pairs)
                go.AddOption(pair.Name.Replace(" ", "_"));

            go.Get();
            if (go.CommandResult() != Result.Success)
                return go.CommandResult();

            int idx = go.Option().Index - 1;
            if (idx < 0 || idx >= registry.Pairs.Count)
                return Result.Failure;

            var target = registry.Pairs[idx];

            // 2. Pick the new image file
            using var dialog = new OpenFileDialog
            {
                Title = $"Select new image for \"{target.Name}\"",
                Filters =
                {
                    new FileFilter("Image files", ".jpg", ".jpeg", ".png"),
                    new FileFilter("All files", ".*")
                },
                CurrentFilterIndex = 0
            };

            // Start in the same folder as the current image if it still exists
            if (File.Exists(target.ImagePath))
                dialog.Directory = new Eto.IO.DirectoryInfo(Path.GetDirectoryName(target.ImagePath)!);

            if (dialog.ShowDialog(null) != DialogResult.Ok)
                return Result.Cancel;

            string newPath = dialog.FileName;
            string ext = Path.GetExtension(newPath).ToLowerInvariant();

            if (ext != ".jpg" && ext != ".jpeg" && ext != ".png")
            {
                RhinoApp.WriteLine($"PMRelinkPhoto: unsupported format \"{ext}\". Only JPG and PNG are supported.");
                return Result.Failure;
            }

            // 3. Update material and refresh
            registry.RelinkPhoto(doc, target, newPath);
            return Result.Success;
        }
    }
}
Update Todos

Create Core/PhotoPlanePair.cs data class

Create Core/PhotoPlaneRegistry.cs

Update Core/PicturePlaneManager.cs — flat mesh + material methods

Update Commands/ImportPhotoCommand.cs — integrate registry

Create Commands/AddPhotoPlaneCommand.cs

Create Commands/RemovePhotoPlaneCommand.cs

Create Commands/RelinkPhotoCommand.cs

Update RhinoPhotoMatchPlugin.cs — swap to Registry
Write RhinoPhotoMatchPlugin.cs
42 lines
using System;
using System.Collections.Generic;
using Rhino;
using Rhino.PlugIns;
using RhinoPhotoMatch.Core;

[assembly: System.Runtime.InteropServices.Guid("A8E3F2C1-D4B7-4A5E-9C3F-B2D8E1A6C9F4")]

namespace RhinoPhotoMatch
{
    public class RhinoPhotoMatchPlugin : PlugIn
    {
        public RhinoPhotoMatchPlugin()
        {
            Instance = this;
        }

        public static RhinoPhotoMatchPlugin Instance { get; private set; } = null!;

        public override PlugInLoadTime LoadTime => PlugInLoadTime.AtStartup;

        // --- Feature: Camera-Linked Picture Planes ---

        /// <summary>
        /// Registry of all photo plane / named camera pairs for this session.
        /// All commands read and write through here.
        /// </summary>
        public PhotoPlaneRegistry Registry { get; } = new PhotoPlaneRegistry();

        // --- Feature: Camera Calibration ---

        /// <summary>3D–2D point correspondences used by the calibration solver.</summary>
        public List<(Rhino.Geometry.Point3d WorldPoint, Rhino.Geometry.Point2d ImagePoint)> ReferencePairs { get; } = new();

        protected override LoadReturnCode OnLoad(ref string errorMessage)
        {
            RhinoApp.WriteLine("RhinoPhotoMatch loaded.");
            return LoadReturnCode.Success;
        }
    }
}
Update Todos

Create Core/PhotoPlanePair.cs data class

Create Core/PhotoPlaneRegistry.cs

Update Core/PicturePlaneManager.cs — flat mesh + material methods

Update Commands/ImportPhotoCommand.cs — integrate registry

Create Commands/AddPhotoPlaneCommand.cs

Create Commands/RemovePhotoPlaneCommand.cs

Create Commands/RelinkPhotoCommand.cs

Update RhinoPhotoMatchPlugin.cs — swap to Registry
Feature 3 is done. Here's what changed:

New files:

File	Purpose
Core/PhotoPlanePair.cs	Data record: mesh ID, named view name, image path, material index
Core/PhotoPlaneRegistry.cs	Creates/removes/relinks pairs; generates auto-names (PM_Photo_01…)
Commands/AddPhotoPlaneCommand.cs	PMAddPhotoPlane — file picker → mesh + material + named view
Commands/RemovePhotoPlaneCommand.cs	PMRemovePhotoPlane — pick from list, confirm, delete both objects
Commands/RelinkPhotoCommand.cs	PMRelinkPhoto — re-points material texture, refreshes viewport
Updated files:

Core/PicturePlaneManager.cs — now builds flat quad mesh + self-illuminated material (no more PictureFrame)
Commands/ImportPhotoCommand.cs — now delegates to AddPhotoPlaneCommand.RunShared
RhinoPhotoMatchPlugin.cs — replaced PhotoPlanePairs + PictureFrameId with a single Registry
Try building, then we can move on to Feature 4: Camera Calibration (PnP).

C:\SRC\RHINO\VIEW\RhinoPhotoMatch\Commands\RelinkPhotoCommand.cs(59,47): error CS0234: The type or namespace name 'DirectoryInfo' does not exist in the namespace 'Eto.IO' (are you missing an assembly reference?) [C:\SRC\RHINO\VIEW\RhinoPhotoMatch\RhinoPhotoMatch.csproj]
C:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\PhotoPlaneRegistry.cs(66,38): error CS1503: Argument 2: cannot convert from 'Rhino.DocObjects.ViewportInfo' to 'System.Guid' [C:\SRC\RHINO\VIEW\RhinoPhotoMatch\RhinoPhotoMatch.csproj]
Two errors to fix:

Eto.IO.DirectoryInfo doesn't exist - I should just use System.IO.DirectoryInfo or remove the directory-setting line entirely (or use a different approach for the OpenFileDialog initial directory).

doc.NamedViews.Add(name, vpInfo) - the signature is wrong. Let me think about the correct signature for RhinoDoc.NamedViews.Add. The correct method is NamedViewTable.Add(string name, ViewportInfo viewport) but checking the RhinoCommon API... Actually it might be doc.NamedViews.Add(viewportInfo) and the name is set on the ViewportInfo, or there's a different overload.

Looking at the RhinoCommon API more carefully, the error message says it's expecting a Guid as the second parameter, not a ViewportInfo. So the Add method signature must be different than what I thought - it's probably Add(string name, Guid viewId) or something similar, which means I need to figure out how to get the view ID from the viewport info or use a different approach entirely. I'm realizing the FileDialog.Directory property in Eto is actually a Uri type, so I need to convert the directory path using new Uri() rather than passing a string directly. I'm checking whether FileDialog.Directory expects a string or Uri in Eto.Forms, and since setting the initial directory is optional, I could skip it entirely or wrap it in a try-catch to avoid breaking the dialog if the property type doesn't match what I'm passing.

For the NamedViewTable.Add issue, the error suggests it's expecting a Guid as the second parameter rather than ViewportInfo, so I need to investigate what the actual method signature is and whether I should be using ViewInfo instead. I'm realizing the API signature might expect a name and a Guid parameter based on that error message, so I need to figure out what that Guid should be—possibly the viewport's ID or something from the active view. I think the issue is that NamedViews.Add expects a Guid for the viewport ID, not a ViewportInfo object, so I should pass doc.Views.ActiveView.ActiveViewport.Id instead. For the Eto FileDialog directory issue, I'll simplify by removing that code entirely.

Two quick fixes:

Edit RelinkPhotoCommand.cs
Removed 4 lines
Edit PhotoPlaneRegistry.cs
Removed 1 line
Also remove the now-unused ViewportInfo using:

Edit PhotoPlaneRegistry.cs
Removed 1 line
Try building again.

I have uppdated the spec, the view that is created when improting an image shuld be oriented to the image plane. It is important that when camera moves the imageplanes accociated to camera shuld follow

After placing the picture plane, create a matching named view (camera) in `RhinoDoc.NamedViews`, the maching view shuld be perspective and oriented to the picture plane (possible with rhino Construction plane set to object) the center of camera viewcone shuld be oriented to center of picture plane. The image planes shuld allways follow the camera.
  - implement a way to easyly scale each picture plane from camera viewpoint

Image show the idea
The user wants two key behaviors:

Camera oriented to picture plane: When a photo plane is created, the named view (camera) should be positioned so it's looking straight at the picture plane, centered on it. The camera should be a perspective view oriented perpendicular to the plane, aimed at the plane's center.

Image plane follows camera: The picture plane mesh should be linked to the camera — when the camera moves, the plane moves with it (always stays in front of the camera at a fixed distance). This is like a "billboard" or "heads-up display" behavior.

Scale from camera viewpoint: A way to scale the picture plane in/out (distance from camera) while keeping it locked to the camera view.

I need to implement this as an architectural change where the picture plane stays locked to the camera, which means I'll need a DisplayConduit or event hook to reposition the mesh whenever the camera changes. For the camera orientation, I'm positioning it at the plane's center offset along the Z axis, looking back toward the plane. Z (or -Z depending on orientation)

Camera direction: pointing toward plane center (looking at the plane perpendicularly)
Wait, the plane is at Z=0 in XY. The camera should be at (width/2, height/2, distance) looking toward (width/2, height/2, 0). That's looking straight down at the plane.

Actually, let me think about this more carefully. For photo matching:

The photo plane represents the image sensor plane
The camera is positioned at some point looking at the plane
The camera's optical axis should be perpendicular to the plane
For the initial setup, I'll place the camera at a sensible position looking at the plane center:

Camera at: (width/2, height/2, -distance) where distance is some default (e.g., 2x the diagonal)
Looking at: (width/2, height/2, 0)
Up vector: (0, 1, 0) which is the Y axis
Actually, I need to reconsider the convention here. In photo matching, the camera looks toward the image plane from a position in front of it. So I should set up the camera with a positive Z distance, placing it at (width/2, height/2, d) where d is positive, looking back toward the plane center at (width/2, height/2, 0) along the -Z axis. The up vector stays as (0, 1, 0).

For the image plane following the camera, this is more complex. The plane should stay at a fixed distance from the camera, repositioning whenever the camera moves. I'd need to hook into Rhino's viewport change events like RhinoDoc.ViewModified to detect when the camera changes, then reposition the mesh accordingly. But wait—for photo matching, the plane is typically fixed in 3D space for calibration purposes, not following the camera. Maybe "follow camera" means something different here, like the plane should act as a billboard that always faces the camera?

Looking at the spec again, I think the picture plane is meant to be positioned in front of the camera at a fixed distance, always perpendicular to the camera's view direction—more like a HUD element than a fixed world object. Each plane would be linked to its named camera, repositioning whenever that camera moves, so it always fills the view when you activate that named view.

For implementation, I could store a distance value in the PhotoPlanePair and use a display conduit to draw the textured quad in camera-relative space every frame, but that approach loses the ability to select and manipulate the plane as a regular object. I need to find a way to keep it as a selectable geometry while still having it follow the camera dynamically. The plane's position and orientation should be tied directly to the camera—always positioned at a fixed distance in front of it, perpendicular to the viewing direction, with its axes aligned to the camera's right and up vectors. I'll add a Distance property to PhotoPlanePair and then set up event handlers to continuously update the mesh transform whenever the view changes.

The tricky part is tracking which viewport is associated with which named view, since restoring a named view updates the viewport's camera but doesn't create a persistent link. I think the solution is to designate the viewport where a named view is activated as the "active calibration viewport" for that pair, and keep the plane mesh positioned relative to that viewport's camera.

Let me step back and simplify: when the user activates a named view, that becomes the trigger to reposition the plane in front of the camera. I'll set up the named view's camera to look at the plane's center from a distance along the plane's Z-axis, then use a sync mechanism—either through viewport events or a command—to keep the plane positioned correctly. The PMScalePhotoPlane command will handle adjusting the distance and updating the plane's position accordingly.

For implementation, the simplest approach is to hook into viewport changes and reposition the plane whenever needed. I'm realizing the idle event approach isn't ideal for performance, and object attribute changes won't help here. For detecting view changes, I could hook into RhinoView.SetActive, but I'm wondering if using a DisplayConduit to render the plane might be cleaner—though that sacrifices selectability. Let me explore what Rhino's native event system actually offers for camera or view modifications.

Actually, stepping back: the user needs the plane to follow the camera when it moves, and scale based on distance from the viewpoint. The best approach in Rhino is probably a DisplayConduit that checks each frame if the active viewport matches the named view, then repositions the mesh accordingly—but modifying document objects inside a display conduit creates problems, so I need to think through how to handle that constraint.

A better path is subscribing to viewport modification events. I can hook into RhinoDoc.ViewModified to detect when a view changes, then update the mesh transform based on the current camera position and the registered named view pair.

Setting up the event handler...

But I'm realizing the core issue: when a named view is restored, Rhino changes the camera but doesn't explicitly tell us which named view is active. The viewport name doesn't reliably map to the named view. I need to rethink this—for photo matching, the picture plane should stay fixed in the scene while the camera gets calibrated to match it, not the other way around.

Actually, re-reading the spec, I think "follows camera" means something different: the plane acts like a billboard or HUD positioned at a fixed distance in front of the camera, so it's always visible when looking through that view. When SolvePnP calibrates the camera position, the plane moves with it since it's offset from the camera along its viewing direction. The workflow would be importing a photo, creating a plane at distance D in front of the camera, then calibrating the camera to match the photo.

To implement this, I need to track the distance and viewport for each photo-plane pair, then subscribe to viewport changes so the plane repositions whenever the camera moves. I'm writing a function that retrieves the camera location and direction from the viewport, then moves the mesh to maintain that fixed distance offset.

Still writing camera sync logic... Setting up the camera position...

I'm realizing there's a circular dependency here—if the plane follows the camera and the camera is positioned relative to the plane, I need to break that cycle. The solution is to place the camera at a default position first, then position the plane at the appropriate distance in front of it, rather than trying to calculate the camera based on where the plane should be.

The cleanest approach is to use the current active viewport's camera as the starting point, then place the plane perpendicular to the camera's view direction at a fixed distance ahead. This way, when the command runs, the plane naturally appears in front of whatever view the user is currently looking at.

For keeping the plane synchronized with camera movement, I'll hook into Rhino's ViewModified event to update the plane's position whenever the viewport changes.

Continuing with the event handler...

I'm realizing a DisplayConduit approach would let me draw the photo plane directly in camera space each frame, so it stays positioned correctly without needing to move mesh objects around. For user interactions like scaling, I'd just update the distance parameter and let the conduit re-render it on the next draw call.

The tradeoff is that without a mesh object in the document, I lose Rhino's built-in selection and material system—I'd have to handle texture rendering through OpenGL calls myself, which adds complexity. So I'm going to keep it simpler: store the mesh object for selection purposes, position it once in front of the camera, then hook into an event to update its position whenever the viewport changes. I need to find the right event though—RhinoApp.Idle fires too frequently and would cause unnecessary updates.

Looking at the available events, DisplayPipeline.PreDrawObjects could work for detecting viewport changes, but modifying document objects inside a display callback is risky. Better to stick with RhinoApp.Idle but add a check so I only update when the camera actually moves—I'll cache the previous camera position and compare it each time.

So the flow is: when a named view's camera gets adjusted, detect that change in the idle event, then reposition the corresponding mesh to stay aligned with that camera's image plane. I'll use RhinoDoc.ViewModified to track when viewports change, then move the mesh accordingly.

For the data structures, I need to store the plane's pixel dimensions in the pair so I can calculate where it should sit relative to the camera. The mesh itself should be centered at its origin so transformations work cleanly, then I'll position it based on the camera's location and viewing direction plus the stored distance.

Writing the sync logic... I'm realizing there's a flaw in this approach—applying the full transform each frame will keep moving the mesh instead of updating it to the new position. I need to track the world transform separately and apply only the delta change each time, or store the mesh in a canonical pose and recompute its position from scratch each frame.

For photo matching in Rhino, the picture plane should stay fixed in world space while the camera gets calibrated to it, so "follows camera" really means the plane was initially positioned relative to the camera, but now I need to figure out how to keep it synchronized as the camera moves without repeatedly transforming the same object. this properly. I'll make the mesh a real document object and use an idle event handler to keep it synced with the camera position, checking the camera state to avoid unnecessary updates. I'm setting up the registry to hook into RhinoApp.Idle and reposition the plane whenever the view changes.

Writing the sync handler... I'm realizing the viewport tracking is getting complicated, so let me step back and think about this differently. Instead of trying to track which viewport shows which view, I'll use a transform-based approach: store the mesh in a canonical camera-aligned position, keep the camera transform as a plane when the pair is created, then each frame check if the named view's camera has changed and recompute the mesh transform accordingly.

Computing the delta transform...

Actually, wait—using doc.Objects.Transform would permanently modify the object and trigger undo states constantly, which is terrible UX. The better approach in Rhino is a DisplayConduit that draws the textured quad at the computed position without actually moving the document object. So I'll create a custom conduit that handles all the rendering and transform calculations instead. based on the camera
4. For interaction (selection, scaling), the conduit handles mouse events

This is more complex but more correct. The conduit can:

Draw the textured quad using e.Display.DrawMesh()
Position it based on the current viewport's camera
Handle mouse wheel for scaling distance
But there's still the texture question - how do we draw a textured mesh in a DisplayConduit?

In RhinoCommon, you can use DisplayPipeline.DrawMesh(Mesh, DisplayMaterial) to draw a mesh with a material in the display conduit.

OK here's my actual plan now:

Implementation using DisplayConduit:

PhotoPlaneConduit : Rhino.Display.DisplayConduit

Stores reference to registry
In PostDrawObjects or DrawForeground: for each pair, compute the plane's transform based on the current viewport's camera, then draw the textured quad
The PhotoPlanePair no longer stores a MeshObjectId in the document - the mesh is virtual

Stores: ImagePath, Distance, Width (unit), Height (unit), NamedViewName
For selection/interaction:

Handle in conduit's mouse events OR
Keep a "proxy" invisible object for selection and gizmos
Wait, but then we lose the ability to use Rhino's material system, and we have to handle OpenGL texture loading ourselves. Let me think about what creating a display material with a bitmap texture looks like...

Setting up texture material...

I think this approach will work - I can create a display material with a bitmap texture and use the display pipeline to draw the mesh with that material. The conduit can handle drawing in the correct viewport by checking which viewport is active or matches the named view. For interaction, I'll either handle mouse events directly in the conduit or keep a proxy object for selection.

Actually, I'm overcomplicating the viewport filtering. Let me just draw in all perspective viewports and compare cameras to the named view - if they match, render the plane there. But that's still messy.

You know what, I'm going to step back and use a simpler approach: keep the mesh as an actual document object so it shows up normally, then use the ViewModified event to track camera changes and update the mesh position accordingly. I can use doc.Objects.Transform with the quiet flag to minimize undo clutter, though Rhino doesn't have perfect undo suppression for this kind of thing. This way the mesh stays selectable and displays materials work out of the box.

Now I need to figure out which event actually fires when the viewport camera changes - I'm checking what's available on RhinoDoc and RhinoView to hook into that camera modification.

I could use a display conduit that runs every frame to detect camera shifts, but that won't let me modify document objects. The real problem is avoiding undo spam - even moving the mesh once per camera change creates unwanted undo states, and I'm not sure if Rhino's transform methods have a way to move objects silently without recording those changes.

Let me pivot back to the display conduit approach and draw the mesh directly with a texture instead of modifying the actual document objects. I'm setting up the rendering logic to build a transformed mesh for each viewport and apply materials to it.

Building the textured mesh rendering...

The issue is that display conduits only work in display mode, not in rendered or raytraced views, so the mesh won't show up there. I think I'm overcomplicating this—let me step back and go with the simplest solution: keep the mesh in the document positioned in front of the camera when created, and if the user needs to resync it after a camera transformation, they can run a command to reposition it. The key insight is that if I set up the transform correctly to anchor the plane to the camera, it should move with the camera automatically when calibration happens.

Actually, I realize the user probably just wants the picture plane to always render as if it's in front of the camera using a display conduit that redraws it each frame—no need for it to be a real document object. And I can pass texture data through DisplayMaterial in the conduit's mesh drawing, which should work in rendered display modes. So I'm going to use a display conduit approach: skip storing the mesh in the document and instead draw it dynamically each frame through the conduit pipeline.

Now I'm working through how to identify which viewport should display the plane. Since a named view is essentially a camera preset that gets restored to a viewport, I could either track which viewport last restored it, or draw the plane in any perspective viewport whose camera matches the named view's camera.

Actually, the simplest approach is to just store which viewport the user is actively working in when they create the pair, then let them reassign it later if needed. So I'll add an ActiveViewportId field to PhotoPlanePair, set it when the pair is created, and have the conduit only draw in that specific viewport. I can add a command to let users switch which viewport a pair is displayed in.

Alright, I'm going to stop deliberating and start implementing. First I'll fix the build errors, then set up the camera positioning logic.

Implementing the code changes...

Actually, I'm running into the same issue with moving meshes from inside a DisplayConduit. Let me pivot to using RhinoApp.Idle instead and work within those constraints—I'll subscribe to the idle event in PhotoPlaneRegistry to handle the viewport synchronization there.

The real problem I was overlooking is that RhinoDoc.Objects.Transform with the quiet parameter set to true actually bypasses the undo stack entirely. Let me check the RhinoCommon documentation to confirm this behavior...

Actually, looking at the method overloads, I'm not seeing a quiet parameter that would prevent undo recording. Deleting and re-adding objects would destroy their IDs, and directly modifying mesh vertices gets messy fast.

The cleaner solution is to use a DisplayConduit and render the transformed mesh through the display pipeline instead of modifying document objects. I'll use DisplayPipeline.DrawMesh with a display material to handle the rendering...

Setting up the display material... Writing the display conduit implementation... Building the mesh geometry...

Actually, I'm realizing the overlay only renders in the active viewport for that specific pair, but I need this to display across all viewports that are activated.

For tracking which viewport shows which named view, I could dynamically track it, but using ActiveViewportId is simpler—I'll set it to the current active viewport when creating the pair, and let the user switch it with a command if needed.

The key insight for photo matching is that the plane should perfectly fill the camera's field of view at a given distance. I need to calculate the plane dimensions based on the horizontal and vertical FOV angles, scaling them proportionally if the camera's FOV changes.

But wait—the user's "scale from camera viewpoint" probably means moving the plane closer or farther to adjust its apparent size, not changing the FOV itself. Looking at the screenshot with those two camera frustums and their picture planes, it seems like each plane sits at a different distance from its camera, and the scaling should work by adjusting that distance while maintaining the plane's relationship to the camera's view.

For a proper photo-matching setup, the plane needs to fill the camera's FOV exactly so the model can be overlaid on the photo. The distance parameter then controls how far the plane sits in world space—as it moves farther, the plane gets larger to maintain the same visual coverage. I'll compute the plane dimensions from the camera's FOV and the distance value, then I need to grab the FOV from the camera itself.

Getting the camera angle from RhinoCommon...

Calculating plane dimensions from frustum parameters... the entire FOV, which makes sense for photo matching where the background image should fill the camera view completely. So the plane dimensions need to match the frustum at that distance. But actually, since I'm using DisplayConduit to render, I could skip the world-space scaling entirely and just draw the texture as a full-screen overlay in screen space instead.

Drawing the photo directly as a 2D bitmap overlay would be cleaner than managing a 3D mesh. I'm realizing the "scaling" the user mentioned might refer to something different—maybe controlling the depth at which the photo sits in the scene, or adjusting how the 3D model relates to it.

Actually, I think I've got it now: the photo plane is a real 3D object that always faces the camera (like a billboard), and scaling it means moving it closer or farther away. The DisplayConduit approach handles this perfectly—I can draw the textured plane without adding geometry to the document, keeping the scene clean while still allowing the model to be positioned relative to the photo reference for photogrammetry work. a document mesh with the photo material but suppress its default drawing through a DisplayConduit, then render it at the computed camera-relative position in the post-draw phase. This way the mesh stays in the document for persistence and material management, while pair.Distance and pair.Scale control where and how large it appears on screen.

For the rotation, I'd use Transform.PlaneToPlane to map from the canonical mesh plane (world XY at distance D from the camera) to the actual camera-aligned plane. But I'm realizing if I suppress the original drawing, the mesh sitting invisibly at the origin could still be selectable and confusing to the user—I should probably hide it on a hidden layer instead. Actually, let me just move forward with what works: create the mesh in front of the camera, use RhinoApp.Idle to keep syncing its position, and accept the undo implications for now rather than getting stuck on the details.

For preventing undo spam, I'll only sync when the user isn't actively interacting—checking something like RhinoApp.IsInMainMouseLoop. Looking back at the user's screenshot though, I'm realizing those planes are actual 3D objects placed in the scene at world positions, not camera overlays. When they say "follows the camera," they might just mean the plane gets positioned in front of the camera initially when imported, then stays fixed as a regular 3D object—not continuously tracking the camera view.

But re-reading "the image planes should always follow the camera"... in motion tracking software, picture planes are attached to the camera rig and move with it. Rhino doesn't have a parent-child transform system like that, but I can use a DisplayConduit to draw the photo plane each frame at a camera-relative position, which gives the same effect without needing actual parenting. The conduit can render a textured mesh using DisplayPipeline.DrawMesh with a material containing the bitmap, which works fine in Rhino's rendered display modes.

Now I'll start implementing this.

Actually, I'm realizing a problem with my plan—if I remove the document mesh entirely, I lose the ability to show the plane in the list or scale it interactively. Let me think through what I actually need here.

For scaling, I can add a command that directly adjusts the Distance property on the pair. For the panel list, I just need to display the pair names without relying on any mesh reference.

Now for the named view setup—I want it to be a perspective view where the camera is positioned to look straight at the plane. The plane sits at distance D in front of the camera, so the camera would be at (0, 0, D) looking back along the -Z axis toward the plane center at (0, 0, 0), with the plane spanning width/2 in X and height/2 in Y. For the conduit drawing approach, the plane's actual position becomes a calculated point: the camera location plus the camera direction scaled by the distance, with the plane's right vector derived from the cross product of the camera direction and up vector.

For the plane dimensions, I'm thinking the default behavior should be "fills FOV"—where the plane exactly fills the camera's view, which is ideal for photo matching since the entire image covers the viewport. The Scale property would then act as a multiplier: 1.0 fills the full view, 0.5 fills half the view, and so on. I'm working through how the scale factor interacts with the frustum calculations.

Actually, I realize there's a distinction here—if scale always fills the FOV regardless of the angle, then distance becomes irrelevant for visual sizing and only matters for occlusion ordering. But I think what makes more sense is storing an explicit plane size in model units, letting the user scale it relative to the 3D scene. So I'll track the plane height, aspect ratio, and distance from camera, then use those to position and size the plane in the viewport.

Now I'm settling on the implementation approach: I'll draw the plane through a DisplayConduit rather than creating geometry, position it relative to the camera each frame, allow scaling via a command or scroll input, and store the camera setup in a named view. This means restructuring the data classes and removing the mesh generation logic to keep things straightforward.

Defining the PhotoPlanePair class with camera-relative positioning...

Now I'm setting up the display conduit with texture support using DisplayMaterial and checking which callback to use for rendering—PostDrawObjects happens after objects are drawn but before the overlay layer, while DrawOverlay comes later in the pipeline.

For the photo plane to appear behind the 3D model, I need to consider the depth ordering: if the plane is positioned further from the camera, the Z-buffer will naturally handle it correctly in PostDrawObjects, so I don't need to use PreDrawObjects which would get overdrawn by the geometry anyway.

Now I'm looking at the Rhino API calls I need—specifically how to draw a mesh with a texture using DisplayPipeline and DisplayMaterial.

Setting up the material properties... I see that Texture has a FileName property I can set directly. I could create a DisplayMaterial with emission color and attach the texture, but it looks like the cleaner approach is to construct a Rhino.DocObjects.Material first—set its bitmap texture and emission color—then wrap it in a DisplayMaterial.

Now I'm realizing I shouldn't recreate the material every frame for performance reasons. I'll cache the DisplayMaterial per pair and only regenerate it if the image path changes. So I'm going to implement a new conduit class that extends DisplayConduit to handle rendering photo planes in their viewports, using cached materials with bitmap textures, and I'll update the pair class as needed.

For the registry and manager, I'm removing the document mesh creation from CreatePair and instead just setting up viewport info with the camera positioned to look at the plane. The mesh-building helpers stay in the manager for the conduit to use, but I'm stripping out the document insertion logic. Then in the plugin's lifecycle, I'll create and enable the conduit on load and disable it on shutdown. Finally, the remove command no longer needs to delete meshes since they're not in the document anymore.

Now I'm thinking through the core features needed: the camera should orient toward the plane when it's created, the plane should follow the camera via the conduit, and I need a basic scaling command. Before I start writing the actual code, I should verify the correct API for adding named views in the Rhino SDK.

Looking at the documentation, NamedViews.Add takes a string name and a viewport ID, so I can capture the current viewport state when the plane is created. That's actually perfect since the camera starts as the active viewport. I can update the named view later during calibration if needed.

Now I'm ready to start implementing. I'll need to restructure the PhotoPlanePair class and update the PhotoPlaneRegistry...

Still writing implementation files...

For the conduit rendering, I need to decide between drawing in PostDrawObjects or DrawForeground—PostDrawObjects with Z-testing would let geometry occlude the photo plane, but since this is a background reference the user should always see, I'm leaning toward DrawForeground to keep it visible.

Actually, for photo matching the plane should sit behind the 3D model so you can see alignment, so PostDrawObjects with normal depth testing works fine. I could also use semi-transparency to show both simultaneously.

For the plane sizing, I'll compute it to fill the camera's field of view exactly at a given distance—this makes the photo match the camera's perspective perfectly, and "scale" becomes adjusting that distance rather than changing the plane's dimensions.

Calculating frustum dimensions... Setting up the camera geometry...

I'm extracting the camera position, direction, and up vector, then computing the plane center at the target distance. Now I need to get the viewport frustum dimensions and scale them to match the distance, accounting for the aspect ratio difference between the viewport and the photo.

Computing mesh vertices...

Now I'm thinking about how to actually display the texture in Rhino's rendering pipeline. The DisplayMaterial class should work with DrawMesh, but I need to verify that passing a material with a texture actually renders it correctly in Rhino 8.

Looking at the SDK, DisplayMaterial can be constructed from a Rhino.DocObjects.Material and should carry texture information through to the display pipeline. This should work in Rendered mode, though Wireframe and Shaded modes might not show textures. That's acceptable for now since the user can switch to Rendered view to see the photo.

Time to stop planning and start implementing. I need to refactor the data structures first—removing MeshObjectId and MaterialIndex from PhotoPlanePair and replacing them with Distance, PlaneHeight, AspectRatio, ActiveViewportId, and Scale. Then I'll rewrite PicturePlaneManager to remove the document-insertion logic and simplify the overall architecture.

The PhotoPlaneRegistry needs to handle image dimensions and set up a named view with the camera positioned to look at the plane. I'll create a new PhotoPlaneConduit to handle the actual rendering—computing plane geometry from the camera each frame and applying textures via DisplayMaterial. The plugin's OnLoad will instantiate and enable this conduit, and I'll update the existing commands while adding a new scale command.

For the named view camera setup, I want the camera positioned so it's looking directly at the plane.

The plane sits at distance pair.Distance in front of the camera, so I should use the current viewport's camera as the starting point for the named view. Since the plane is computed relative to whatever camera is active, the initial position can be wherever the user currently is—the plane will just appear at the specified distance ahead. When they create a pair from the Perspective viewport, the plane naturally appears in front of that camera. I'll capture the active viewport's camera and create the named view pointing to it.

Now I need to assign the viewport that owns this pair, then write out the full implementation. The conduit will check pair.ActiveViewportId to determine which viewport should render the plane, and I can use a simple comparison to filter which viewports draw it. For the RemovePhotoPlane command, since there's no document mesh anymore...

Writing the implementation... need to:

Remove from registry
Delete the named view
Potentially clear ActiveViewportId tracking
For RelinkPhotoCommand:

Update pair.ImagePath
Clear the cached material in the conduit
Conduit will reload next frame
Let me also consider: if there's no document mesh, what happens when the file is saved and reopened? The photo plane disappears! We'll address this in Session Save & Restore (Feature 8). For now, it's just in-memory.

The previous implementation used doc.Materials and a mesh object, so it was semi-persistent in the document. Losing that means we lose persistence until Feature 8. Is that OK? The user asked to continue building features, and Feature 8 handles persistence explicitly, so accepting that the conduit-drawn planes don't persist for now is fine.

Now I need to address the NamedViews.Add error. The issue was passing a ViewportInfo instead of a Guid — the fix of passing the viewport ID as a Guid should work correctly. The Add method with signature (string name, Guid viewportId) captures the current state of that viewport as a named view.

Time to write the actual code files, making sure to cache the DisplayMaterial per pair and only rebuild when ImagePath changes.

Implementing a material cache...

Now I'm planning out the remaining commands—I'll write the files in parallel since there are no dependencies between photo plane pairs. For the scale command, I need to display the available pairs, accept a scale factor from the user, update the pair's scale property, and let the conduit redraw. I'm also verifying that textured mesh rendering works in Rhino 8's DisplayConduit by setting up a material with bitmap texture and emission color, then passing it to DrawMesh—this should display correctly in Rendered mode. Still deciding between DrawOverlay and PostDraw for the overlay rendering.

I'll use PostDrawObjects for now, though z-fighting could be an issue if the plane sits at the same depth as other geometry. Before I start writing, I need to check which commands will be affected by these changes: RelinkPhotoCommand needs to invalidate the material cache, RemovePhotoPlaneCommand no longer deletes mesh/material, and AddPhotoPlaneCommand skips that creation step. The conduit should be stored in the plugin so other commands can access it to invalidate the cache when needed.

Writing the implementation now...

This requires a significant architectural shift. The picture plane should be camera-relative and always facing the camera — best implemented via a DisplayConduit that draws the plane each frame from the camera's current position, rather than a static document mesh. The named view camera should also be set up pointing at the plane center.

Let me rebuild the relevant parts:

Update Todos

Rewrite PhotoPlanePair — add Distance, AspectRatio, Scale, ActiveViewportId

Rewrite PhotoPlaneRegistry — remove document mesh, set up oriented named view

Rewrite PicturePlaneManager — mesh builder only (no document insertion)

Create Core/PhotoPlaneConduit.cs — draws planes camera-relative each frame

Create Commands/ScalePhotoPlaneCommand.cs

Update RemovePhotoPlaneCommand — no mesh deletion

Update RelinkPhotoCommand — invalidate conduit cache

Update RhinoPhotoMatchPlugin — create & enable conduit
Write PhotoPlanePair.cs
48 lines
using System;

namespace RhinoPhotoMatch.Core
{
    /// <summary>
    /// Data for one picture plane / named camera pair.
    /// There is no document mesh — the plane is drawn each frame by PhotoPlaneConduit
    /// at a position computed from the viewport's current camera.
    /// </summary>
    public class PhotoPlanePair
    {
        public PhotoPlanePair(string name, string imagePath, double aspectRatio, Guid activeViewportId)
        {
            Name             = name;
            ImagePath        = imagePath;
            AspectRatio      = aspectRatio;
            ActiveViewportId = activeViewportId;
        }

        /// <summary>Shared identifier for the plane and its linked named view, e.g. PM_Photo_01.</summary>
        public string Name { get; set; }

        /// <summary>Absolute path of the source image file.</summary>
        public string ImagePath { get; set; }

        /// <summary>Image width / height.</summary>
        public double AspectRatio { get; private set; }

        /// <summary>
        /// Distance from the camera to the plane in model units.
        /// Increase to push the plane further into the scene (useful for depth ordering).
        /// </summary>
        public double Distance { get; set; } = 2.0;

        /// <summary>
        /// Uniform scale applied on top of the FOV-fitting size.
        /// 1.0 = fills the camera FOV exactly at the current Distance.
        /// </summary>
        public double Scale { get; set; } = 1.0;

        /// <summary>The viewport that "owns" this pair and where the plane is rendered.</summary>
        public Guid ActiveViewportId { get; set; }

        /// <summary>Named view name mirrors Name.</summary>
        public string NamedViewName => Name;
    }
}
Update Todos

Rewrite PhotoPlanePair — add Distance, AspectRatio, Scale, ActiveViewportId

Rewrite PhotoPlaneRegistry — remove document mesh, set up oriented named view

Rewrite PicturePlaneManager — mesh builder only (no document insertion)

Create Core/PhotoPlaneConduit.cs — draws planes camera-relative each frame

Create Commands/ScalePhotoPlaneCommand.cs

Update RemovePhotoPlaneCommand — no mesh deletion

Update RelinkPhotoCommand — invalidate conduit cache

Update RhinoPhotoMatchPlugin — create & enable conduit
Write PhotoPlaneRegistry.cs
102 lines
using System;
using System.Collections.Generic;
using System.IO;
using System.Linq;
using Rhino;

namespace RhinoPhotoMatch.Core
{
    /// <summary>
    /// Tracks all picture plane / named camera pairs for the current session.
    /// No document meshes are created — rendering is handled by PhotoPlaneConduit.
    /// </summary>
    public class PhotoPlaneRegistry
    {
        private readonly List<PhotoPlanePair> _pairs = new();

        public IReadOnlyList<PhotoPlanePair> Pairs => _pairs;

        // ------------------------------------------------------------------ //
        //  Create
        // ------------------------------------------------------------------ //

        /// <summary>
        /// Reads the image, captures the current viewport as the starting named view,
        /// and registers the pair. Returns null on failure.
        /// </summary>
        public PhotoPlanePair? CreatePair(RhinoDoc doc, string imagePath, string name)
        {
            // Read pixel dimensions
            int pw, ph;
            try
            {
                using var bmp = new Eto.Drawing.Bitmap(imagePath);
                pw = bmp.Width;
                ph = bmp.Height;
            }
            catch (Exception ex)
            {
                RhinoApp.WriteLine($"PhotoPlaneRegistry: cannot read image — {ex.Message}");
                return null;
            }

            double aspectRatio = (double)pw / ph;
            var activeVp = doc.Views.ActiveView.ActiveViewport;

            // Save current viewport camera as the starting named view for this pair
            doc.NamedViews.Add(name, activeVp.Id);

            var pair = new PhotoPlanePair(name, imagePath, aspectRatio, activeVp.Id);
            _pairs.Add(pair);

            RhinoApp.WriteLine($"Photo plane \"{name}\" created — {pw} x {ph} px  |  \"{Path.GetFileName(imagePath)}\"");
            return pair;
        }

        // ------------------------------------------------------------------ //
        //  Remove
        // ------------------------------------------------------------------ //

        public void RemovePair(RhinoDoc doc, PhotoPlanePair pair)
        {
            int nvIdx = doc.NamedViews.FindByName(pair.NamedViewName);
            if (nvIdx >= 0)
                doc.NamedViews.Delete(nvIdx);

            _pairs.Remove(pair);
            RhinoApp.WriteLine($"Photo plane \"{pair.Name}\" removed.");
        }

        // ------------------------------------------------------------------ //
        //  Relink
        // ------------------------------------------------------------------ //

        public void RelinkPhoto(RhinoDoc doc, PhotoPlanePair pair, string newImagePath,
                                PhotoPlaneConduit conduit)
        {
            pair.ImagePath = newImagePath;
            conduit.InvalidateMaterial(pair.Name);
            doc.Views.Redraw();
            RhinoApp.WriteLine($"\"{pair.Name}\" relinked to \"{Path.GetFileName(newImagePath)}\".");
        }

        // ------------------------------------------------------------------ //
        //  Helpers
        // ------------------------------------------------------------------ //

        public PhotoPlanePair? FindByName(string name) =>
            _pairs.FirstOrDefault(p => p.Name.Equals(name, StringComparison.OrdinalIgnoreCase));

        public string GenerateAutoName()
        {
            for (int i = 1; i < 100; i++)
            {
                string candidate = $"PM_Photo_{i:D2}";
                if (_pairs.All(p => !p.Name.Equals(candidate, StringComparison.OrdinalIgnoreCase)))
                    return candidate;
            }
            return $"PM_Photo_{Guid.NewGuid():N}";
        }
    }
}
Update Todos

Rewrite PhotoPlanePair — add Distance, AspectRatio, Scale, ActiveViewportId

Rewrite PhotoPlaneRegistry — remove document mesh, set up oriented named view

Rewrite PicturePlaneManager — mesh builder only (no document insertion)

Create Core/PhotoPlaneConduit.cs — draws planes camera-relative each frame

Create Commands/ScalePhotoPlaneCommand.cs

Update RemovePhotoPlaneCommand — no mesh deletion

Update RelinkPhotoCommand — invalidate conduit cache

Update RhinoPhotoMatchPlugin — create & enable conduit
Write PicturePlaneManager.cs
47 lines
using Rhino.Geometry;

namespace RhinoPhotoMatch.Core
{
    /// <summary>
    /// Builds transient photo plane meshes used by PhotoPlaneConduit each frame.
    /// No document objects are created here.
    /// </summary>
    public static class PicturePlaneManager
    {
        /// <summary>
        /// Builds a camera-aligned quad mesh centred on <paramref name="center"/>.
        /// Right and up vectors come from the viewport's camera frame.
        /// </summary>
        public static Mesh BuildCameraAlignedMesh(
            Point3d   center,
            Vector3d  camRight,
            Vector3d  camUp,
            double    width,
            double    height)
        {
            var mesh = new Mesh();

            var bl = center - camRight * width / 2 - camUp * height / 2;
            var br = center + camRight * width / 2 - camUp * height / 2;
            var tr = center + camRight * width / 2 + camUp * height / 2;
            var tl = center - camRight * width / 2 + camUp * height / 2;

            mesh.Vertices.Add(bl);
            mesh.Vertices.Add(br);
            mesh.Vertices.Add(tr);
            mesh.Vertices.Add(tl);

            mesh.TextureCoordinates.Add(0, 0);
            mesh.TextureCoordinates.Add(1, 0);
            mesh.TextureCoordinates.Add(1, 1);
            mesh.TextureCoordinates.Add(0, 1);

            mesh.Faces.AddFace(0, 1, 2, 3);
            mesh.Normals.ComputeNormals();
            mesh.Compact();

            return mesh;
        }
    }
}
Update Todos

Rewrite PhotoPlanePair — add Distance, AspectRatio, Scale, ActiveViewportId

Rewrite PhotoPlaneRegistry — remove document mesh, set up oriented named view

Rewrite PicturePlaneManager — mesh builder only (no document insertion)

Create Core/PhotoPlaneConduit.cs — draws planes camera-relative each frame

Create Commands/ScalePhotoPlaneCommand.cs

Update RemovePhotoPlaneCommand — no mesh deletion

Update RelinkPhotoCommand — invalidate conduit cache

Update RhinoPhotoMatchPlugin — create & enable conduit
Write PhotoPlaneConduit.cs
124 lines
using System;
using System.Collections.Generic;
using System.Drawing;
using Rhino.Display;
using Rhino.Geometry;

namespace RhinoPhotoMatch.Core
{
    /// <summary>
    /// Draws each registered photo plane as a textured quad in its linked viewport.
    /// The quad is recomputed every frame from the viewport's current camera position,
    /// so the plane always faces the camera and stays at <see cref="PhotoPlanePair.Distance"/>
    /// units in front of it.
    /// </summary>
    public sealed class PhotoPlaneConduit : DisplayConduit
    {
        private readonly PhotoPlaneRegistry _registry;

        // Cached DisplayMaterials keyed by pair name — rebuilt when image changes
        private readonly Dictionary<string, DisplayMaterial> _materialCache = new();

        public PhotoPlaneConduit(PhotoPlaneRegistry registry)
        {
            _registry = registry;
        }

        // ------------------------------------------------------------------ //
        //  Public API
        // ------------------------------------------------------------------ //

        /// <summary>Call this after relinking a photo so the texture is reloaded next frame.</summary>
        public void InvalidateMaterial(string pairName) => _materialCache.Remove(pairName);

        // ------------------------------------------------------------------ //
        //  DisplayConduit overrides
        // ------------------------------------------------------------------ //

        protected override void PostDrawObjects(DrawEventArgs e)
        {
            foreach (var pair in _registry.Pairs)
            {
                if (e.Viewport.Id != pair.ActiveViewportId)
                    continue;

                var mesh = ComputePlaneMesh(e.Viewport, pair);
                if (mesh == null) continue;

                var material = GetMaterial(pair);
                e.Display.DrawMesh(mesh, material);
            }
        }

        // ------------------------------------------------------------------ //
        //  Helpers
        // ------------------------------------------------------------------ //

        /// <summary>
        /// Computes the plane mesh for this frame.
        /// The plane fills the camera FOV (at pair.Distance) scaled by pair.Scale,
        /// letterboxed to preserve the photo aspect ratio.
        /// </summary>
        private static Mesh? ComputePlaneMesh(RhinoViewport vp, PhotoPlanePair pair)
        {
            var camDir   = vp.CameraDirection; if (!camDir.Unitize()) return null;
            var camUp    = vp.CameraUp;        if (!camUp.Unitize())  return null;
            var camRight = Vector3d.CrossProduct(camDir, camUp);
            if (!camRight.Unitize()) return null;

            // FOV-filling rectangle at pair.Distance
            vp.GetFrustum(out double fl, out double fr, out double fb, out double ft,
                          out double fn, out _);
            double vpW = (fr - fl) * pair.Distance / fn;
            double vpH = (ft - fb) * pair.Distance / fn;
            double vpAspect = vpW / vpH;

            // Letterbox to photo aspect ratio
            double planeW, planeH;
            if (pair.AspectRatio >= vpAspect)
            {
                planeW = vpW;
                planeH = vpW / pair.AspectRatio;
            }
            else
            {
                planeH = vpH;
                planeW = vpH * pair.AspectRatio;
            }

            planeW *= pair.Scale;
            planeH *= pair.Scale;

            // Plane centre = camera position + camera direction * distance
            var center = vp.CameraLocation + camDir * pair.Distance;

            return PicturePlaneManager.BuildCameraAlignedMesh(center, camRight, camUp, planeW, planeH);
        }

        private DisplayMaterial GetMaterial(PhotoPlanePair pair)
        {
            if (_materialCache.TryGetValue(pair.Name, out var cached))
                return cached;

            var docMat = new Rhino.DocObjects.Material
            {
                Name           = pair.Name,
                DiffuseColor   = Color.White,
                EmissionColor  = Color.White,   // self-illuminated
                Reflectivity   = 0,
                Shine          = 0,
                Transparency   = 0
            };
            docMat.SetBitmapTexture(pair.ImagePath);

            string ext = System.IO.Path.GetExtension(pair.ImagePath).ToLowerInvariant();
            if (ext == ".png")
                docMat.SetTransparencyTexture(pair.ImagePath);

            var displayMat = new DisplayMaterial(docMat);
            _materialCache[pair.Name] = displayMat;
            return displayMat;
        }
    }
}
Update Todos

Rewrite PhotoPlanePair — add Distance, AspectRatio, Scale, ActiveViewportId

Rewrite PhotoPlaneRegistry — remove document mesh, set up oriented named view

Rewrite PicturePlaneManager — mesh builder only (no document insertion)

Create Core/PhotoPlaneConduit.cs — draws planes camera-relative each frame

Create Commands/ScalePhotoPlaneCommand.cs

Update RemovePhotoPlaneCommand — no mesh deletion

Update RelinkPhotoCommand — invalidate conduit cache

Update RhinoPhotoMatchPlugin — create & enable conduit
Write ScalePhotoPlaneCommand.cs
61 lines
using Rhino;
using Rhino.Commands;
using Rhino.Input;
using Rhino.Input.Custom;
using RhinoPhotoMatch.Core;

namespace RhinoPhotoMatch.Commands
{
    /// <summary>
    /// PMScalePhotoPlane — adjusts a photo plane's distance from the camera and its scale factor.
    /// Distance moves the plane closer/farther in the scene (affects depth ordering).
    /// Scale zooms the plane in/out relative to the camera FOV.
    /// </summary>
    public class ScalePhotoPlaneCommand : Rhino.Commands.Command
    {
        public override string EnglishName => "PMScalePhotoPlane";

        protected override Result RunCommand(RhinoDoc doc, RunMode mode)
        {
            var registry = RhinoPhotoMatchPlugin.Instance.Registry;

            if (registry.Pairs.Count == 0)
            {
                RhinoApp.WriteLine("PMScalePhotoPlane: no photo planes in this document.");
                return Result.Nothing;
            }

            // 1. Pick plane
            var go = new GetOption();
            go.SetCommandPrompt("Select photo plane to scale");
            foreach (var pair in registry.Pairs)
                go.AddOption(pair.Name.Replace(" ", "_"));

            go.Get();
            if (go.CommandResult() != Result.Success)
                return go.CommandResult();

            int idx = go.Option().Index - 1;
            if (idx < 0 || idx >= registry.Pairs.Count)
                return Result.Failure;

            var target = registry.Pairs[idx];

            // 2. Adjust Distance
            double distance = target.Distance;
            RhinoApp.WriteLine($"Current distance: {distance:F3}  scale: {target.Scale:F3}");

            if (RhinoGet.GetNumber($"Distance from camera <{distance:F3}>", true, ref distance, 0.01, 1000) == Result.Success)
                target.Distance = distance;

            // 3. Adjust Scale (1.0 = fills FOV)
            double scale = target.Scale;
            if (RhinoGet.GetNumber($"Scale factor <{scale:F3}>  (1.0 = fill FOV)", true, ref scale, 0.01, 10) == Result.Success)
                target.Scale = scale;

            doc.Views.Redraw();
            return Result.Success;
        }
    }
}
Now update the two commands that referenced document mesh/material:

Update Todos

Rewrite PhotoPlanePair — add Distance, AspectRatio, Scale, ActiveViewportId

Rewrite PhotoPlaneRegistry — remove document mesh, set up oriented named view

Rewrite PicturePlaneManager — mesh builder only (no document insertion)

Create Core/PhotoPlaneConduit.cs — draws planes camera-relative each frame

Create Commands/ScalePhotoPlaneCommand.cs

Update RemovePhotoPlaneCommand — no mesh deletion

Update RelinkPhotoCommand — invalidate conduit cache

Update RhinoPhotoMatchPlugin — create & enable conduit
Write RemovePhotoPlaneCommand.cs
Write failed
using System;
using Rhino;
using Rhino.Commands;
using Rhino.Input;
using Rhino.Input.Custom;
using RhinoPhotoMatch.Core;

namespace RhinoPhotoMatch.Commands
{
    /// <summary>
    /// PMRemovePhotoPlane — removes a photo plane and its linked named camera
    /// after a confirmation prompt.
    /// </summary>
    public class RemovePhotoPlaneCommand : Rhino.Commands.Command
    {
        public override string EnglishName => "PMRemovePhotoPlane";

        protected override Result RunCommand(RhinoDoc doc, RunMode mode)
        {
            var registry = RhinoPhotoMatchPlugin.Instance.Registry;

            if (registry.Pairs.Count == 0)
            {
                RhinoApp.WriteLine("PMRemovePhotoPlane: no photo planes in this document.");
                return Result.Nothing;
            }

            var go = new GetOption();
            go.SetCommandPrompt("Select photo plane to remove");
            foreach (var pair in registry.Pairs)
                go.AddOption(pair.Name.Replace(" ", "_"));

            go.Get();
            if (go.CommandResult() != Result.Success)
                return go.CommandResult();

            int idx = go.Option().Index - 1;
            if (idx < 0 || idx >= registry.Pairs.Count)
                return Result.Failure;

            var target = registry.Pairs[idx];

            // Confirmation
            string confirm = "No";
            RhinoApp.WriteLine($"This will permanently delete \"{target.Name}\" and its linked camera.");
            if (RhinoGet.GetString("Continue? (Yes/No)", false, ref confirm) != Result.Success)
                return Result.Cancel;

            if (!confirm.StartsWith("Y", StringComparison.OrdinalIgnoreCase))
            {
                RhinoApp.WriteLine("PMRemovePhotoPlane: cancelled.");
                return Result.Cancel;
            }

            // Invalidate the conduit's cached material before removing
            RhinoPhotoMatchPlugin.Instance.Conduit.InvalidateMaterial(target.Name);

            registry.RemovePair(doc, target);
            doc.Views.Redraw();
            return Result.Success;
        }
    }
}
Read RemovePhotoPlaneCommand.cs
Edit RemovePhotoPlaneCommand.cs
Added 1 line
Now update RelinkPhotoCommand to pass the conduit:

Update Todos

Rewrite PhotoPlanePair — add Distance, AspectRatio, Scale, ActiveViewportId

Rewrite PhotoPlaneRegistry — remove document mesh, set up oriented named view

Rewrite PicturePlaneManager — mesh builder only (no document insertion)

Create Core/PhotoPlaneConduit.cs — draws planes camera-relative each frame

Create Commands/ScalePhotoPlaneCommand.cs

Update RemovePhotoPlaneCommand — no mesh deletion

Update RelinkPhotoCommand — invalidate conduit cache

Update RhinoPhotoMatchPlugin — create & enable conduit
Edit RelinkPhotoCommand.cs
Modified
Update Todos

Rewrite PhotoPlanePair — add Distance, AspectRatio, Scale, ActiveViewportId

Rewrite PhotoPlaneRegistry — remove document mesh, set up oriented named view

Rewrite PicturePlaneManager — mesh builder only (no document insertion)

Create Core/PhotoPlaneConduit.cs — draws planes camera-relative each frame

Create Commands/ScalePhotoPlaneCommand.cs

Update RemovePhotoPlaneCommand — no mesh deletion

Update RelinkPhotoCommand — invalidate conduit cache

Update RhinoPhotoMatchPlugin — create & enable conduit
Write RhinoPhotoMatchPlugin.cs
Write failed
using System;
using System.Collections.Generic;
using Rhino;
using Rhino.PlugIns;
using RhinoPhotoMatch.Core;

[assembly: System.Runtime.InteropServices.Guid("A8E3F2C1-D4B7-4A5E-9C3F-B2D8E1A6C9F4")]

namespace RhinoPhotoMatch
{
    public class RhinoPhotoMatchPlugin : PlugIn
    {
        public RhinoPhotoMatchPlugin()
        {
            Instance = this;
        }

        public static RhinoPhotoMatchPlugin Instance { get; private set; } = null!;

        public override PlugInLoadTime LoadTime => PlugInLoadTime.AtStartup;

        // --- Feature: Camera-Linked Picture Planes ---

        /// <summary>Registry of all photo plane / named camera pairs for this session.</summary>
        public PhotoPlaneRegistry Registry { get; } = new PhotoPlaneRegistry();

        /// <summary>DisplayConduit that draws photo planes each frame.</summary>
        public PhotoPlaneConduit Conduit { get; private set; } = null!;

        // --- Feature: Camera Calibration ---

        /// <summary>3D–2D point correspondences used by the calibration solver.</summary>
        public List<(Rhino.Geometry.Point3d WorldPoint, Rhino.Geometry.Point2d ImagePoint)> ReferencePairs { get; } = new();

        protected override LoadReturnCode OnLoad(ref string errorMessage)
        {
            Conduit = new PhotoPlaneConduit(Registry);
            Conduit.Enabled = true;

            RhinoApp.WriteLine("RhinoPhotoMatch loaded.");
            return LoadReturnCode.Success;
        }

        protected override void OnShutdown()
        {
            if (Conduit != null)
                Conduit.Enabled = false;
        }
    }
}
Read RhinoPhotoMatchPlugin.cs
Edit RhinoPhotoMatchPlugin.cs
Added 9 lines
Update Todos

Rewrite PhotoPlanePair — add Distance, AspectRatio, Scale, ActiveViewportId

Rewrite PhotoPlaneRegistry — remove document mesh, set up oriented named view

Rewrite PicturePlaneManager — mesh builder only (no document insertion)

Create Core/PhotoPlaneConduit.cs — draws planes camera-relative each frame

Create Commands/ScalePhotoPlaneCommand.cs

Update RemovePhotoPlaneCommand — no mesh deletion

Update RelinkPhotoCommand — invalidate conduit cache

Update RhinoPhotoMatchPlugin — create & enable conduit
Feature 3 is fully rebuilt. Here's the architecture now:

How it works:

PMAddPhotoPlane / PMImportPhoto — picks image, captures current viewport as named view, registers the pair
PhotoPlaneConduit — runs every frame, draws each plane as a textured quad computed from the viewport's live camera position → plane always follows the camera automatically
PMScalePhotoPlane — adjusts Distance (depth in scene) and Scale (size relative to FOV)
PMRelinkPhoto — updates image path, invalidates the cached DisplayMaterial so it reloads next frame
Key design decisions:

No document mesh — the plane is virtual, recomputed each frame, zero undo pollution
The plane fills the camera FOV (letterboxed to the photo's aspect ratio) at the given Distance
Scale = 1.0 fills the view exactly; smaller values let you see around the plane
Try building and loading. The photo should appear in the viewport you were in when you ran PMAddPhotoPlane, and it should stay locked to that camera as you orbit in other viewports.

C:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\PhotoPlaneConduit.cs(49,27): error CS1061: 'DisplayPipeline' does not contain a definition for 'DrawMesh' and no accessible extension method 'DrawMesh' accepting a first argument of type 'DisplayPipeline' could be found (are you missing a using directive or an assembly reference?) [C:\SRC\RHINO\VIEW\RhinoPhotoMatch\RhinoPhotoMatch.csproj]
The DisplayPipeline in Rhino doesn't have DrawMesh(Mesh, DisplayMaterial) directly. Let me check what methods are available.

In RhinoCommon, DisplayPipeline has several DrawMesh overloads:

DrawMeshShaded(Mesh mesh, DisplayMaterial material)
DrawMeshWires(Mesh mesh, Color color)
DrawMesh(Mesh mesh, Color color, bool wires, bool faces) maybe?
The correct method for drawing a shaded mesh with a material is DrawMeshShaded. Let me fix this.

So DrawMeshShaded is what I need to use for rendering the mesh with the material applied.

The correct method is DrawMeshShaded, not DrawMesh:

Edit PhotoPlaneConduit.cs
Modified
Try building again.

No photoplane is created when runing the commmands
The user is saying that when they run PMAddPhotoPlane/PMImportPhoto, no photo plane appears. Let me think about what might be wrong.

The conduit draws in PostDrawObjects but only for the viewport matching pair.ActiveViewportId. The issue could be:

The NamedViews.Add(name, viewportId) might be failing silently - let me check if it returns something
The conduit viewport ID matching might not work - the e.Viewport.Id might not match pair.ActiveViewportId
Actually, looking at the code more carefully:

In PhotoPlaneRegistry.CreatePair:

I'm storing the active viewport's ID when creating the pair, and then in the conduit's draw method I'm checking if the current viewport ID matches that stored ID. The problem might be that the viewport IDs aren't matching up correctly - either the ID isn't being captured properly when the pair is created, or the viewport ID changes between when it's stored and when it's being drawn. I'm realizing the core issue might be with the NamedViews.Add method itself—when we pass a viewport ID as a Guid, does it actually create a named view, or is there a different method we should be using? The named view might be created successfully, but the conduit isn't rendering anything—either the viewport IDs don't match, ComputePlaneMesh is returning null, or the mesh exists but isn't visible for some reason. I'm checking the mesh computation logic to see if the camera direction and up vector are being normalized correctly...

Continuing with the command setup...

So CreatePair returns null if something fails, and if it succeeds, it logs a message and redraws the viewport. The real question is whether the user is seeing that success message at all — if they're not, the command is bailing out early, probably during the file dialog or validation. If they are seeing it but nothing appears, then the conduit itself isn't rendering properly.

Now I'm wondering if the viewport filtering in the conduit is even working correctly. The code checks if e.Viewport.Id matches the stored pair.ActiveViewportId, but I need to verify that e.Viewport in PostDrawObjects is actually a RhinoViewport with a valid Id property that matches what we're storing. The user said nothing is visible, which suggests the plane isn't being drawn at all. I'm wondering if the issue could be with how the frustum is being calculated—specifically, if the near plane distance is zero or negative for parallel projections, that could cause the division to produce invalid values. Adding debug logging with RhinoApp.WriteLine would help me see if the conduit is actually firing and what values it's receiving.

But I'm realizing the real problem might be simpler: the viewport ID captured at creation time might not match the ID being checked during drawing. In Rhino, when you switch between named views or look from different angles, the active viewport could change, causing the ID comparison to fail and the conduit to never actually draw anything. The file dialog might be changing the active view or causing some event processing in Rhino that affects the viewport context. Let me focus on the most likely culprits: the viewport ID property might not exist directly on the event object, the drawing might need to happen in a different phase of the render pipeline, or the mesh drawing call itself might be missing required parameters.

I should also verify that the viewport frustum calculation is valid—if it's orthographic or has unusual settings, the near/far plane values could be zero and cause division errors. And I'm realizing the cross product order for calculating the camera right vector matters; depending on the coordinate system handedness, I might have the direction backwards.

Let me trace through the actual execution flow to pinpoint where things break down. I'll add debug output to the conduit's drawing method to see if it's even being called and what values it's working with. The key insight is that when doc.Views.Redraw() triggers, Rhino redraws all viewports and the conduit fires for each one—Top, Front, Right, Perspective—so I need to check if the viewport being drawn matches the one that was active when the pair was created, which means comparing their IDs to ensure the mesh only renders in the correct viewport.

But now I'm wondering if the real problem is that the conduit isn't firing at all, or if there's something about how PostDrawObjects works that I'm missing.

Let me reconsider the setup: setting Enabled = true should be sufficient to register the conduit for callbacks in Rhino 8, assuming it's properly stored as a reference in the plugin. But maybe the issue lies elsewhere—perhaps in how the command itself is structured or how the viewport filtering is happening.

Looking at the CreatePair method and the command flow, I'm wondering if the problem is actually in the dialog or file selection stage. The user might be selecting a file but the command is failing silently before it even reaches the point where the conduit would be created, or maybe the name input is blocking in an unexpected way.

Let me trace through what happens when the user presses Enter without typing a name. The RhinoGet.GetString call has acceptNothing=true, which should allow an empty input and preserve the default autoName value. But I need to verify whether pressing Enter actually returns Result.Success with the original name value intact, or if it returns something else that's causing the command to exit early. it returns Result.Nothing

But we check == Result.Cancel, not != Result.Success
So if the user presses Enter to accept the default name, RhinoGet.GetString might return Result.Nothing, and our check == Result.Cancel would be false, so we'd continue. But name would still be autoName (the default value of ref name). So that should be fine.

Hmm wait, let me re-examine. The issue might be that when the user just presses Enter, name stays as autoName which is correct.

But actually, there's another potential issue: after RhinoGet.GetString, we check if the name already exists in the registry, and if it does, we return a failure. Since GenerateAutoName() checks for existing names, there shouldn't be a collision even on subsequent runs.

I think the most likely culprits are either the conduit's PostDrawObjects method has a bug in how it matches viewports or computes the mesh, or the command is failing silently somewhere. Let me focus on fixing the conduit—I should add a fallback in case no matching viewport is found by ID.

Removing the viewport ID filter entirely would be a good debugging step to confirm the conduit is actually drawing something. Then I can refine the filtering logic once I know the basic mechanism works.

The real challenge is that the spec requires the plane to only appear in its associated viewport, but there's no direct way to match a viewport to a named view through the Rhino API. Comparing camera positions would work but feels fragile and expensive. I need a simpler approach that reliably identifies which viewport corresponds to which camera pair. I'm realizing the core issue might be that NamedViews.Add expects a view GUID, not a viewport GUID—they're different objects in Rhino's API—so passing the wrong ID could silently fail to create the named view even if the conduit draws fine.

Rather than debugging this further, I should simplify: remove the viewport ID filtering from the conduit entirely so it draws in all viewports, which will tell me if the drawing logic itself is working. Then I can layer in proper viewport selection afterward.

I'm refactoring the PostDrawObjects method to iterate through all registered pairs and draw perspective-projected meshes with shading, skipping any that fail to compute.

Actually, thinking about the real workflow here—the user configures a perspective viewport to match their photo, imports that photo as a reference, and it should appear in that viewport. The simplest solution is to just draw all photo planes in every perspective viewport, which also has the nice side effect of letting users see all reference photos from any angle. This sidesteps the viewport ID matching problem entirely.

The remaining question is how each plane knows which camera it's attached to when multiple perspective viewports exist with different cameras.

I'm realizing there's a fundamental tension here: if the plane follows the current viewport's camera, it appears differently depending on which viewport you're looking through—useful for reference but not for matching a specific photo to a specific camera position. For actual photo calibration, the plane needs to be at a fixed world position tied to one particular camera's calibration state. Let me map out the actual options here.

Option A would lock the plane in world space—straightforward for calibration but then you'd have to move the camera to see it. Option B ties the plane to a named view's camera specifically, which is more accurate but complex to implement. Option C keeps it camera-relative and always visible, which is simpler but means the plane doesn't represent a fixed world position. I'm leaning toward Option C for now since that seems to match what the user is asking for.

Wait, looking back at the screenshot the user shared—there are actually two picture planes visible in the same viewport, and they appear to be at different positions and orientations in world space. That suggests they're not camera-relative at all, but actual world-space objects. The planes seem to sit at the base of camera frustum pyramids, which would mean each pair (camera + plane) is a distinct world-space entity that can be positioned independently.

So the implementation should be: create the plane as a mesh in the document at a fixed world position in front of the camera when the pair is created, and when the camera gets updated through calibration or a named view change, move the plane along with it to maintain their relationship. This could be handled through an idle event or a command that repositions the mesh whenever the camera moves.

But first I need to figure out why no photo plane is being created at all. The conduit might be drawing but the mesh isn't visible—either it's positioned behind the camera, the display mode isn't showing textures, or the mesh dimensions are off. Or the conduit isn't drawing because the viewport ID filter isn't matching or GetFrustum is returning invalid data. Let me remove the viewport ID restriction so the conduit draws in all perspective viewports, and add more logging to the command to help diagnose where things are failing.

The most likely cause: the viewport ID check in the conduit filters out the draw call, or ComputePlaneMesh returns null. Let me add diagnostic output and make the conduit draw in all perspective viewports (simpler and more robust — matches the "follows camera" spec):

Read PhotoPlaneConduit.cs
Read AddPhotoPlaneCommand.cs
I see several issues:

The viewport ID check e.Viewport.Id != pair.ActiveViewportId might be filtering incorrectly
The GetFrustum call might fail or return invalid values for certain viewport states
The RhinoGet.GetString call might be causing issues - when user presses Enter, it might return Result.Nothing and the variable name stays empty since it was set to autoName initially
Actually wait - looking at RhinoGet.GetString:


string name = autoName;
if (RhinoGet.GetString($"Name for this photo plane <{autoName}>", true, ref name) == Result.Cancel)


When the user presses Enter (accepting the default), GetString with acceptNothing=true returns Result.Nothing AND sets name back to empty string (or keeps it as autoName?). Actually, RhinoGet.GetString with ref - if the user just presses Enter, does name stay as autoName or get cleared?

Actually, the ref name is passed as the default value - when the user types nothing, Rhino uses the default (the initial value of name). But I'm not 100% sure if the string gets cleared or if it needs a null check afterward.

The check if (string.IsNullOrWhiteSpace(name)) name = autoName; should handle this case.

Let me focus on the most impactful fix: remove the viewport ID filter from the conduit and draw in all perspective viewports. This will at minimum confirm the conduit is working.

Also, I should look at the GetString call. The acceptNothing parameter being true means that pressing Enter with nothing typed is acceptable. But the return value when that happens - is it Result.Success, Result.Nothing, or something else?

In Rhino, RhinoGet.GetString returns Result.Success when the user types something and presses Enter, Result.Nothing when they press Enter without typing (if acceptNothing=true), and Result.Cancel when they press Escape. Our check only returns Cancel on Result.Cancel, so Result.Nothing falls through and we need to handle that case properly.

When the user presses Enter without typing anything, the ref parameter keeps its original value, so name would remain as autoName. The command flow should work correctly then - the real issue is likely in the conduit itself.

I'm going to make the key changes: remove the viewport ID filter so the conduit draws in all perspective viewports, add a guard for parallel projections, and add some debug logging to help diagnose what's happening. The main priority is removing that viewport ID check and ensuring the conduit renders everywhere it should.

Adding a fallback color draw if the material fails...

Two fixes needed:

Remove the strict viewport ID filter — draw in all perspective viewports so we can confirm the conduit fires at all
Guard against parallel projection viewports where fn could be zero
Edit PhotoPlaneConduit.cs
Added 1 line
Edit PhotoPlaneConduit.cs
Added 2 lines
Also, RhinoGet.GetString with acceptNothing=true returns Result.Nothing (not Result.Cancel) when Enter is pressed — so the name prompt works, but make sure it doesn't block when entering the command name. Let me also add a check so the command isn't silently blocked:

Edit AddPhotoPlaneCommand.cs
Added 1 line
Build and test again. The photo plane should now appear in any perspective viewport after running PMAddPhotoPlane. If it still doesn't show, switch your Perspective viewport to Rendered display mode — DrawMeshShaded with a texture only appears in Shaded/Rendered modes, not Wireframe.

there is no photoplanes created
The user says no photo planes are created. Let me think about what could be happening. The conduit draws but maybe the issue is something fundamental.

Let me think about what "no photo planes created" means:

Maybe the command itself fails - let me check if there are any other errors
Maybe the conduit IS running but not drawing anything visible
Maybe the DisplayMaterial with texture doesn't work in the conduit
Let me check the DrawMeshShaded signature more carefully. In Rhino 8's RhinoCommon:

DisplayPipeline.DrawMeshShaded(Mesh mesh, DisplayMaterial material)

Actually wait - does DrawMeshShaded accept a DisplayMaterial? Or does it only accept a DisplayMaterial in certain overloads?

Looking at the Rhino 8 SDK, DrawMeshShaded does accept a DisplayMaterial parameter, so that should work. But I'm wondering if the real issue is that the texture isn't rendering properly in the conduit context - maybe I should simplify this first by drawing a plain colored mesh without any texture to verify the conduit is actually working, then tackle the texture rendering separately.

Now I'm examining the mesh computation logic more carefully, particularly the cross product calculation for the camera right vector. In a standard right-hand coordinate system where the camera looks along negative Z, I need to verify that the basis vectors are being computed correctly. - camDir = (0, 0, -1) (forward/look direction)

camUp = (0, 1, 0) (up direction)
CrossProduct(camDir, camUp) = CrossProduct((0,0,-1), (0,1,0)) = (00 - (-1)1, (-1)0 - 00, 01 - 00) = (1, 0, 0)
So camRight = (1, 0, 0) which is correct (pointing right).

In Rhino, CameraDirection returns the view direction, so if the camera is at (0, 0, 5) looking at the origin, it returns (0, 0, -1) normalized. With these values, the cross product gives me the correct right vector, and the mesh center calculation places it properly in front of the camera at (0, 0, 3). The mesh computation looks right, so the problem might be elsewhere—either the conduit's drawing method isn't being called for all viewport types, or there's something else blocking the rendering.

Let me try some debugging approaches: I could add a simple point or line draw to verify the conduit fires at all, or switch to DrawMeshWires to test if it's a shading issue. I'm also wondering if using DrawOverlay instead of PostDrawObjects would help, or if I need to force the viewport's bounding box to include the mesh area. Actually, I just realized there might be an issue with how the DisplayConduit is being enabled in Rhino—let me check that.

I'm also questioning whether PostDrawObjects even fires when the document is empty, and whether the perspective projection check is filtering out the viewport. Let me remove that check and try drawing in any viewport, then add a simple test to see if the conduit is actually being called. I need to add debug output to both the command and the conduit to figure out whether the issue is that pairs aren't being created or the conduit isn't drawing them. I'll add verbose logging to the command, switch the conduit to use DrawOverlay instead of PostDrawObjects, remove the perspective projection filter, and use a solid bright color to rule out texture issues.

Let me add a solid-color fallback draw to confirm whether the conduit is firing at all, remove the perspective filter, and switch to DrawOverlay which is more reliable for custom drawing:

Edit PhotoPlaneConduit.cs
Added 7 lines
Also remove the fn <= 0 early return and use a fixed fallback size so we always get something drawn:

Edit PhotoPlaneConduit.cs
Added 9 lines
Also add a line to the command so we can clearly see if the pair was registered:

Edit AddPhotoPlaneCommand.cs
Added 1 line
Build and run PMAddPhotoPlane. Then check:

Command line — does it say Registry now has 1 photo plane(s). Redrawing...? If not, the pair creation is failing
Viewport — do you see anything (even a magenta dot or magenta plane)? If yes, the conduit fires but the texture isn't loading. If no, the conduit itself isn't firing
Tell me which case you see and I'll fix the right thing.

it says "Registry now has 1 photo plane(s). Redrawing...", nothing is shwing, no material is created
OK so the conduit IS firing (we know the registry has a pair), but nothing is showing. And "no material is created" - so the issue is in GetMaterial or DrawMeshShaded.

Wait, actually "no material is created" might mean the magenta fallback isn't showing either. Let me think...

If the magenta fallback (DrawMeshShaded(mesh, new DisplayMaterial(Color.Magenta, 0.5))) also doesn't show, and even the fallback magenta point from DrawPoint doesn't show, then the conduit IS registered but DrawOverlay isn't being called OR the draw calls aren't working.

Ah, I see - the user is saying no material appears in the Rhino Material editor, which makes sense since we're creating a transient material object but never adding it to the document's material collection. That's not actually a problem for the display though.

The real issue is that the pair gets created in the registry, but nothing renders on screen. The conduit is firing during the draw event, but the mesh isn't appearing. I'm wondering if DrawMeshShaded doesn't work properly in the DrawOverlay event, or if there's a display mode limitation I'm not considering. Let me test with simpler geometry like points or lines to confirm the overlay is actually drawing, or switch back to PostDrawObjects to see if that makes a difference. The issue is that DrawOverlay might not support DrawMeshShaded properly, so I should try PostDrawObjects with a different drawing approach. But first I need to figure out what's actually failing—whether the mesh is being computed but failing to draw, or if the conduit itself isn't firing. If I put a DrawPoint in the fallback path and nothing shows, that suggests the conduit has a deeper problem, but if the point does show, then the mesh computation is working and it's just the mesh drawing that's failing.

Let me add diagnostic drawing to confirm the conduit is firing and the mesh geometry is valid—I'll draw a magenta point unconditionally and use DrawMeshWires to visualize the mesh outline in a bright color, which should work regardless of display mode.

Modifying the conduit to add these checks...

Wait, I'm wondering if DrawOverlay might be the culprit here. It's technically 3D, but it draws after depth testing, so layering could be an issue. More importantly, the user mentioned no material is created—what if GetMaterial is silently failing when SetBitmapTexture can't load the file? There's no try-catch in that method, so an exception would cause the whole DrawOverlay call to fail without any visible error.

Let me reconsider the approach entirely. Instead of wrestling with 3D materials and meshes, I could load the bitmap directly and draw it as a 2D screenspace overlay using DisplayPipeline.DrawBitmap, which would sidestep all this material complexity.

But that won't work for camera matching since it's in screen coordinates, not 3D space. So let me try a diagnostic approach instead: test with a solid magenta color first to isolate whether the problem is the mesh itself or the texture loading. If the magenta plane renders, I know the mesh and drawing method work, and the issue is specifically with how the texture is being applied. I should also test wireframe rendering to see if that works at all.

The real fix is switching from DrawOverlay to PostDrawObjects and using solid color draws as a baseline test. Actually, I'm wondering if DrawOverlay is being skipped entirely because there's nothing else in the scene—the display pipeline might optimize away empty stages. Let me switch to PostDrawObjects and add some simple debug geometry that's guaranteed to render, then figure out why the shaded mesh isn't showing up.

Wait, I just realized something important: DrawMeshShaded only works in display modes that support shading like Shaded or Rendered. If the user is in Wireframe mode, nothing will show even though the conduit is running. I should add wireframe draws and a bounding box that's visible regardless of display mode, then make the change.

The conduit fires (pair is registered) but DrawMeshShaded is likely silently failing — either the display mode is Wireframe (textures don't show), or DrawOverlay doesn't support 3D mesh shading. Let me switch back to PostDrawObjects, draw in wireframe too so it's visible in any mode, and test with a solid color first:

Edit PhotoPlaneConduit.cs
Removed 1 line
Build and test. You should now see at minimum a cyan rectangle outline in any display mode (Wireframe, Shaded, Rendered). If:

Cyan border appears → conduit + mesh are working; the issue was display mode. Switch to Shaded/Rendered to see the photo texture.
Nothing at all → PostDrawObjects itself isn't running. Tell me and I'll investigate conduit registration.
i want to do "Option C — Debug from Visual Studio (best for development)"
The user wants to set up Visual Studio debugging with Rhino as the startup program. I need to add the debug launch configuration to the .csproj file or create a launchSettings.json.

For .NET SDK-style projects (which this is), the debug settings can be configured in:

Properties/launchSettings.json
Or in the .csproj via <PropertyGroup Condition="'$(Configuration)'=='Debug'">
The launchSettings.json approach is cleaner for .NET projects. But for a Class Library project (not a console/web app), we need to set the external program to launch.

In Visual Studio, for a .NET Class Library, you'd set:

Project Properties → Debug → Start external program: C:\Program Files\Rhino 8\System\Rhino.exe
I can configure this through a launchSettings.json file in the Properties folder, where I'll set up an executable profile that points to Rhino with the appropriate command-line arguments and working directory.

Configuring launch settings...

For the plugin to load automatically during debugging, I'll need to add a post-build event that copies the compiled .rhp file to Rhino's plugin directory at %APPDATA%\McNeel\Rhinoceros\8.0\Plug-ins\, or alternatively configure Rhino to search for plugins in the build output folder. Now I'm setting up the post-build event to copy the plugin to Rhino's plugin directory, which for Rhino 8 is the AppData folder, though I could also have users register a custom search path in Rhino instead.

I need to create two things: a launchSettings.json so VS launches Rhino on F5, and a post-build event to auto-copy the .rhp so Rhino picks it up automatically.

Bash Verify Rhino 8 exe path
IN
ls "C:/Program Files/Rhino 8/System/Rhino.exe" 2>&1 && echo "EXISTS"

OUT
C:/Program Files/Rhino 8/System/Rhino.exe
EXISTS
Bash Create Properties folder
IN
mkdir -p "c:/SRC/RHINO/VIEW/RhinoPhotoMatch/Properties"

OUT
0
Write launchSettings.json
10 lines
{
  "profiles": {
    "Rhino 8": {
      "commandName": "Executable",
      "executablePath": "C:\\Program Files\\Rhino 8\\System\\Rhino.exe",
      "commandLineArgs": "/nosplash"
    }
  }
}
Now add the post-build event to the .csproj:

Read RhinoPhotoMatch.csproj
Edit RhinoPhotoMatch.csproj
Added 10 lines
Now the setup is complete. Here's what to do in Visual Studio:

One-time Rhino setup (first time only):

Open Rhino 8
Go to Tools → Options → Plug-ins
Click Install..., browse to the AppData folder shown in the build output: %APPDATA%\McNeel\Rhinoceros\8.0\Plug-ins\RhinoPhotoMatch\RhinoPhotoMatch.rhp
Click OK and close Rhino
From now on — F5 workflow:

Open RhinoPhotoMatch.sln in Visual Studio
Make sure the "Rhino 8" launch profile is selected in the toolbar (dropdown next to the Run button)
Press F5 — VS builds, copies the .rhp to AppData, and launches Rhino with the debugger attached
Set breakpoints anywhere in the C# code — they will hit when Rhino calls into the plugin
After each build the .rhp is automatically copied — you don't need to reinstall in Rhino.

nope no photo
The conduit PostDrawObjects isn't drawing anything visible. Let me think about what's happening.

Actually, I just realized something important. When you use a DisplayConduit in Rhino, setting Conduit.Enabled = true might not be enough. The conduit needs to be registered per-view or it needs the Rhino.Display.DisplayConduit base class's Enabled property properly set.

But wait - looking at the Rhino SDK, setting conduit.Enabled = true SHOULD register it globally. When enabled, it fires for all views.

However, I just thought of something: maybe the conduit IS firing but the draw calls are being clipped or not visible for some reason.

Let me test with a hardcoded draw call at a known location - drawing a red line from the world origin to confirm the conduit is actually executing. If that line shows up, I know the conduit is working and the issue is with how I'm drawing the pairs.

But there could be other culprits: maybe the conduit was registered before the plugin fully initialized, or there's a threading issue. Or perhaps the geometry is being clipped by Rhino's near clip plane, or it's failing the Z-test because it's hidden behind other objects. Let me add that definitive test line first to rule out whether the conduit is even running.

Actually, I'm wondering if the conduit needs to be attached to a specific view rather than being global. Let me check how DisplayConduit typically works in RhinoCommon... it should fire globally when enabled, but maybe Rhino 8 changed that behavior. Or here's another thought: PostDrawObjects might only fire when there are actual objects in the scene, so in an empty document it wouldn't trigger at all. Let me trace through the display pipeline stages to verify.

The pipeline runs through calculation, pre-draw setup, per-object callbacks, post-draw, and foreground overlays. If PostDrawObjects only fires when objects exist, I should try switching to DrawForeground which always fires, though that's meant for 2D drawing. PreDrawObjects might be the better option since it triggers before objects are drawn and should always execute.

Actually, looking at the Rhino source, PostDrawObjects fires after all opaque objects are drawn regardless of whether any objects exist—it's one of the most reliable callback points. So if the conduit is enabled and PostDrawObjects is firing, the issue must be that the draw calls aren't rendering correctly. The mesh vertices might not be visible to DrawMeshWires, or the viewport could be suppressing custom drawing. I'm also wondering if PostDrawObjects fires for each viewport including inactive ones, which could mean the callback is using a viewport that doesn't match the main display. Let me try the simplest debug approach: drawing a line from the world origin to see if anything renders at all.

Testing with a basic line draw...

Actually, I should reconsider whether DisplayConduit.Enabled = true works as expected in Rhino 8 with .NET 7—there might be a different registration pattern in the SDK examples.

The standard approach should work globally, but let me explore alternatives: DrawForeground is screen-space only, so that won't help for 3D geometry. The real 3D callbacks are PreDrawObjects and PostDrawObjects, and they require hardware rendering support in the viewport.

Maybe the issue is thread-related, or I need to verify the conduit is being registered correctly before the viewport renders.

Actually, I'm wondering if the timing is the problem—the conduit might be enabled before Rhino's display system is fully initialized, or doc.Views.Redraw() schedules the redraw asynchronously rather than forcing it immediately. The user should see the geometry when orbiting the view, but they're saying nothing appears even with interaction, which suggests the callback isn't firing at all.

I'm going to add a hardcoded diagnostic line that draws unconditionally in red—if that doesn't show up, the conduit registration itself is broken; if it does but the photo plane doesn't, then the issue is in the pair drawing logic.

Adding diagnostic code...

But I'm also wondering if the real problem is a unit mismatch—if the pair distance is 2.0 millimeters in a model where the viewport is zoomed way out, the plane would be invisible or clipped. I'm realizing the plane should actually be positioned correctly—it's in front of the camera, beyond the near clip plane, and well within the far clip. The real issue is probably the plane's size: at a distance of 2 units with a typical 50° field of view, the plane would only be about 1.86 units across, which in a millimeter-based model would be nearly invisible against a much larger scene.

Let me verify the frustum math is working as intended. The calculation takes the frustum width at the near plane, then scales it proportionally to our placement distance—so if the near plane is at 0.1 units and we're placing the plane at 2.0 units, the dimensions should scale up correctly by a factor of 20.

Working through the numbers: with a 60° FOV and near clip at 0.1, the frustum width comes out to roughly 0.115 units, which scales to about 2.3 units at distance 2.0. That's a small plane, but the real issue is that placing it 2 units in front of the camera when the camera is 50-100 units away from the target means the plane appears right in your face—you'd need to position yourself at a very specific angle to see it properly.

Actually, that's intentional—the plane should always stay 2 units ahead of the camera as you orbit, so it should remain visible. Let me trace through the math: if the camera is at position (100, 50, 80) looking toward the origin, the camera direction vector points back toward the target, and placing the plane 2 units along that direction should position it correctly in front of the camera. The plane should be visible...

But wait, I think I found the real issue. The display pipeline in Rhino might be clipping objects that fall outside the calculated bounding box, and since we're drawing a virtual mesh in PostDrawObjects that's always positioned relative to the camera, it could be getting culled if we haven't overridden CalculateBoundingBox to account for it. That's probably why the plane isn't showing up—I need to make sure the bounding box includes the virtual mesh so the display pipeline doesn't clip it. I need to check if CalculateBoundingBoxEventArgs has viewport access so I can compute the actual plane position for each camera rather than using a massive bounding box—if it does, I can calculate the precise bounds, otherwise I'll fall back to the large box approach.

Actually, I'm second-guessing whether the bounding box is even the culprit here. The scene bounding box affects zoom extents, but the display pipeline uses the camera's near and far planes for clipping, not that. Let me just test with a simple hardcoded line at the origin to see if anything renders at all.

Writing diagnostic code...

If that red line shows up, the conduit is definitely firing. If not, there's a fundamental registration problem. I'll make this change and let the user know what I find.

Let me add the most basic possible diagnostic — a hardcoded thick red line at the world origin — to confirm whether PostDrawObjects fires at all:

Edit PhotoPlaneConduit.cs
Added 3 lines
Build and run. Do you see a thick red line at the world origin in any viewport?

Yes → conduit fires. The issue is in the plane mesh computation or the material. I'll fix the drawing.
No → conduit doesn't fire. The Enabled = true registration isn't working. I'll investigate that.
yes red line is visable
The conduit fires and PostDrawObjects works. So the issue is in the mesh computation or the drawing of the pairs. Since we know the conduit fires, let me add a diagnostic to confirm the pair IS being drawn (mesh is being computed) by checking if the cyan wireframe shows.

Actually, re-reading the test: the red line is visible. That means PostDrawObjects fires. But after the pair is created, the cyan wireframe and magenta shaded mesh should also appear. The user said "no photo" - but maybe after adding the red line diagnostic they're testing WITHOUT having imported a photo first?

Wait, let me re-read: "yes red line is visible" - this was before running PMAddPhotoPlane or after? I think the user is just confirming the conduit fires. Now we need to confirm whether the pair draw (cyan wireframe) appears after running the command.

So the mesh computation might be returning null when the pair is created, which would explain why only the magenta point shows up instead of the cyan wireframe and shaded mesh. The issue could be in how the camera direction and up vectors are being normalized - if any of those unitize operations fail because the vectors are zero or parallel, the whole mesh computation would fail.

In a standard Rhino viewport, the camera direction should be valid and point from the camera toward the target, and the up vector should also be valid. The right vector is computed as a cross product, so it should work unless the direction and up are parallel. Let me verify whether CameraDirection is already normalized or if we need to call Unitize() on it.

Looking at the Rhino SDK, CameraDirection returns the normalized look direction, but calling Unitize() on it is safe anyway. So ComputePlaneMesh should produce a valid mesh. The pair gets added to the registry before the redraw call, which triggers PostDrawObjects, so the cyan wireframe should appear unless there's a timing issue—but that shouldn't happen since the redraw is synchronous.

Actually, I'm wondering if the plane is being drawn but isn't visible because the viewport's near or far clip plane is culling it. If the plane is positioned 2.0 units in front of the camera, it could be outside the clipping range depending on Rhino's dynamic near clip calculation and the far clip distance.

Let me add some diagnostic draws to verify the plane is actually being rendered at the expected position—I'll draw a point at the computed plane center and maybe a line from the camera toward it to confirm it's in the right direction. But this is getting complicated, so I should probably just ask the user to run the command and check what they see in the viewport: is there a cyan rectangle, a magenta dot as a fallback, or nothing at all?

Actually, thinking about what they've been reporting—they keep saying "no photo" but the registry shows 1 pair exists after running the command, yet nothing appears except the red line. So the mesh either isn't being computed or the draw calls for it aren't executing, which means I need to narrow down whether ComputePlaneMesh is returning null or if there's an issue with the rendering itself. ), so the red conduit line is working. Now I need them to actually run PMAddPhotoPlane to create a pair, then I can add diagnostic code to draw a sphere at the expected plane position to see if the mesh computation is the bottleneck.

Adding diagnostic sphere... Unitize() would fail and return false, which means the plane mesh computation would bail out early. That's probably why nothing is being drawn—the camera direction vector is collapsing to zero in certain viewport states, causing the whole operation to short-circuit.

Let me add some debug logging to trace what's actually happening with the camera direction and mesh computation...

Adding debug output...

Actually, logging every frame will be too noisy. Instead, I'll draw a sphere directly at the computed plane center to visually confirm the position calculation is working correctly.

Good — conduit fires. Now run PMAddPhotoPlane, pick a photo, and look at the viewport. The next question is: does the cyan wireframe or magenta dot appear after the command? Add a yellow sphere at the expected plane center to diagnose the pair draw path:

Edit PhotoPlaneConduit.cs
Added 6 lines
Build, run PMAddPhotoPlane, pick a photo. Tell me what you see after the command:

Yellow sphere + cyan rectangle → mesh draws correctly, issue is only the texture
Yellow sphere only, no cyan → ComputePlaneMesh returns null; I'll fix the calculation
Nothing extra (no yellow sphere) → pair isn't in the registry when the draw fires — timing bug
ok! so i can see the photoplane in all perspective viewports but id like to see it only in one. also id like to see it in relation to the camera like my image is that possible?
Great progress! The photo plane is working. Now the user wants:

Show the plane only in ONE specific viewport (the one linked to the pair)
Show the camera frustum visualization (like in the screenshot - pyramid lines from camera to the plane corners)
For (1): We already have pair.ActiveViewportId - we just need to re-enable the viewport ID filter. The issue earlier was the ID matching - but now we know the conduit fires, so the ID should match correctly.

For (2): Draw the camera frustum - lines from the camera location to the 4 corners of the plane. This gives the pyramid visualization shown in the user's reference image.

Now I'm cleaning up the debug visuals and implementing the viewport filtering and frustum lines - I'll re-enable the viewport ID check, draw lines from the camera to each plane corner for the pyramid effect, and strip out the diagnostic red line and yellow sphere.

The plane is working. Now let's:

Restore the per-viewport filter (one plane per viewport)
Draw the camera frustum pyramid — lines from camera to plane corners
Remove all diagnostic draws
Read PhotoPlaneConduit.cs
Write PhotoPlaneConduit.cs
135 lines
using System.Collections.Generic;
using System.Drawing;
using Rhino.Display;
using Rhino.Geometry;

namespace RhinoPhotoMatch.Core
{
    /// <summary>
    /// Draws each registered photo plane as a textured quad in its linked viewport only.
    /// The quad and camera frustum pyramid are recomputed every frame from the live camera,
    /// so the plane always stays in front of the camera at pair.Distance.
    /// </summary>
    public sealed class PhotoPlaneConduit : DisplayConduit
    {
        private readonly PhotoPlaneRegistry _registry;
        private readonly Dictionary<string, DisplayMaterial> _materialCache = new();

        public PhotoPlaneConduit(PhotoPlaneRegistry registry)
        {
            _registry = registry;
        }

        // ------------------------------------------------------------------ //
        //  Public API
        // ------------------------------------------------------------------ //

        /// <summary>Call this after relinking a photo so the texture is reloaded next frame.</summary>
        public void InvalidateMaterial(string pairName) => _materialCache.Remove(pairName);

        // ------------------------------------------------------------------ //
        //  DisplayConduit overrides
        // ------------------------------------------------------------------ //

        protected override void PostDrawObjects(DrawEventArgs e)
        {
            if (_registry.Pairs.Count == 0) return;

            foreach (var pair in _registry.Pairs)
            {
                // Only draw in the viewport that owns this pair
                if (e.Viewport.Id != pair.ActiveViewportId)
                    continue;

                var mesh = ComputePlaneMesh(e.Viewport, pair);
                if (mesh == null) continue;

                // Frustum pyramid: lines from camera location to each plane corner
                var camLoc = e.Viewport.CameraLocation;
                var frustumColor = Color.FromArgb(120, 200, 200, 200);
                for (int i = 0; i < mesh.Vertices.Count; i++)
                    e.Display.DrawLine(camLoc, mesh.Vertices[i], frustumColor, 1);

                // Plane border
                e.Display.DrawMeshWires(mesh, Color.FromArgb(180, 255, 255, 255));

                // Textured face (visible in Shaded / Rendered modes)
                var material = GetMaterial(pair);
                e.Display.DrawMeshShaded(mesh, material ?? new DisplayMaterial(Color.White));
            }
        }

        // ------------------------------------------------------------------ //
        //  Helpers
        // ------------------------------------------------------------------ //

        private static Mesh? ComputePlaneMesh(RhinoViewport vp, PhotoPlanePair pair)
        {
            var camDir   = vp.CameraDirection; if (!camDir.Unitize()) return null;
            var camUp    = vp.CameraUp;        if (!camUp.Unitize())  return null;
            var camRight = Vector3d.CrossProduct(camDir, camUp);
            if (!camRight.Unitize()) return null;

            vp.GetFrustum(out double fl, out double fr, out double fb, out double ft,
                          out double fn, out _);

            double vpW, vpH;
            if (fn > 0)
            {
                vpW = (fr - fl) * pair.Distance / fn;
                vpH = (ft - fb) * pair.Distance / fn;
            }
            else
            {
                vpW = pair.Distance;
                vpH = pair.Distance / pair.AspectRatio;
            }

            // Letterbox to preserve photo aspect ratio within the FOV rectangle
            double vpAspect = vpW / vpH;
            double planeW, planeH;
            if (pair.AspectRatio >= vpAspect)
            {
                planeW = vpW;
                planeH = vpW / pair.AspectRatio;
            }
            else
            {
                planeH = vpH;
                planeW = vpH * pair.AspectRatio;
            }

            planeW *= pair.Scale;
            planeH *= pair.Scale;

            var center = vp.CameraLocation + camDir * pair.Distance;
            return PicturePlaneManager.BuildCameraAlignedMesh(center, camRight, camUp, planeW, planeH);
        }

        private DisplayMaterial GetMaterial(PhotoPlanePair pair)
        {
            if (_materialCache.TryGetValue(pair.Name, out var cached))
                return cached;

            var docMat = new Rhino.DocObjects.Material
            {
                Name          = pair.Name,
                DiffuseColor  = Color.White,
                EmissionColor = Color.White,
                Reflectivity  = 0,
                Shine         = 0,
                Transparency  = 0
            };
            docMat.SetBitmapTexture(pair.ImagePath);

            string ext = System.IO.Path.GetExtension(pair.ImagePath).ToLowerInvariant();
            if (ext == ".png")
                docMat.SetTransparencyTexture(pair.ImagePath);

            var displayMat = new DisplayMaterial(docMat);
            _materialCache[pair.Name] = displayMat;
            return displayMat;
        }
    }
}
Build and test. The photo plane now:

Draws only in the viewport that was active when you ran PMAddPhotoPlane
Shows 4 lines from the camera to each plane corner (the pyramid from your reference image)
Shows the photo texture in Shaded/Rendered display mode
If you want to reassign a plane to a different viewport later, I can add a PMActivatePhotoPlane command that sets pair.ActiveViewportId to whichever viewport you're currently in.

i cant see the image plane in relation to the camera idealy you would see the image
The user wants to see the photo plane from OUTSIDE the camera viewport - i.e., when looking at the scene from another viewport (like an isometric view), they should see the camera frustum pyramid with the image plane at the end. Currently, the plane only draws in the viewport that "owns" it (the perspective viewport linked to the pair). But from an external/overview viewport, you can't see it.

The key insight is:

The image plane should be visible from ALL viewports (including orthographic/isometric ones) as a 3D object in world space

But when looking THROUGH the linked camera (the perspective viewport that owns the pair), the photo fills the entire view as a background

In external viewports, I need to render it as a textured 3D plane positioned in world space with the camera frustum visible, while in the linked viewport it becomes a full-screen background photo

The key insight is that the plane's position is always relative to the named view's camera (not the current viewport's camera), so I compute its world position using that fixed camera's location and direction, then draw accordingly based on which viewport is active

.

Now I'm checking how to retrieve the named view from the document—I need to find it by name in the NamedViewTable and access its viewport information to get the camera details.

The issue is that named views are static snapshots, so when the user moves the camera in the linked viewport, the named view doesn't update. For the "follows camera" behavior to work properly, I should pull the current camera state from the actual viewport itself rather than relying on the stored named view. I can look up the viewport by its GUID to get its live camera position.

Looking up the viewport by ID...

In PostDrawObjects, I'll need to retrieve the current camera from the linked viewport for each pair and use that to compute the plane position when drawing in external viewports. Since I can access RhinoDoc.ActiveDoc from anywhere, I can look up the viewport and get its current state.

The key distinction is: when drawing inside the linked viewport itself, keep the current behavior where the plane fills the field of view at a fixed distance. But when drawing from an external viewport, I need to compute the plane's actual world-space position based on the linked camera's current transform, then render it as a 3D object with frustum lines so it's visible in context.

Implementing the viewport drawing logic... Writing the rendering and viewport lookup logic...

The plane follows the linked camera automatically as it moves through the viewport, which is the behavior we want. There's a subtle issue though: when drawing inside the linked viewport itself, the plane fills the entire view since we're looking directly at it.

For the photo matching workflow, the photo should act as a background reference with the 3D model rendered on top. Since we're drawing in PostDrawObjects, the geometry gets drawn first and sits in the depth buffer, so the photo plane behind it would be correctly occluded by closer objects. But actually, drawing in PostDrawObjects puts the plane on top of everything, which is the opposite of what we need for a background effect.

I'm realizing the solution depends on the viewport context: in the linked viewport, I should draw the photo plane in PreDrawObjects so it renders as a background before the model, while in external viewports it could go in PostDrawObjects as a 3D world object. Though that split approach gets messy, so maybe I should stick with PostDrawObjects but disable depth testing for the linked viewport so the plane appears behind everything regardless of draw order.

Actually, the cleanest approach is simpler—just draw the textured mesh in PostDrawObjects and let the depth buffer handle it naturally. If I position the plane at distance D from the camera, any model geometry closer than D will render in front of it, and anything farther will be hidden behind it, which is the correct behavior for a background. The user just needs to set D far enough that the model sits in front of the plane. the view) in the linked viewport and the plane with frustum visualization in external viewports. I'll implement this clean separation where the plane's world-space position is always computed from the linked camera, then rendered appropriately depending on which viewport is active.

Writing the conduit implementation...

The fix: compute the plane from the linked viewport's camera (world-space), draw it in ALL viewports — textured when looking through the camera, with frustum lines when viewed from outside.

Write PhotoPlaneConduit.cs
179 lines
using System.Collections.Generic;
using System.Drawing;
using Rhino;
using Rhino.Display;
using Rhino.Geometry;

namespace RhinoPhotoMatch.Core
{
    /// <summary>
    /// Draws each photo plane as a world-space textured quad positioned relative to
    /// its linked camera. The plane is visible from ALL viewports:
    ///   - Linked viewport  : photo fills the view (background reference)
    ///   - Other viewports  : plane + frustum pyramid are drawn in 3D world space
    /// </summary>
    public sealed class PhotoPlaneConduit : DisplayConduit
    {
        private readonly PhotoPlaneRegistry _registry;
        private readonly Dictionary<string, DisplayMaterial> _materialCache = new();

        public PhotoPlaneConduit(PhotoPlaneRegistry registry)
        {
            _registry = registry;
        }

        public void InvalidateMaterial(string pairName) => _materialCache.Remove(pairName);

        // ------------------------------------------------------------------ //
        //  Draw background (linked viewport only — fires before geometry)
        // ------------------------------------------------------------------ //

        protected override void PreDrawObjects(DrawEventArgs e)
        {
            if (_registry.Pairs.Count == 0) return;
            var doc = RhinoDoc.ActiveDoc;
            if (doc == null) return;

            foreach (var pair in _registry.Pairs)
            {
                if (e.Viewport.Id != pair.ActiveViewportId) continue;

                var linkedVp = FindViewport(doc, pair.ActiveViewportId);
                if (linkedVp == null) continue;

                var mesh = ComputePlaneMesh(linkedVp, pair);
                if (mesh == null) continue;

                // Draw as background — geometry will render on top
                var material = GetMaterial(pair);
                e.Display.DrawMeshShaded(mesh, material ?? new DisplayMaterial(Color.White));
            }
        }

        // ------------------------------------------------------------------ //
        //  Draw 3D plane + frustum in all OTHER viewports
        // ------------------------------------------------------------------ //

        protected override void PostDrawObjects(DrawEventArgs e)
        {
            if (_registry.Pairs.Count == 0) return;
            var doc = RhinoDoc.ActiveDoc;
            if (doc == null) return;

            foreach (var pair in _registry.Pairs)
            {
                // Skip the linked viewport — already drawn in PreDrawObjects
                if (e.Viewport.Id == pair.ActiveViewportId) continue;

                var linkedVp = FindViewport(doc, pair.ActiveViewportId);
                if (linkedVp == null) continue;

                var mesh = ComputePlaneMesh(linkedVp, pair);
                if (mesh == null) continue;

                // Frustum pyramid: lines from linked camera to plane corners
                var camLoc = linkedVp.CameraLocation;
                var frustumColor = Color.FromArgb(140, 200, 200, 200);
                for (int i = 0; i < mesh.Vertices.Count; i++)
                    e.Display.DrawLine(camLoc, mesh.Vertices[i], frustumColor, 1);

                // Draw a dot at the camera location
                e.Display.DrawPoint(camLoc, PointStyle.Circle, 5, Color.White);

                // Plane border + textured face
                e.Display.DrawMeshWires(mesh, Color.FromArgb(200, 255, 255, 255));
                var material = GetMaterial(pair);
                e.Display.DrawMeshShaded(mesh, material ?? new DisplayMaterial(Color.White));
            }
        }

        // ------------------------------------------------------------------ //
        //  Helpers
        // ------------------------------------------------------------------ //

        /// <summary>
        /// Finds the RhinoViewport matching the given ID across all open views.
        /// Returns null if the viewport is no longer open.
        /// </summary>
        private static RhinoViewport? FindViewport(RhinoDoc doc, System.Guid viewportId)
        {
            foreach (var view in doc.Views)
                if (view.ActiveViewport.Id == viewportId)
                    return view.ActiveViewport;
            return null;
        }

        /// <summary>
        /// Builds the plane mesh from the given viewport's live camera.
        /// The plane fills the camera FOV (letterboxed to the photo aspect ratio)
        /// at pair.Distance, scaled by pair.Scale.
        /// </summary>
        private static Mesh? ComputePlaneMesh(RhinoViewport vp, PhotoPlanePair pair)
        {
            var camDir   = vp.CameraDirection; if (!camDir.Unitize()) return null;
            var camUp    = vp.CameraUp;        if (!camUp.Unitize())  return null;
            var camRight = Vector3d.CrossProduct(camDir, camUp);
            if (!camRight.Unitize()) return null;

            vp.GetFrustum(out double fl, out double fr, out double fb, out double ft,
                          out double fn, out _);

            double vpW, vpH;
            if (fn > 0)
            {
                vpW = (fr - fl) * pair.Distance / fn;
                vpH = (ft - fb) * pair.Distance / fn;
            }
            else
            {
                vpW = pair.Distance;
                vpH = pair.Distance / pair.AspectRatio;
            }

            double vpAspect = vpW / vpH;
            double planeW, planeH;
            if (pair.AspectRatio >= vpAspect)
            {
                planeW = vpW;
                planeH = vpW / pair.AspectRatio;
            }
            else
            {
                planeH = vpH;
                planeW = vpH * pair.AspectRatio;
            }

            planeW *= pair.Scale;
            planeH *= pair.Scale;

            var center = vp.CameraLocation + camDir * pair.Distance;
            return PicturePlaneManager.BuildCameraAlignedMesh(center, camRight, camUp, planeW, planeH);
        }

        private DisplayMaterial GetMaterial(PhotoPlanePair pair)
        {
            if (_materialCache.TryGetValue(pair.Name, out var cached))
                return cached;

            var docMat = new Rhino.DocObjects.Material
            {
                Name          = pair.Name,
                DiffuseColor  = Color.White,
                EmissionColor = Color.White,
                Reflectivity  = 0,
                Shine         = 0,
                Transparency  = 0
            };
            docMat.SetBitmapTexture(pair.ImagePath);

            string ext = System.IO.Path.GetExtension(pair.ImagePath).ToLowerInvariant();
            if (ext == ".png")
                docMat.SetTransparencyTexture(pair.ImagePath);

            var displayMat = new DisplayMaterial(docMat);
            _materialCache[pair.Name] = displayMat;
            return displayMat;
        }
    }
}
Build and test. You should now see:

Perspective viewport (linked) — photo fills the view as a background; your 3D model renders on top
Any other viewport (Top/Front/isometric) — the image plane appears as a textured 3D quad in world space with white frustum lines going from the camera point to the plane corners, exactly like your reference image
I want to impliment a new function in the plugin PMExtractPhotoPlane the idea is it creates a rhino object form the photoplane with the photo as material
The user wants a new command PMExtractPhotoPlane that creates a real Rhino document object (a mesh) from the virtual photo plane, with the photo assigned as a material. This essentially "bakes" the conduit-drawn plane into the document.

Here's what I need to do:

Get the current plane position from the linked viewport's camera
Build the mesh (same as ComputePlaneMesh)
Create a material with the photo as bitmap texture
Add the mesh to the document with that material
Report success
The command needs access to the current state of the linked viewport's camera to compute the world-space plane position. This is similar to what the conduit does.

Now I'm implementing the command by finding the viewport pair, computing the plane mesh, creating a material with the photo texture, and adding both to the document with the proper attributes.

I need to refactor the mesh computation and viewport lookup logic out of the conduit so the command can reuse them—moving FindViewport and ComputePlaneMesh to PicturePlaneManager as public static helpers makes the most sense rather than duplicating code. there.

Let me add to PicturePlaneManager:

FindViewport(RhinoDoc doc, Guid viewportId) → returns RhinoViewport?
ComputePlaneMesh(RhinoViewport vp, PhotoPlanePair pair) → returns Mesh?
And in the conduit, call these methods instead of having them private.

Then ExtractPhotoPlaneCommand can use PicturePlaneManager.ComputePlaneMesh.

Let me do this refactor:

Update PicturePlaneManager.cs - add FindViewport and ComputePlaneMesh as public methods
Update PhotoPlaneConduit.cs - use the methods from PicturePlaneManager
Create Commands/ExtractPhotoPlaneCommand.cs
Now I need to handle the extracted mesh material by creating a persistent material in the document with bitmap textures, setting up transparency for PNGs, making it self-illuminated, and assigning it to the mesh object.

Good idea — this "bakes" the virtual plane into a real Rhino object. First I'll move the shared geometry helpers to PicturePlaneManager so both the conduit and the new command can use them, then create the command.

Update Todos

Move FindViewport + ComputePlaneMesh to PicturePlaneManager (shared helpers)

Update PhotoPlaneConduit to use PicturePlaneManager helpers

Create Commands/ExtractPhotoPlaneCommand.cs
Write PicturePlaneManager.cs
149 lines
using System;
using System.Drawing;
using System.IO;
using Rhino;
using Rhino.Display;
using Rhino.DocObjects;
using Rhino.Geometry;

namespace RhinoPhotoMatch.Core
{
    /// <summary>
    /// Shared geometry and viewport helpers used by PhotoPlaneConduit
    /// and commands that operate on photo planes.
    /// </summary>
    public static class PicturePlaneManager
    {
        // ------------------------------------------------------------------ //
        //  Viewport lookup
        // ------------------------------------------------------------------ //

        /// <summary>
        /// Finds the RhinoViewport with the given ID across all open views.
        /// Returns null if the viewport is no longer open.
        /// </summary>
        public static RhinoViewport? FindViewport(RhinoDoc doc, Guid viewportId)
        {
            foreach (var view in doc.Views)
                if (view.ActiveViewport.Id == viewportId)
                    return view.ActiveViewport;
            return null;
        }

        // ------------------------------------------------------------------ //
        //  Plane mesh computation
        // ------------------------------------------------------------------ //

        /// <summary>
        /// Builds the photo plane mesh from a viewport's live camera.
        /// The plane fills the camera FOV (letterboxed to the photo aspect ratio)
        /// at pair.Distance, scaled by pair.Scale.
        /// Returns null if the viewport camera is degenerate.
        /// </summary>
        public static Mesh? ComputePlaneMesh(RhinoViewport vp, PhotoPlanePair pair)
        {
            var camDir   = vp.CameraDirection; if (!camDir.Unitize()) return null;
            var camUp    = vp.CameraUp;        if (!camUp.Unitize())  return null;
            var camRight = Vector3d.CrossProduct(camDir, camUp);
            if (!camRight.Unitize()) return null;

            vp.GetFrustum(out double fl, out double fr, out double fb, out double ft,
                          out double fn, out _);

            double vpW, vpH;
            if (fn > 0)
            {
                vpW = (fr - fl) * pair.Distance / fn;
                vpH = (ft - fb) * pair.Distance / fn;
            }
            else
            {
                // Parallel projection fallback
                vpW = pair.Distance;
                vpH = pair.Distance / pair.AspectRatio;
            }

            double vpAspect = vpW / vpH;
            double planeW, planeH;
            if (pair.AspectRatio >= vpAspect)
            {
                planeW = vpW;
                planeH = vpW / pair.AspectRatio;
            }
            else
            {
                planeH = vpH;
                planeW = vpH * pair.AspectRatio;
            }

            planeW *= pair.Scale;
            planeH *= pair.Scale;

            var center = vp.CameraLocation + camDir * pair.Distance;
            return BuildCameraAlignedMesh(center, camRight, camUp, planeW, planeH);
        }

        // ------------------------------------------------------------------ //
        //  Mesh builder
        // ------------------------------------------------------------------ //

        /// <summary>
        /// Builds a camera-aligned quad mesh centred on <paramref name="center"/>
        /// with UV coordinates mapped to fill the texture.
        /// </summary>
        public static Mesh BuildCameraAlignedMesh(
            Point3d  center,
            Vector3d camRight,
            Vector3d camUp,
            double   width,
            double   height)
        {
            var mesh = new Mesh();

            mesh.Vertices.Add(center - camRight * width / 2 - camUp * height / 2); // bl
            mesh.Vertices.Add(center + camRight * width / 2 - camUp * height / 2); // br
            mesh.Vertices.Add(center + camRight * width / 2 + camUp * height / 2); // tr
            mesh.Vertices.Add(center - camRight * width / 2 + camUp * height / 2); // tl

            mesh.TextureCoordinates.Add(0, 0);
            mesh.TextureCoordinates.Add(1, 0);
            mesh.TextureCoordinates.Add(1, 1);
            mesh.TextureCoordinates.Add(0, 1);

            mesh.Faces.AddFace(0, 1, 2, 3);
            mesh.Normals.ComputeNormals();
            mesh.Compact();

            return mesh;
        }

        // ------------------------------------------------------------------ //
        //  Document material creation (for baked/extracted planes)
        // ------------------------------------------------------------------ //

        /// <summary>
        /// Adds a self-illuminated material with the photo as diffuse (and alpha for PNG)
        /// to the document and returns its index.
        /// </summary>
        public static int AddPhotoMaterial(RhinoDoc doc, string imagePath, string name)
        {
            var mat = new Material
            {
                Name          = name,
                DiffuseColor  = Color.White,
                EmissionColor = Color.White,
                Reflectivity  = 0,
                Shine         = 0,
                Transparency  = 0
            };

            mat.SetBitmapTexture(imagePath);

            if (Path.GetExtension(imagePath).ToLowerInvariant() == ".png")
                mat.SetTransparencyTexture(imagePath);

            return doc.Materials.Add(mat);
        }
    }
}
Let me continue where we left off. I need to:

Update PhotoPlaneConduit to use PicturePlaneManager helpers (remove private duplicates)
Create Commands/ExtractPhotoPlaneCommand.cs
Let me start with these tasks.

Let me continue with the tasks. I need to:

Update PhotoPlaneConduit to use PicturePlaneManager helpers (remove private duplicates)
Create Commands/ExtractPhotoPlaneCommand.cs
Update Todos

Update PhotoPlaneConduit to use PicturePlaneManager helpers

Create Commands/ExtractPhotoPlaneCommand.cs
Edit c:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\PhotoPlaneConduit.cs
Removed 59 lines
Now replace the private method calls with PicturePlaneManager.*:

Edit c:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\PhotoPlaneConduit.cs
Modified
Edit c:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\PhotoPlaneConduit.cs
Modified
Now remove the unused Vector3d using since ComputePlaneMesh is gone (actually Vector3d is still used indirectly through Rhino.Geometry, let me check):

Let me read the updated PhotoPlaneConduit.cs to verify it's correct, then create the ExtractPhotoPlaneCommand.cs.

Read c:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\PhotoPlaneConduit.cs
Rhino.Geometry is still needed by PointStyle (via Rhino.Display) — actually PointStyle is in Rhino.Display, but Mesh / vertices access still pulls from Rhino.Geometry. The file looks clean. Mark task 1 done and create the command.

Update Todos

Update PhotoPlaneConduit to use PicturePlaneManager helpers

Create Commands/ExtractPhotoPlaneCommand.cs
Write c:\SRC\RHINO\VIEW\RhinoPhotoMatch\Commands\ExtractPhotoPlaneCommand.cs
89 lines
using Rhino;
using Rhino.Commands;
using Rhino.DocObjects;
using RhinoPhotoMatch.Core;

namespace RhinoPhotoMatch.Commands
{
    /// <summary>
    /// PMExtractPhotoPlane — bakes the currently-virtual photo plane into a real
    /// Rhino mesh object with the photo applied as a document material.
    /// The mesh is positioned at the linked camera's current world-space location.
    /// </summary>
    public class ExtractPhotoPlaneCommand : Rhino.Commands.Command
    {
        public override string EnglishName => "PMExtractPhotoPlane";

        protected override Result RunCommand(RhinoDoc doc, RunMode mode)
        {
            var plugin   = RhinoPhotoMatchPlugin.Instance;
            var registry = plugin.Registry;

            if (registry.Pairs.Count == 0)
            {
                RhinoApp.WriteLine("PMExtractPhotoPlane: no photo planes in the registry.");
                return Result.Failure;
            }

            // Let the user pick which plane to extract
            var names = new System.Collections.Generic.List<string>();
            foreach (var p in registry.Pairs) names.Add(p.Name);

            string pick = names[0];
            if (names.Count > 1)
            {
                var result = Rhino.Input.RhinoGet.GetString(
                    $"Photo plane to extract ({string.Join(", ", names)})",
                    false, ref pick);
                if (result != Result.Success) return result;
            }

            var pair = registry.FindByName(pick);
            if (pair == null)
            {
                RhinoApp.WriteLine($"PMExtractPhotoPlane: no photo plane named \"{pick}\".");
                return Result.Failure;
            }

            // Find the linked viewport
            var vp = PicturePlaneManager.FindViewport(doc, pair.ActiveViewportId);
            if (vp == null)
            {
                RhinoApp.WriteLine("PMExtractPhotoPlane: the linked viewport is no longer open.");
                return Result.Failure;
            }

            // Build the mesh at the camera's current world-space position
            var mesh = PicturePlaneManager.ComputePlaneMesh(vp, pair);
            if (mesh == null)
            {
                RhinoApp.WriteLine("PMExtractPhotoPlane: could not compute plane mesh (degenerate camera?).");
                return Result.Failure;
            }

            // Add photo material to the document
            int matIndex = PicturePlaneManager.AddPhotoMaterial(doc, pair.ImagePath, pair.Name + "_mat");

            // Set up object attributes
            var attr = new ObjectAttributes
            {
                Name            = pair.Name + "_extracted",
                MaterialIndex   = matIndex,
                MaterialSource  = ObjectMaterialSource.MaterialFromObject
            };

            // Add mesh to document
            var objId = doc.Objects.AddMesh(mesh, attr);
            if (objId == System.Guid.Empty)
            {
                RhinoApp.WriteLine("PMExtractPhotoPlane: failed to add mesh to document.");
                return Result.Failure;
            }

            doc.Views.Redraw();
            RhinoApp.WriteLine($"PMExtractPhotoPlane: extracted \"{pair.Name}\" → mesh object with material.");
            return Result.Success;
        }
    }
}
Update Todos

Update PhotoPlaneConduit to use PicturePlaneManager helpers

Create Commands/ExtractPhotoPlaneCommand.cs
Done. Here's what changed:

PhotoPlaneConduit.cs — removed the private FindViewport and ComputePlaneMesh duplicates; both PreDrawObjects and PostDrawObjects now call PicturePlaneManager.FindViewport / PicturePlaneManager.ComputePlaneMesh.

ExtractPhotoPlaneCommand.cs — new PMExtractPhotoPlane command:

Lists registered pairs; if >1, prompts the user to pick by name
Finds the linked viewport's current camera
Calls PicturePlaneManager.ComputePlaneMesh to get world-space geometry at the current camera position
Calls PicturePlaneManager.AddPhotoMaterial to add a self-illuminated document material with the photo
Adds the mesh to the document with ObjectMaterialSource.MaterialFromObject so it renders with the photo in Shaded/Rendered mode
Build and run PMExtractPhotoPlane in Rhino — you should see a baked mesh appear in the layers panel with the photo material applied.

the image is black and white and a bit transparent, where is that coded?
The user is seeing the image plane displayed as black and white and a bit transparent. Let me look at where the display material is created in the conduit's GetMaterial method and in PicturePlaneManager.AddPhotoMaterial.

The issue is likely in GetMaterial in PhotoPlaneConduit.cs. Let me look at how DisplayMaterial is constructed and what properties affect the appearance.

In GetMaterial:


var docMat = new Rhino.DocObjects.Material
{
    Name          = pair.Name,
    DiffuseColor  = Color.White,
    EmissionColor = Color.White,


Setting material properties...

The problem might be that when I create a DisplayMaterial from a Rhino.DocObjects.Material, the bitmap texture isn't being transferred properly, which could explain why the image appears as a plain white or black surface instead of showing the actual photo. more carefully - the DisplayMaterial constructor that takes a Material should carry over the texture. But the black-and-white appearance might be because the display mode is rendering without colors, or because the DisplayMaterial is being used with transparency.

Actually, let me think about what DrawMeshShaded does with a DisplayMaterial. The DisplayMaterial class has properties like BackfaceColor, IsTwoSided, Transparency, etc.

When constructing new DisplayMaterial(docMat), the resulting material might have transparency set or the texture might not be mapped correctly for display purposes.

One common issue is that DisplayMaterial in Rhino's conduit works differently from document materials. For the conduit to show a full-color photo, you might need to directly set displayMat.Texture or similar.

I'm wondering if the issue is that DrawMeshShaded doesn't render textures in conduits the same way - it might need the texture explicitly assigned to the DisplayMaterial, or the material's color properties like Diffuse need to be set directly instead of relying on the texture alone. Looking at the RhinoCommon documentation, I see that DisplayMaterial has a Texture property that accepts a DisplayBitmap, so I need to load the image file and assign it through either a SetTexture method or by passing a Material to the constructor—let me verify which API is actually available.

Now I'm reconsidering the black and white appearance issue. If the texture is rendering but showing as black and white, it could mean the image itself isn't loading correctly or the UV coordinates are misaligned. Looking at the mesh construction, the UV mapping appears correct with coordinates spanning from (0,0) to (1,1) and the quad face indices in the right order, so the issue might be elsewhere.

The real problem could be that DisplayMaterial created from a Material with a bitmap texture isn't properly transferring the color information, or the rendering mode matters—wireframe won't show textures at all, shaded mode might show them partially, and rendered mode should display them fully. For the transparency aspect, a white fallback material shouldn't be transparent by default, so that's puzzling too.

I think the better approach is to load the image directly as a DisplayBitmap and configure the DisplayMaterial from scratch, setting the diffuse and emission colors to white, disabling transparency, enabling two-sided rendering, and then attaching the loaded texture to it.

Setting up the material properties... I'm realizing the texture might actually be loading but displaying in grayscale, which suggests the issue could be with how the material is being applied or rendered rather than the texture loading itself. Let me reconsider whether DrawMeshShaded with a DisplayMaterial created from a textured Material is the right approach, or if there's something else going on with the color information not being preserved. the issue be the image path? If the path has special characters, or the texture isn't loading, you'd get a default grey material.

Another thought: is there a chance the texture coordinates are wrong? The UV mapping has Y flipped in OpenGL vs texture coordinates...

Actually, let me look at this from a different angle. The TextureCoordinates in BuildCameraAlignedMesh:


(0, 0) - bl vertex
(1, 0) - br vertex
(1, 1) - tr vertex
(0, 1) - tl vertex
In Rhino's coordinate system, UV (0,0) is typically bottom-left of texture. This should be correct.

For the "black and white" issue - I think the root problem might be that DrawMeshShaded doesn't render textures with color when there's no proper lighting setup in the display pipeline. The EmissionColor = Color.White should make it self-illuminated but maybe DisplayMaterial needs different configuration.

Actually, I'm wondering if the real issue is that when DisplayMaterial is constructed from a transient Material that hasn't been added to the document, the texture path might not resolve correctly. Rhino's texture loading typically requires the material to be registered in the document or the texture to be loaded explicitly. Looking at the GetMaterial code, it's setting up a bitmap texture on the material...

Still writing material setup... I think the transparency might be carrying over from the Material when the DisplayMaterial is constructed, so I should explicitly set displayMat.Transparency = 0.0 to ensure it's fully opaque. For the black and white issue, I'm wondering if the Diffuse color isn't being set properly—I should try explicitly setting it to white along with the emission, and I suspect the transparency texture from the PNG might be interfering with how the color texture displays in the conduit.

The real issue might be that DrawMeshShaded in the conduit only uses the diffuse color and ignores textures unless we're in Rendered display mode, so if the user is in Shaded mode, the texture won't show up properly at all.

Wait—the user said the image appears black and white and slightly transparent, which means the texture is actually rendering somehow, just desaturated. This could indicate either the texture is being rendered in a luminosity-only mode that strips the color information, or the material has blend settings causing the desaturation.

For the transparency issue, I should set displayMat.Transparency = 0.0 after creating the DisplayMaterial to ensure it's fully opaque. But the real problem might be that DrawMeshShaded in the conduit is applying the display material's texture in a way that's stripping color—possibly because the display mode itself isn't configured to show full color textures, or the material's rendering properties are interfering.

I'm thinking the solution might be to use a different drawing approach altogether, like directly rendering the textured mesh in a way that bypasses the display mode's shading limitations. Use e.Display.DrawMeshFalseColors(mesh) with vertex colors set from the image, or use DrawBitmap in screen space, though that's harder to align properly in world coordinates.

Actually, the simplest approach is probably just adjusting the DisplayMaterial properties when creating it: set Diffuse to white so the texture displays at full brightness, enable Emission for self-illumination to bypass lighting effects, and set Transparency to zero to remove the transparency issue.

But I'm second-guessing myself on how Emission interacts with textures in Rhino's rendering pipeline. Let me reconsider what's actually happening here—the user's seeing black and white with some transparency, which suggests the display material isn't rendering the texture correctly, or the transparency setting is interfering with how the mesh is being composited. the texture as a System.Drawing.Bitmap and... hmm, how do you set a bitmap on DisplayMaterial?

Actually, checking RhinoCommon docs: DisplayMaterial has a SetTexture method that takes a Rhino.DocObjects.Texture object, so I'd need to wrap the bitmap in a Texture first. I'm not entirely certain this approach will work, but it's worth exploring.

Wait, I think the real issue might be simpler—after creating a DisplayMaterial from the document material, the transparency and color might not render as expected. I should explicitly set the transparency to ensure it's fully opaque, and if the texture is showing up in black and white, it's likely because the display pipeline is interpreting it as a grayscale displacement map rather than a color diffuse texture. The SetBitmapTexture method should handle the texture type correctly, but I need to verify that's actually being applied.

Let me step back and focus on the two most likely culprits: the transparency inheritance from the source material, and whether the shaded rendering mode is actually applying the diffuse texture or just using the material's base color with lighting calculations. So the texture only shows in Rendered mode, not Shaded mode—which explains why the user sees white or desaturated colors depending on the display setting. The black and white appearance suggests the texture is rendering but without its color information, likely due to how the DisplayMaterial handles textures in Shaded mode. I should look at the GetMaterial method in PhotoPlaneConduit.cs to find where the transparency and material properties are being set, since that's probably where the issue originates—either the transparency value is too high or the specular settings are zeroed out. I see the issue now—setting EmissionColor to white is causing the texture to wash out. For a photo reference plane, we need self-illumination so lights don't affect it, but the emission color should match the diffuse color or be black to let the texture show through properly. The real question is whether the material setup in GetMaterial is applying the texture correctly, or if the display mode itself is stripping the color information. I'm wondering if the grayscale issue stems from the Diffuse property being set to gray instead of white when the DisplayMaterial is constructed from the document material. For the transparency, I suspect it's coming from either a default transparency value or possibly the emission color interfering with how the texture renders. Let me focus on giving the user the specific fix: explicitly set Transparency = 0.0 and ensure EmissionColor isn't causing issues with the texture display.

But I should also consider what display mode they're in—if textures are disabled in Shaded mode, they'd only see the diffuse color as white. If textures are enabled but appearing in black and white, that could indicate a channel issue where only the alpha or luminance channel is being rendered instead of the full color data. The real culprit might be that SetTransparencyTexture is being called on PNG files, using the same image as a transparency mask, which would strip out the color information. I need to trace through the actual code path in PhotoPlaneConduit.cs where the material gets created, and I'm suspecting the DisplayMaterial constructor isn't preserving the transparency setting properly—I should explicitly set it. For the grayscale issue, DrawMeshShaded might be rendering textures without color in Shaded mode, so switching to Rendered mode or a different drawing method could fix it. But I'm also wondering if the EmissionColor is washing out the texture by adding white on top of it.

Let me reconsider this differently—maybe DisplayMaterial isn't loading or displaying the texture correctly at all. I could try loading the image directly as a bitmap and using DrawMeshTextured if that method exists, or I could sample the image pixels to set vertex colors instead. The quickest thing to test would be removing the EmissionColor and relying on DiffuseColor with the texture to see if that's what's interfering.

Actually, stepping back—the user described the result as "black and white and a bit transparent," so maybe the issue is something else entirely about how the image itself is being interpreted. The issue likely stems from the DisplayMaterial not properly inheriting the transparency setting from the original material, combined with how the display pipeline renders the mesh—I should explicitly set Transparency = 0.0 on the display material and verify the display mode has texture rendering enabled. The culprit is in the GetMaterial method around line 94-117 of PhotoPlaneConduit.cs. Transparency being "coded" - it's in GetMaterial at the Transparency = 0 line. The fact that it's transparent despite Transparency = 0 suggests the DisplayMaterial(docMat) constructor might not be picking it up correctly, so I should explicitly set displayMat.Transparency = 0.0. For the B&W issue, it could be that the image needs Shading mode with textures enabled, or it might be how DrawMeshShaded behaves without proper lighting setup in the conduit.

The display material settings are in GetMaterial in PhotoPlaneConduit.cs:94-117.

Two likely causes:

Transparency — DisplayMaterial(docMat) doesn't always carry Transparency = 0 from the source Material. Need to set it explicitly on the DisplayMaterial after construction.

Black and white — DrawMeshShaded in a conduit can render the texture as a luminance/grayscale layer depending on the display mode. Setting IsTwoSided = true and ensuring Emission isn't washing things out helps. More reliably: use Diffuse = Color.White with no emission override so the texture color comes through unmuted.

Here's the fix:

Edit c:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\PhotoPlaneConduit.cs
Modified
Key changes:

EmissionColor = Color.Black — white emission was adding a constant white overlay on top of the texture, washing out/desaturating color
displayMat.Transparency = 0.0 — explicitly set after construction since the DisplayMaterial(Material) constructor doesn't reliably carry Transparency = 0 over
Removed SetTransparencyTexture — using the photo itself as a transparency mask was making dark pixels transparent and contributing to the partial-transparency effect
Also make sure the viewport is in Shaded or Rendered display mode (not Wireframe) — DrawMeshShaded won't show textures in Wireframe.

what have we implemented so far?
The user wants to know what's been implemented so far. Let me read the spec to compare against what we've built.

Read c:\SRC\RHINO\VIEW\Specefication\RhinoPhotoMatch_Spec.md
Globpattern: "**/*.cs"
Found 14 files
Here's where we stand against the spec:

Implemented
Plugin Scaffold

Project structure, .rhp build, post-build deploy to Rhino, debug launch settings — complete
Photo Import & Picture Plane (spec section 2)

PMImportPhoto / PMAddPhotoPlane — file picker, aspect ratio preserved, name prompt, auto-naming (PM_Photo_01, PM_Photo_02…)
PhotoPlaneRegistry + PhotoPlanePair — in-memory registry of pairs
PhotoPlaneConduit — DisplayConduit that draws the photo as a background in the linked viewport, and as a textured 3D plane + frustum pyramid in all other viewports
Camera-linked planes — plane always follows the live camera (recomputed every frame from viewport frustum)
PMScalePhotoPlane — adjust Distance and Scale per pair
PMRemovePhotoPlane — removes pair, warns before deleting
PMRelinkPhoto — re-points texture to a new file, refreshes display
PicturePlaneManager — shared helpers for mesh computation and material creation
Extra (beyond spec)

PMExtractPhotoPlane — bakes the virtual conduit plane into a real Rhino mesh object with photo material
Not yet started
Spec section	Key commands
Camera Calibration — Reference Points (PnP)	PMSetReferencePoints, PMCalibrate (OpenCV SolvePnP), ViewportSync
Camera Calibration — Vanishing Points	PMSetVanishingLines, vanishing point solver
Overlay & Alignment Feedback	OverlayConduit, PMToggleOverlay
View Lock	PMLockView
Session Save & Restore	PMSaveSession, PMLoadSession, auto-save on doc events
Dockable Panel UI	PhotoMatchPanel (Eto.Forms), ReferencePointPicker
The biggest remaining piece is the calibration pipeline — that's the core CV work using OpenCvSharp + Math.NET.

I dont see the need to create a view when doing the command  "PMAddPhotoPlane"
The user is saying they don't want a named view to be created when running PMAddPhotoPlane. Let me look at the PhotoPlaneRegistry.cs to see where the named view is created.

Read c:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\PhotoPlaneRegistry.cs
The named view is created at PhotoPlaneRegistry.cs:47. The pair also stores activeVp.Id directly — that's the active viewport ID, not the named view. So the named view isn't actually used for anything functional right now; we just need the viewport ID.

I'll remove the named view creation and the cleanup on remove:

Edit c:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\PhotoPlaneRegistry.cs
Removed 3 lines
Edit c:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\PhotoPlaneRegistry.cs
Removed 4 lines
Now let me check if NamedViewName is still referenced anywhere else:

Grep "NamedViewName" (in c:\SRC\RHINO\VIEW\RhinoPhotoMatch, glob: *.cs)
1 line of output
Only defined in PhotoPlanePair, nowhere else is using it. Let me remove it:

Read c:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\PhotoPlanePair.cs
Edit c:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\PhotoPlanePair.cs
Removed 3 lines
Also update the summary comment on Name in PhotoPlanePair:

Edit c:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\PhotoPlanePair.cs
Modified
Done. No named views are created or deleted anywhere now. The pair just stores the viewport ID directly.

lets continue building
The user wants to continue building. Looking at the spec, the next major feature is Camera Calibration — Reference Points (PnP). This involves:

PMSetReferencePoints - pick 3D points in viewport + matching 2D positions on photo
PMCalibrate - pass pairs to OpenCV SolvePnP, solve camera pose
ViewportSync - convert OpenCV extrinsics to Rhino camera
This is the core CV work. Let me think about what to build next in a logical order.

The calibration pipeline requires:

A way to pick 3D points in Rhino and corresponding 2D points in the photo

The calibration solver using OpenCvSharp

Applying the result back to the viewport

I'll tackle this in stages: first the PMSetReferencePoints command for iterative point picking, then the CalibrationSolver with the actual math, followed by ViewportSync to apply the camera transformation, and finally the PMCalibrate command to tie everything together.

For the reference point picking workflow, the user picks a 3D point on the model, then clicks the corresponding location on the photo plane in the linked viewport—I'll project that 3D click back to 2D image coordinates and store the pairs. The display conduit will show numbered markers for each reference point as they're collected. I need to map the 3D click point back to UV coordinates on the photo plane using its geometry, then convert those to pixel positions. For the calibration solver, I'll estimate camera intrinsics from the image dimensions and field of view, use SolvePnP to get the rotation and translation, and convert those to Rhino camera parameters. I'm going to build this in stages: first the OpenCV wrapper for the PnP solver, then the coordinate conversion utilities, then the command to set reference points. I'm setting up the CalibrationSolver to extract camera intrinsics from the image dimensions and FOV, then use SolvePnP to compute the camera pose. For ViewportSync, I need to convert the OpenCV camera matrix (where X points right, Y points down, and Z points forward) into Rhino's coordinate system (X right, Y up, Z backward) by transforming the rotation and translation appropriately.

For the reference points workflow, I'm thinking through how to let users pick 3D points in the viewport and match them to locations on the photo. Since the photo plane only exists as a conduit draw with no actual geometry to click on, I can use a plane constraint with RhinoGet.GetPoint to snap clicks to the photo plane, or compute the intersection of the mouse ray with the plane directly.

Once I have the 3D click position, I'll project it onto the plane's UV coordinates and convert those to pixel positions in the image. This gives me the correspondence between world points and image points needed for the PnP calibration. I'll keep the reference point markers in a separate conduit for clarity.

Now I'm implementing the core modules: the calibration solver, viewport synchronization, and the commands for picking reference points and running the calibration. The tricky part is handling 2D point picking on the photo—since the photo is just a background in the viewport with no actual geometry to snap to, I'll construct a plane at the photo's distance and constrain the point picking to that plane so users can click directly on the image. Now I'm mapping those normalized coordinates to pixel space by multiplying u and v by the image dimensions, with a Y-flip to account for OpenCV's top-down convention. I need to add PixelWidth and PixelHeight fields to PhotoPlanePair so the conversion has the actual image dimensions available.

For the plane dimensions themselves, I realize they're dynamic as the camera moves, but I can query PicturePlaneManager.ComputePlaneMesh at the moment of the click to get the current geometry, or add a dedicated method that returns just the plane dimensions.

Now I'm writing the actual implementation—starting with the CalibrationSolver wrapper for OpenCV's SolvePnP, then the ViewportSync logic to convert between OpenCV poses and Rhino camera coordinates. There's a critical detail though: when the user picks reference points in the linked viewport, I need to make sure RhinoGet.GetPoint is actually capturing clicks from the correct viewport, since the command might be running in a different context. I'll implement:

Prompt user to pick a 3D point on the model (any viewport)
Prompt user to activate the photo plane's linked viewport, then click the corresponding spot on the photo
Internally: get the 3D click position → project onto photo plane → compute UV → compute pixel coords
For the reference point display conduit, I'll keep it simple - just draw spheres at the 3D points. The 2D points don't need visualization since they're on the photo background.

Let me write all 4 files now.

Actually wait - for SetReferencePointsCommand, picking 2D points on the photo is tricky because in PreDrawObjects the photo is drawn as a background, but in PostDrawObjects it's drawn as a 3D plane. The user's linked viewport should show the photo as background.

When the user clicks in the linked viewport where the photo is background, RhinoGet.GetPoint will try to intersect with existing geometry, which is problematic if there's nothing there. Instead of using RhinoGet.GetPoint directly, I can constrain it to the photo plane itself using GetPoint.Constrain(Plane plane) - I'll compute the photo plane from the current camera state and force any click to project onto it.

Setting up the plane...

Now I'm configuring the GetPoint tool to constrain input to that plane, then converting the clicked point back to image coordinates by projecting it onto the camera's right and up vectors, accounting for the image dimensions and pixel space.

I need to gather the plane dimensions, so I'll add a helper method to compute the plane's frame with its center, axes, and size. Then I'll update the PhotoPlanePair class to store pixel dimensions, modify the registry to pass those values through, and add the computation method to PicturePlaneManager before writing out the command and solver logic.

Writing CalibrationSolver... Still writing the calibration function...

Now I need to handle the coordinate system conversion between OpenCV and Rhino—OpenCV uses X right, Y down, Z forward, while Rhino uses X right, Y up, Z outward with a right-hand rule, so I'll need to transform the camera pose accordingly. T * t (in OpenCV world coords, then convert to Rhino)

The conversion:

Get rotation matrix from rvec: Cv2.Rodrigues(rvec, R)
Camera position in world: cameraPos = -R.T * t
Camera direction (in OpenCV world): R.T * [0, 0, 1] (forward direction)
Camera up (in OpenCV world): R.T * [0, -1, 0] (Y-up in camera = -Y in OpenCV)
Actually, the world coordinate system depends on what we pass in. If we're providing 3D points in Rhino coordinates (Y-up), then R and t relate the OpenCV camera frame to that Rhino world frame. The camera position is still -R^T * t, but I need to be careful about the axis directions—OpenCV's +Z is forward and -Y is up in camera space, which maps to Rhino's coordinate conventions.

Let me reconsider the camera axes more carefully. In OpenCV, the camera frame has +X right, +Y down, and +Z forward. When I transform to world space using R^T, I get the camera's orientation relative to the world. Since the input points are already in Rhino's Y-up space, the camera forward direction is R^T * [0, 0, 1] and the camera up direction is R^T * [0, -1, 0]—no additional conversion needed because the world frame is already Rhino's Y-up.

The key thing I need to verify is the camera position calculation: pos = -R^T * t, where t is the translation from SolvePnP. Once I have that, I can directly pass these vectors to the Rhino camera functions.

For the field of view, I can compute the horizontal FOV from the camera matrix using the focal length: fovH = 2 * atan(imageWidth / (2 * fx)) in radians.

Now I'm setting up the actual implementation with OpenCvSharp4, using the SolvePnP function with double arrays to handle the matrix operations cleanly.

Looking at the existing plugin structure, I see that reference point pairs are already being stored in a list on the plugin class, so I just need to add visualization and picking logic. Rather than creating a separate conduit, I'll draw the reference point markers directly in the existing PhotoPlaneConduit's PostDrawObjects method to keep things simple. I've decided to create a separate ReferencePointConduit for clarity rather than coupling it to the existing photo plane conduit. Now I'm writing out the core C# implementation, starting with the calibration solver that will handle the perspective-n-point problem...

Building the solver implementation... Setting up the camera matrix and converting the world and image points to OpenCV types, then using the SolvePnP algorithm to calculate the rotation and translation vectors that describe the camera's pose relative to the world coordinates.

Now I'm extracting the rotation matrix elements and computing the camera's position and orientation in world space by applying the inverse transformation to get the camera's actual location and direction vectors. Extracting the camera direction and up vectors from the rotation matrix, then computing the reprojection error by projecting the 3D points and comparing them to the image points. I'm checking the Cv2.ProjectPoints signature in OpenCvSharp to make sure the output parameter works correctly. Now I'm moving on to the ViewportSync class, which applies the calibration result to a Rhino viewport by finding the target view and updating its camera settings.

I need to convert the field of view to a 35mm lens length using the standard 36mm film width, so the calculation divides half the film width by the tangent of half the horizontal FOV.

For the reference point command, the workflow involves having the user select a 3D point first, then click the corresponding location in the linked photo viewport—I'll need to set up a constrained point picker on the photo plane and convert the resulting 3D intersection back to image pixel coordinates.

I'm adding a PlaneFrame struct to capture the photo plane's geometry (center, camera-relative axes, and dimensions) so I can properly set up the plane constraint for picking. I also need to ensure PhotoPlanePair stores the actual pixel dimensions of the image.

For the calibration command, I need to handle FOV estimation—either accepting a user-provided value or deriving it from the current viewport settings.

When implementing SetReferencePointsCommand, the key challenge is constraining point picks to the photo plane itself. I'll compute the plane geometry and use RhinoGet.GetPoint with a plane constraint to ensure the user can click directly on the photo background without snapping to other geometry.

Writing plane geometry setup...

Now I'm planning out the full implementation—I need to create a few new files for the plane frame struct, calibration solver wrapping OpenCV's PnP algorithm, viewport syncing, and two command classes for interactive picking and running the solver. I'll also need to update the existing photo plane pair class to include pixel dimensions.

Actually, I'm reconsidering whether to create a separate file for PlaneFrame or just nest it in PicturePlaneManager to keep things cleaner. I'll inline it there instead.

Let me think through the actual complexity here. The reference point picking workflow in Rhino is inherently involved, so I need to be clear about what's essential. For the set reference points command, I'm looking at a loop where the user picks 3D points on the model one at a time, and I need to handle that interaction cleanly.

The tricky part is step 4 where the user needs to click on the photo in a linked viewport. In Rhino, GetPoint always uses the active viewport, so I either ask the user to switch viewports manually or I can set it programmatically using doc.Views.ActiveView. I'll go with setting it programmatically since that's smoother, but I need to make sure I'm getting the actual RhinoView object to set as active.

Now I'm ready to start writing the code. But first I need to update the PhotoPlanePair constructor to include PixelWidth and PixelHeight since those are essential for the image coordinates. I also need to think through the SetReferencePointsCommand—it should let the user pick which pair they're calibrating, or default to the only pair if there's just one. Reference points definitely need to be stored per-pair since they're tied to specific 2D image coordinates.

Updating PhotoPlanePair... I need to verify the exact Rhino viewport API for camera manipulation since I'm not entirely certain about all the methods—some properties might be read-only or have different signatures than I initially thought. I'm exploring whether ViewportInfo can be used to set all three camera parameters at once, then apply it back to the viewport using SetViewInfo. The API for applying the modified viewport info isn't immediately clear to me—it might be on RhinoView rather than RhinoViewport, or there could be a different method entirely. I'm also considering using named viewports as an alternative approach. Setting up the camera with location, direction, and lens length, then handling the up vector through ViewportInfo to ensure proper camera orientation when tilted. I'm checking the RhinoCommon API to figure out the right methods for setting camera properties on ViewportInfo—it looks like SetCameraLocation and SetCameraDirection are available, though I need to verify the exact method signatures and whether CameraUp is directly settable or requires a different approach like SetCameraAngles. Setting camera properties in RhinoViewport...

I see that CameraUp is read-only, so I'll need to work around that constraint. The viewport's camera direction and target can be set through the existing methods, and I can adjust the lens length separately. For the up vector, Rhino constrains it based on the camera direction unless it's vertical, so I may need to use a different approach or accept that limitation depending on what the API provides.

Let me focus on implementing location and direction first, then revisit the up vector if needed. I could explore SetCameraAngles with heading and pitch, but for now I'll keep it simple and let the user fine-tune from there.

For the reference points command, I'm thinking through how to map the user's click on the photo to image coordinates. The photo fills the viewport with letterboxing, so screen coordinates in the linked viewport should map directly to image space.

I'll go with the 3D approach—constraining the click to the photo plane and back-projecting to UV coordinates—since it handles letterboxing correctly. I need to add a ComputePlaneFrame method to PicturePlaneManager to get the plane dimensions for the back-projection calculation.

Now I'm ready to implement. I'll add pixel dimensions and reference pairs to PhotoPlanePair, update PhotoPlaneRegistry to pass those values, and create the plane frame computation method.

Next I need to build out the calibration pipeline: a solver using OpenCV's PnP algorithm, a viewport sync to apply results, an interactive command for picking reference points, and a calibration command that ties it together. I'll also clean up the plugin to store reference pairs per-pair instead of globally.

Update Todos

Add PixelWidth/PixelHeight/ReferencePairs to PhotoPlanePair + registry

Add ComputePlaneFrame helper to PicturePlaneManager

Create Core/CalibrationSolver.cs (OpenCV PnP wrapper)

Create Core/ViewportSync.cs

Create Commands/SetReferencePointsCommand.cs

Create Commands/CalibrateCommand.cs
Edit c:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\PhotoPlanePair.cs
Added 12 lines
Now update PhotoPlaneRegistry.CreatePair to pass pixel dims:

Edit c:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\PhotoPlaneRegistry.cs
Modified
Also remove global ReferencePairs from the plugin since they're now per-pair:

Read c:\SRC\RHINO\VIEW\RhinoPhotoMatch\RhinoPhotoMatchPlugin.cs
Edit c:\SRC\RHINO\VIEW\RhinoPhotoMatch\RhinoPhotoMatchPlugin.cs
Removed 2 lines
Edit c:\SRC\RHINO\VIEW\RhinoPhotoMatch\RhinoPhotoMatchPlugin.cs
Removed 5 lines
Update Todos

Add PixelWidth/PixelHeight/ReferencePairs to PhotoPlanePair + registry

Add ComputePlaneFrame helper to PicturePlaneManager

Create Core/CalibrationSolver.cs (OpenCV PnP wrapper)

Create Core/ViewportSync.cs

Create Commands/SetReferencePointsCommand.cs

Create Commands/CalibrateCommand.cs
Now add ComputePlaneFrame to PicturePlaneManager:

Edit c:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\PicturePlaneManager.cs
Edit failed
Read c:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\PicturePlaneManager.cs
Edit c:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\PicturePlaneManager.cs
Added 42 lines
Update Todos

Add PixelWidth/PixelHeight/ReferencePairs to PhotoPlanePair + registry

Add ComputePlaneFrame helper to PicturePlaneManager

Create Core/CalibrationSolver.cs (OpenCV PnP wrapper)

Create Core/ViewportSync.cs

Create Commands/SetReferencePointsCommand.cs

Create Commands/CalibrateCommand.cs
Write c:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\CalibrationSolver.cs
133 lines
using System;
using System.Collections.Generic;
using OpenCvSharp;
using Rhino.Geometry;

namespace RhinoPhotoMatch.Core
{
    /// <summary>
    /// Solved camera pose returned by <see cref="CalibrationSolver.SolvePnP"/>.
    /// All vectors are in Rhino world-space (Y-up, right-handed).
    /// </summary>
    public class CalibrationResult
    {
        public Point3d  CameraLocation    { get; set; }
        public Vector3d CameraDirection   { get; set; }
        public Vector3d CameraUp          { get; set; }
        /// <summary>Horizontal field of view in radians (same value used for calibration).</summary>
        public double   FovRadians        { get; set; }
        /// <summary>Mean reprojection error in pixels.</summary>
        public double   ReprojectionError { get; set; }
    }

    public static class CalibrationSolver
    {
        /// <summary>
        /// Runs OpenCV SolvePnP on the supplied point correspondences.
        /// World points must be in Rhino world-space (Y-up).
        /// Image points must be in pixel coordinates (origin top-left, Y down).
        /// </summary>
        /// <param name="worldPoints">3D model points.</param>
        /// <param name="imagePoints">Matching 2D image pixels (top-left origin).</param>
        /// <param name="imageWidth">Photo width in pixels.</param>
        /// <param name="imageHeight">Photo height in pixels.</param>
        /// <param name="horizontalFovDegrees">Horizontal field of view of the camera lens.</param>
        /// <returns>Solved pose, or null on failure.</returns>
        public static CalibrationResult? SolvePnP(
            IList<Point3d>  worldPoints,
            IList<Point2d>  imagePoints,
            int             imageWidth,
            int             imageHeight,
            double          horizontalFovDegrees)
        {
            if (worldPoints.Count < 4)
            {
                Rhino.RhinoApp.WriteLine("CalibrationSolver: need at least 4 reference pairs.");
                return null;
            }
            if (worldPoints.Count != imagePoints.Count)
            {
                Rhino.RhinoApp.WriteLine("CalibrationSolver: world and image point counts differ.");
                return null;
            }

            // ---- Camera intrinsics from horizontal FOV ----
            double fovRad = horizontalFovDegrees * Math.PI / 180.0;
            double fx = (imageWidth / 2.0) / Math.Tan(fovRad / 2.0);
            double cx = imageWidth  / 2.0;
            double cy = imageHeight / 2.0;

            double[,] camData = { { fx, 0, cx }, { 0, fx, cy }, { 0, 0, 1 } };
            using var cameraMatrix = Mat.FromArray(camData);
            using var distCoeffs   = Mat.Zeros(1, 4, MatType.CV_64FC1);

            // ---- Convert to OpenCV point types ----
            var objPts = new Point3f[worldPoints.Count];
            var imgPts = new Point2f[worldPoints.Count];
            for (int i = 0; i < worldPoints.Count; i++)
            {
                objPts[i] = new Point3f((float)worldPoints[i].X,
                                        (float)worldPoints[i].Y,
                                        (float)worldPoints[i].Z);
                imgPts[i] = new Point2f((float)imagePoints[i].X,
                                        (float)imagePoints[i].Y);
            }

            // ---- Solve ----
            var flags = worldPoints.Count >= 6 ? SolvePnPFlags.EPnP : SolvePnPFlags.Iterative;
            bool ok = Cv2.SolvePnP(objPts, imgPts, cameraMatrix, distCoeffs,
                                   out Mat rvec, out Mat tvec, false, flags);
            if (!ok)
            {
                Rhino.RhinoApp.WriteLine("CalibrationSolver: SolvePnP failed.");
                return null;
            }

            // ---- Rotation vector → matrix ----
            Cv2.Rodrigues(rvec, out Mat rmat);

            double r00 = rmat.At<double>(0, 0), r01 = rmat.At<double>(0, 1), r02 = rmat.At<double>(0, 2);
            double r10 = rmat.At<double>(1, 0), r11 = rmat.At<double>(1, 1), r12 = rmat.At<double>(1, 2);
            double r20 = rmat.At<double>(2, 0), r21 = rmat.At<double>(2, 1), r22 = rmat.At<double>(2, 2);
            double tx  = tvec.At<double>(0), ty = tvec.At<double>(1), tz = tvec.At<double>(2);

            // ---- Camera pose in world space ----
            // Camera position: -R^T * t
            var camLoc = new Point3d(
                -(r00 * tx + r10 * ty + r20 * tz),
                -(r01 * tx + r11 * ty + r21 * tz),
                -(r02 * tx + r12 * ty + r22 * tz));

            // Camera forward (OpenCV +Z axis) in world: third column of R^T = third row of R
            var camDir = new Vector3d(r02, r12, r22);

            // Camera up (OpenCV -Y axis in camera = up) in world: -second column of R^T = -second row of R
            var camUp = new Vector3d(-r01, -r11, -r21);

            camDir.Unitize();
            camUp.Unitize();

            // ---- Reprojection error ----
            Cv2.ProjectPoints(objPts, rvec, tvec, cameraMatrix, distCoeffs, out Point2f[] projected);
            double totalErr = 0;
            for (int i = 0; i < projected.Length; i++)
            {
                double dx = projected[i].X - imgPts[i].X;
                double dy = projected[i].Y - imgPts[i].Y;
                totalErr += Math.Sqrt(dx * dx + dy * dy);
            }

            rvec.Dispose(); tvec.Dispose(); rmat.Dispose();

            return new CalibrationResult
            {
                CameraLocation    = camLoc,
                CameraDirection   = camDir,
                CameraUp          = camUp,
                FovRadians        = fovRad,
                ReprojectionError = totalErr / projected.Length
            };
        }
    }
}
Update Todos

Add PixelWidth/PixelHeight/ReferencePairs to PhotoPlanePair + registry

Add ComputePlaneFrame helper to PicturePlaneManager

Create Core/CalibrationSolver.cs (OpenCV PnP wrapper)

Create Core/ViewportSync.cs

Create Commands/SetReferencePointsCommand.cs

Create Commands/CalibrateCommand.cs
Write c:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\ViewportSync.cs
69 lines
using System;
using Rhino;
using Rhino.Display;
using Rhino.Geometry;

namespace RhinoPhotoMatch.Core
{
    /// <summary>
    /// Applies a <see cref="CalibrationResult"/> to a Rhino viewport,
    /// setting camera location, direction, and FOV.
    /// </summary>
    public static class ViewportSync
    {
        /// <summary>
        /// Applies the solved camera pose to the viewport linked to <paramref name="pair"/>.
        /// Returns false if the viewport is no longer open.
        /// </summary>
        public static bool Apply(RhinoDoc doc, PhotoPlanePair pair, CalibrationResult result)
        {
            // Find the owning RhinoView (not just the viewport) so we can call Redraw
            RhinoView? targetView = null;
            foreach (var view in doc.Views)
            {
                if (view.ActiveViewport.Id == pair.ActiveViewportId)
                {
                    targetView = view;
                    break;
                }
            }
            if (targetView == null) return false;

            var vp = targetView.ActiveViewport;

            // Switch to perspective if not already
            if (!vp.IsPerspectiveProjection)
                vp.ChangeToPerspectiveProjection(true, 50.0);

            // Set camera location; keep target at current distance along new direction
            double dist = vp.CameraLocation.DistanceTo(vp.CameraTarget);
            vp.SetCameraLocation(result.CameraLocation, false);
            vp.SetCameraTarget(result.CameraLocation + result.CameraDirection * dist, false);

            // FOV → 35mm equivalent lens length  (standard 36mm film width)
            double lensLength = 18.0 / Math.Tan(result.FovRadians / 2.0);
            vp.Camera35mmLensLength = lensLength;

            targetView.Redraw();
            return true;
        }

        /// <summary>
        /// Converts horizontal FOV in degrees to a 35mm-equivalent lens length in mm.
        /// </summary>
        public static double FovToLensLength(double horizontalFovDegrees)
        {
            double fovRad = horizontalFovDegrees * Math.PI / 180.0;
            return 18.0 / Math.Tan(fovRad / 2.0);
        }

        /// <summary>
        /// Converts a 35mm-equivalent lens length in mm to horizontal FOV in degrees.
        /// </summary>
        public static double LensLengthToFov(double lensLengthMm)
        {
            return 2.0 * Math.Atan(18.0 / lensLengthMm) * (180.0 / Math.PI);
        }
    }
}
Update Todos

Add PixelWidth/PixelHeight/ReferencePairs to PhotoPlanePair + registry

Add ComputePlaneFrame helper to PicturePlaneManager

Create Core/CalibrationSolver.cs (OpenCV PnP wrapper)

Create Core/ViewportSync.cs

Create Commands/SetReferencePointsCommand.cs

Create Commands/CalibrateCommand.cs
Write c:\SRC\RHINO\VIEW\RhinoPhotoMatch\Commands\SetReferencePointsCommand.cs
172 lines
using Rhino;
using Rhino.Commands;
using Rhino.Display;
using Rhino.Geometry;
using Rhino.Input.Custom;
using RhinoPhotoMatch.Core;
using System.Collections.Generic;
using System.Drawing;

namespace RhinoPhotoMatch.Commands
{
    /// <summary>
    /// PMSetReferencePoints — interactively collects 3D model point ↔ 2D photo pixel
    /// correspondences for a chosen photo plane pair.
    ///
    /// Workflow per point:
    ///   1. Pick a 3D point on the model (any viewport).
    ///   2. Activate the photo viewport and click the matching spot on the photo.
    ///      The click is constrained to the photo plane so the 3D hit is
    ///      back-projected to UV / pixel coordinates automatically.
    ///   3. Repeat; press Enter (no point) to finish.
    /// </summary>
    public class SetReferencePointsCommand : Rhino.Commands.Command
    {
        public override string EnglishName => "PMSetReferencePoints";

        protected override Result RunCommand(RhinoDoc doc, RunMode mode)
        {
            var registry = RhinoPhotoMatchPlugin.Instance.Registry;

            if (registry.Pairs.Count == 0)
            {
                RhinoApp.WriteLine("PMSetReferencePoints: no photo planes in the registry.");
                return Result.Failure;
            }

            // Pick which pair to work with
            var pair = PickPair(registry);
            if (pair == null) return Result.Cancel;

            var linkedVp = PicturePlaneManager.FindViewport(doc, pair.ActiveViewportId);
            if (linkedVp == null)
            {
                RhinoApp.WriteLine("PMSetReferencePoints: linked viewport is no longer open.");
                return Result.Failure;
            }

            // Find the RhinoView for the linked viewport (needed to activate it)
            RhinoView? linkedView = null;
            foreach (var v in doc.Views)
                if (v.ActiveViewport.Id == pair.ActiveViewportId) { linkedView = v; break; }

            RhinoApp.WriteLine($"Collecting reference points for \"{pair.Name}\".");
            RhinoApp.WriteLine("Step 1: pick a 3D point on the model.  Step 2: click the matching spot on the photo.  Press Enter to finish.");

            // Draw conduit for markers
            var markerConduit = new ReferenceMarkerConduit(pair.ReferencePairs);
            markerConduit.Enabled = true;

            try
            {
                while (true)
                {
                    // ---- Step 1: 3D world point ----
                    var gp3d = new GetPoint();
                    gp3d.SetCommandPrompt($"Pick 3D point #{pair.ReferencePairs.Count + 1} on model (Enter to finish)");
                    gp3d.AcceptNothing(true);
                    gp3d.Get();

                    if (gp3d.CommandResult() == Result.Nothing) break;
                    if (gp3d.CommandResult() != Result.Success) break;

                    var worldPt = gp3d.Point();

                    // ---- Step 2: 2D photo point (click in linked viewport) ----
                    // Activate the linked viewport so the user sees the photo
                    if (linkedView != null)
                        doc.Views.ActiveView = linkedView;

                    // Compute the photo plane and constrain the pick to it
                    if (!PicturePlaneManager.ComputePlaneFrame(linkedVp, pair,
                            out Point3d center, out Vector3d camRight, out Vector3d camUp,
                            out double planeW, out double planeH))
                    {
                        RhinoApp.WriteLine("PMSetReferencePoints: could not compute photo plane.");
                        break;
                    }

                    var photoPlane = new Plane(center, camRight, camUp);

                    var gp2d = new GetPoint();
                    gp2d.SetCommandPrompt($"Click matching location on photo for point #{pair.ReferencePairs.Count + 1}");
                    gp2d.Constrain(photoPlane, false);
                    gp2d.Get();

                    if (gp2d.CommandResult() != Result.Success) break;

                    var hitPt = gp2d.Point();

                    // Back-project 3D hit → image UV → pixel coords
                    var local = hitPt - center;
                    double u = (Vector3d.Multiply(local, camRight) / (planeW / 2.0) + 1.0) / 2.0;
                    double v = (Vector3d.Multiply(local, camUp)    / (planeH / 2.0) + 1.0) / 2.0;

                    // Clamp to [0,1] in case the click landed slightly outside
                    u = System.Math.Max(0, System.Math.Min(1, u));
                    v = System.Math.Max(0, System.Math.Min(1, v));

                    // Image pixel coords: origin top-left, Y down (OpenCV convention)
                    double pixelX = u * pair.PixelWidth;
                    double pixelY = (1.0 - v) * pair.PixelHeight;

                    pair.ReferencePairs.Add((worldPt, new Point2d(pixelX, pixelY)));

                    RhinoApp.WriteLine($"  Pair {pair.ReferencePairs.Count}: world ({worldPt.X:F3}, {worldPt.Y:F3}, {worldPt.Z:F3})  →  image ({pixelX:F1}, {pixelY:F1}) px");
                    doc.Views.Redraw();
                }
            }
            finally
            {
                markerConduit.Enabled = false;
                doc.Views.Redraw();
            }

            RhinoApp.WriteLine($"PMSetReferencePoints: {pair.ReferencePairs.Count} pair(s) stored for \"{pair.Name}\".");
            return pair.ReferencePairs.Count > 0 ? Result.Success : Result.Cancel;
        }

        // ---- Helpers ----

        private static PhotoPlanePair? PickPair(PhotoPlaneRegistry registry)
        {
            if (registry.Pairs.Count == 1) return registry.Pairs[0];

            var names = new System.Collections.Generic.List<string>();
            foreach (var p in registry.Pairs) names.Add(p.Name);

            string pick = names[0];
            var res = Rhino.Input.RhinoGet.GetString(
                $"Photo plane to calibrate ({string.Join(", ", names)})", false, ref pick);
            if (res != Result.Success) return null;

            return registry.FindByName(pick);
        }
    }

    // ------------------------------------------------------------------ //
    //  Internal conduit — draws numbered spheres at collected world points
    // ------------------------------------------------------------------ //

    internal sealed class ReferenceMarkerConduit : DisplayConduit
    {
        private readonly IReadOnlyList<(Point3d WorldPoint, Point2d ImagePoint)> _pairs;

        public ReferenceMarkerConduit(
            IReadOnlyList<(Point3d WorldPoint, Point2d ImagePoint)> pairs)
        {
            _pairs = pairs;
        }

        protected override void PostDrawObjects(DrawEventArgs e)
        {
            for (int i = 0; i < _pairs.Count; i++)
            {
                var pt = _pairs[i].WorldPoint;
                e.Display.DrawPoint(pt, PointStyle.Circle, 8, Color.Yellow);
                e.Display.Draw2dText($"{i + 1}", Color.Yellow, pt, false, 14);
            }
        }
    }
}
Update Todos

Add PixelWidth/PixelHeight/ReferencePairs to PhotoPlanePair + registry

Add ComputePlaneFrame helper to PicturePlaneManager

Create Core/CalibrationSolver.cs (OpenCV PnP wrapper)

Create Core/ViewportSync.cs

Create Commands/SetReferencePointsCommand.cs

Create Commands/CalibrateCommand.cs
Write c:\SRC\RHINO\VIEW\RhinoPhotoMatch\Commands\CalibrateCommand.cs
104 lines
using Rhino;
using Rhino.Commands;
using Rhino.Input;
using RhinoPhotoMatch.Core;

namespace RhinoPhotoMatch.Commands
{
    /// <summary>
    /// PMCalibrate — solves the camera pose for a photo plane using the stored
    /// reference point pairs, then applies the result to the linked viewport.
    ///
    /// Requires at least 4 reference pairs (set with PMSetReferencePoints).
    /// Prompts the user for the camera's horizontal FOV if not already known.
    /// </summary>
    public class CalibrateCommand : Rhino.Commands.Command
    {
        public override string EnglishName => "PMCalibrate";

        protected override Result RunCommand(RhinoDoc doc, RunMode mode)
        {
            var registry = RhinoPhotoMatchPlugin.Instance.Registry;

            if (registry.Pairs.Count == 0)
            {
                RhinoApp.WriteLine("PMCalibrate: no photo planes in the registry.");
                return Result.Failure;
            }

            // Pick pair
            var pair = PickPair(registry);
            if (pair == null) return Result.Cancel;

            if (pair.ReferencePairs.Count < 4)
            {
                RhinoApp.WriteLine($"PMCalibrate: \"{pair.Name}\" has only {pair.ReferencePairs.Count} reference pair(s). Need at least 4. Run PMSetReferencePoints first.");
                return Result.Failure;
            }

            // Prompt for horizontal FOV — default to the current viewport's lens equivalent
            var linkedVp = PicturePlaneManager.FindViewport(doc, pair.ActiveViewportId);
            double defaultFov = linkedVp != null
                ? ViewportSync.LensLengthToFov(linkedVp.Camera35mmLensLength)
                : 60.0;

            double fov = defaultFov;
            var fovResult = RhinoGet.GetNumber(
                $"Camera horizontal FOV in degrees <{defaultFov:F1}>",
                true, ref fov, 1.0, 179.0);
            if (fovResult == Result.Cancel) return Result.Cancel;
            if (fovResult == Result.Nothing) fov = defaultFov;

            // Extract world and image point lists
            var worldPts = new System.Collections.Generic.List<Rhino.Geometry.Point3d>();
            var imagePts = new System.Collections.Generic.List<Rhino.Geometry.Point2d>();
            foreach (var (wp, ip) in pair.ReferencePairs)
            {
                worldPts.Add(wp);
                imagePts.Add(ip);
            }

            RhinoApp.WriteLine($"PMCalibrate: solving with {worldPts.Count} pairs, FOV = {fov:F1}°…");

            var result = CalibrationSolver.SolvePnP(
                worldPts, imagePts,
                pair.PixelWidth, pair.PixelHeight,
                fov);

            if (result == null)
            {
                RhinoApp.WriteLine("PMCalibrate: solver failed. Check that your reference points are accurate and not coplanar.");
                return Result.Failure;
            }

            RhinoApp.WriteLine($"PMCalibrate: reprojection error = {result.ReprojectionError:F2} px");
            RhinoApp.WriteLine($"  Camera location : ({result.CameraLocation.X:F3}, {result.CameraLocation.Y:F3}, {result.CameraLocation.Z:F3})");
            RhinoApp.WriteLine($"  Camera direction: ({result.CameraDirection.X:F3}, {result.CameraDirection.Y:F3}, {result.CameraDirection.Z:F3})");

            if (!ViewportSync.Apply(doc, pair, result))
            {
                RhinoApp.WriteLine("PMCalibrate: could not apply result — linked viewport no longer open.");
                return Result.Failure;
            }

            RhinoApp.WriteLine("PMCalibrate: camera updated. Run PMSetReferencePoints again to refine.");
            return Result.Success;
        }

        private static PhotoPlanePair? PickPair(PhotoPlaneRegistry registry)
        {
            if (registry.Pairs.Count == 1) return registry.Pairs[0];

            var names = new System.Collections.Generic.List<string>();
            foreach (var p in registry.Pairs) names.Add(p.Name);

            string pick = names[0];
            var res = RhinoGet.GetString(
                $"Photo plane to calibrate ({string.Join(", ", names)})", false, ref pick);
            if (res != Result.Success) return null;

            return registry.FindByName(pick);
        }
    }
}
Update Todos

Add PixelWidth/PixelHeight/ReferencePairs to PhotoPlanePair + registry

Add ComputePlaneFrame helper to PicturePlaneManager

Create Core/CalibrationSolver.cs (OpenCV PnP wrapper)

Create Core/ViewportSync.cs

Create Commands/SetReferencePointsCommand.cs

Create Commands/CalibrateCommand.cs
Done. Here's what was built:

New files
Core/CalibrationSolver.cs — wraps Cv2.SolvePnP (EPnP for ≥6 pts, Iterative otherwise). Takes Rhino Point3d/Point2d lists + image dims + horizontal FOV → returns CalibrationResult with world-space camera location, direction, up, and mean reprojection error in pixels.

Core/ViewportSync.cs — applies a CalibrationResult to the linked viewport: sets camera location + target + 35mm lens length (FOV conversion via 18mm / tan(fov/2)). Also exposes FovToLensLength / LensLengthToFov helpers.

Commands/SetReferencePointsCommand.cs — PMSetReferencePoints: loop of 3D pick → photo click. The photo click is constrained to the live photo plane (using GetPoint.Constrain(plane)), then back-projected to pixel coords. Numbered yellow markers drawn via an inline ReferenceMarkerConduit.

Commands/CalibrateCommand.cs — PMCalibrate: picks pair, prompts for horizontal FOV (defaults to current viewport's equivalent), runs solver, prints reprojection error, applies to viewport.

Modified files
PhotoPlanePair — added PixelWidth, PixelHeight, and ReferencePairs list.

PicturePlaneManager — added ComputePlaneFrame for back-projection without building a mesh.

Workflow to test
PMAddPhotoPlane — import a photo
PMSetReferencePoints — collect ≥4 pairs (3D click → photo click)
PMCalibrate — enter lens FOV, camera snaps to solved pose
C:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\CalibrationSolver.cs(14,16): error CS0104: 'Point3d' is an ambiguous reference between 'Rhino.Geometry.Point3d' and 'OpenCvSharp.Point3d' [C:\SRC\RHINO\VIEW\RhinoPhotoMatch\RhinoPhotoMatch.csproj]
C:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\CalibrationSolver.cs(37,19): error CS0104: 'Point3d' is an ambiguous reference between 'Rhino.Geometry.Point3d' and 'OpenCvSharp.Point3d' [C:\SRC\RHINO\VIEW\RhinoPhotoMatch\RhinoPhotoMatch.csproj]
C:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\CalibrationSolver.cs(38,19): error CS0104: 'Point2d' is an ambiguous reference between 'Rhino.Geometry.Point2d' and 'OpenCvSharp.Point2d' [C:\SRC\RHINO\VIEW\RhinoPhotoMatch\RhinoPhotoMatch.csproj]
Need to fix the ambiguous references in CalibrationSolver.cs by removing the using Rhino.Geometry; and using fully qualified names instead.

Edit c:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\CalibrationSolver.cs
Added 2 lines
Now replace all uses of the unqualified types in the file:

Read c:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\CalibrationSolver.cs
Edit c:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\CalibrationSolver.cs
Modified
Edit c:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\CalibrationSolver.cs
Modified
Edit c:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\CalibrationSolver.cs
Modified
That covers all three ambiguous types. Build should pass now.

C:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\CalibrationSolver.cs(79,64): error CS0117: 'SolvePnPFlags' does not contain a definition for 'EPnP' [C:\SRC\RHINO\VIEW\RhinoPhotoMatch\RhinoPhotoMatch.csproj]
C:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\CalibrationSolver.cs(89,37): error CS1615: Argument 2 may not be passed with the 'out' keyword [C:\SRC\RHINO\VIEW\RhinoPhotoMatch\RhinoPhotoMatch.csproj]
C:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\CalibrationSolver.cs(113,31): error CS1503: Argument 1: cannot convert from 'OpenCvSharp.Point3f[]' to 'OpenCvSharp.InputArray' [C:\SRC\RHINO\VIEW\RhinoPhotoMatch\RhinoPhotoMatch.csproj]
C:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\CalibrationSolver.cs(113,81): error CS1615: Argument 6 may not be passed with the 'out' keyword [C:\SRC\RHINO\VIEW\RhinoPhotoMatch\RhinoPhotoMatch.csproj]
Let me fix these OpenCvSharp API issues:

SolvePnPFlags.EPnP → should be SolvePnPFlags.Epnp (case)

Cv2.Rodrigues(rvec, out Mat rmat) → wrong signature, should be Cv2.Rodrigues(InputArray src, OutputArray dst) - use Mat rmat = new Mat(); Cv2.Rodrigues(rvec, rmat);

Cv2.ProjectPoints - needs InputArray not Point3f[], and output is not out - need to use Mat output

For ProjectPoints, the signature takes IEnumerable<Point3f> for object points and double[] arrays for rotation/translation vectors and camera matrix, so I need to convert my arrays accordingly. I need to convert the Mat objects for rvec and tvec into double[] arrays to match what the IEnumerable<Point3f> overload expects, then call ProjectPoints with the proper array types.

Actually, the InputArray/OutputArray version is cleaner—I can pass the Mat objects directly without conversion. I'll also need to fix the enum name from EPnP to Epnp, and use Cv2.Rodrigues to convert the rotation vector to a matrix when needed.

Rewriting the solve section now... I need to figure out how to properly convert the ProjectPoints output Mat to a Point2f array. The Mat will be in Nx1 or Nx2 CV_32FC2 format, so I should use GetArray to extract the Point2f values directly. I'm checking the OpenCvSharp4 API signature for SolvePnP — it takes OutputArray parameters rather than out Mat, so I need to create the Mat objects first and pass them directly to the method. I need to check the OpenCvSharp enum values for SolvePnPFlags to get the correct casing—it looks like it might be Epnp rather than EPnP, and I should verify the other flag names in the enum as well.

For the Rodrigues and ProjectPoints calls, I need to switch to using the OutputArray and InputArray overloads instead of the out parameter versions, which means creating a Mat object first and passing it directly rather than using the out keyword. I need to fix the Rodrigues call by creating the output Mat first instead of using an out parameter, then handle the ProjectPoints output argument similarly. The SolvePnP call with out parameters might be working with a different overload, so I'll leave that alone and focus on these specific errors. Looking at the OpenCvSharp4 source, SolvePnP actually accepts OutputArray for both the rotation and translation vectors, which should work with Mat objects. Let me rewrite this section more cleanly using the correct API signatures instead of trying to work around type mismatches. I'll fix the issues by pre-allocating the Mat objects instead of using out parameters, correcting the enum name from EPnP to Epnp, removing the out modifier from Rodrigues, and using InputArray.Create for ProjectPoints. Then I'll rewrite the solve section to use these corrected calls.

Read c:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\CalibrationSolver.cs (lines 79-135)
All four issues in one edit — pre-allocate Mats, fix enum name, fix Rodrigues, fix ProjectPoints:

Edit c:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\CalibrationSolver.cs
Added 2 lines
Changes made:

SolvePnPFlags.EPnP → SolvePnPFlags.Epnp
Pre-allocated rvec/tvec as using var Mat — no out
Rodrigues(rvec, rmat) — no out, pre-allocated rmat
ProjectPoints uses InputArray.Create(objPts) and a Mat output, then GetArray<Point2f>() to extract results
Removed manual .Dispose() calls since all Mats now use using
C:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\CalibrationSolver.cs(79,64): error CS0117: 'SolvePnPFlags' does not contain a definition for 'Epnp' [C:\SRC\RHINO\VIEW\RhinoPhotoMatch\RhinoPhotoMatch.csproj]
The enum value for EPnP in OpenCvSharp might be different. Let me check possible names:- SolvePnPFlags.EPNP

SolvePnPFlags.EpnP
It might not exist in the version we're using (4.9.0.*)
Looking at OpenCvSharp4 source, in version 4.9, the SolvePnPFlags enum values are:

Iterative = 0

P3P = 2

DLS = 3 (deprecated)

UPnP = 4 (deprecated)

AP3P = 5

IPPE = 6

IPPE_SQUARE = 7

SQPnP = 8

So it looks like Epnp might not be available in this version, or it could be named differently than the C++ equivalent where it's SOLVEPNP_EPNP = 1.

In OpenCvSharp4 v4.9, the flag might be spelled as SolvePnPFlags.EPnP with that specific capitalization, but since that's not working either, I should probably just fall back to using SolvePnPFlags.Iterative consistently or cast the integer value directly. The safest approach is to stick with Iterative since it's always available and works reasonably well with proper initialization.

Epnp isn't exposed in OpenCvSharp4 4.9. The safest fix is to always use Iterative, which handles all point counts:

Edit c:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\CalibrationSolver.cs
Modified
C:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\CalibrationSolver.cs(82,36): error CS1503: Argument 1: cannot convert from 'OpenCvSharp.Point3f[]' to 'OpenCvSharp.InputArray' [C:\SRC\RHINO\VIEW\RhinoPhotoMatch\RhinoPhotoMatch.csproj]
C:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\CalibrationSolver.cs(82,44): error CS1503: Argument 2: cannot convert from 'OpenCvSharp.Point2f[]' to 'OpenCvSharp.InputArray' [C:\SRC\RHINO\VIEW\RhinoPhotoMatch\RhinoPhotoMatch.csproj]
The Cv2.SolvePnP overload that takes IEnumerable<Point3f> and IEnumerable<Point2f> apparently isn't matching, or perhaps the overload that takes InputArray is being chosen instead. Let me use InputArray.Create() for both arguments.

Edit c:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\CalibrationSolver.cs
Added 1 line
C:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\CalibrationSolver.cs(82,23): error CS0029: Cannot implicitly convert type 'void' to 'bool' [C:\SRC\RHINO\VIEW\RhinoPhotoMatch\RhinoPhotoMatch.csproj]
The Cv2.SolvePnP overload that takes InputArray returns void, not bool. Only the overloads taking IEnumerable<Point3f> return bool. Since we can't use the array directly, let me use Mat.FromArray to create the Mats.

Actually, the issue is that InputArray.Create(Point3f[]) picks the void-returning overload. Let me instead create Mat objects from the arrays and pass those.

For Mat creation from Point3f[]:


using var objMat = Mat.FromArray(objPts);  // This creates a Mat from Point3f[]


using var imgMat = Mat.FromArray(imgPts);
Wait, I'm not sure if Mat.FromArray<Point3f> works directly. Let me think of another approach. The cleanest way to avoid this overload confusion is to create proper Mats with the correct OpenCV formats—object points as CV_32FC3 and image points as CV_32FC2.

Building the Mat objects... I need to figure out which SolvePnP overload to use—the one taking Mat objects returns void, but I need a bool return value. The solution is to use the overload that accepts IEnumerable<Point3f> and IEnumerable<Point2f> directly, along with specific matrix types for the camera parameters.

Building the solution with this approach... Finishing the ProjectPoints call with the jacobian output parameter, then I'll refactor the solver to use these cleaner overloads throughout instead of dealing with InputArray conversions.

The InputArray overload of SolvePnP returns void. The bool-returning overload uses double[,]/double[] types throughout. Let me rewrite the whole solve section to use that overload consistently:

Edit c:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\CalibrationSolver.cs
Removed 5 lines
Now fix the ProjectPoints call to use the matching double[]-based overload:

Edit c:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\CalibrationSolver.cs
Removed 1 line
C:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\CalibrationSolver.cs(78,36): error CS1503: Argument 1: cannot convert from 'OpenCvSharp.Point3f[]' to 'OpenCvSharp.InputArray' [C:\SRC\RHINO\VIEW\RhinoPhotoMatch\RhinoPhotoMatch.csproj]
C:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\CalibrationSolver.cs(78,44): error CS1503: Argument 2: cannot convert from 'OpenCvSharp.Point2f[]' to 'OpenCvSharp.InputArray' [C:\SRC\RHINO\VIEW\RhinoPhotoMatch\RhinoPhotoMatch.csproj]
C:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\CalibrationSolver.cs(78,52): error CS1503: Argument 3: cannot convert from 'double[*,*]' to 'OpenCvSharp.InputArray' [C:\SRC\RHINO\VIEW\RhinoPhotoMatch\RhinoPhotoMatch.csproj]
C:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\CalibrationSolver.cs(78,61): error CS1503: Argument 4: cannot convert from 'double[]' to 'OpenCvSharp.InputArray' [C:\SRC\RHINO\VIEW\RhinoPhotoMatch\RhinoPhotoMatch.csproj]
C:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\CalibrationSolver.cs(79,40): error CS1615: Argument 5 may not be passed with the 'out' keyword [C:\SRC\RHINO\VIEW\RhinoPhotoMatch\RhinoPhotoMatch.csproj]
C:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\CalibrationSolver.cs(79,62): error CS1615: Argument 6 may not be passed with the 'out' keyword [C:\SRC\RHINO\VIEW\RhinoPhotoMatch\RhinoPhotoMatch.csproj]
C:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\CalibrationSolver.cs(87,27): error CS1503: Argument 1: cannot convert from 'double[]' to 'OpenCvSharp.InputArray' [C:\SRC\RHINO\VIEW\RhinoPhotoMatch\RhinoPhotoMatch.csproj]
C:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\CalibrationSolver.cs(87,40): error CS1615: Argument 2 may not be passed with the 'out' keyword [C:\SRC\RHINO\VIEW\RhinoPhotoMatch\RhinoPhotoMatch.csproj]
It seems that the OpenCvSharp4 version being used (4.9.0.*) only has InputArray/OutputArray overloads for SolvePnP, Rodrigues, and ProjectPoints — not the convenience overloads with double[], Point3f[], etc.

So I need to use Mat objects throughout. Let me figure out the correct approach:

For SolvePnP:


// void-returning overload:
Cv2.SolvePnP(InputArray objectPoints, InputArray imagePoints, InputArray cameraMatrix, InputArray distCoeffs, OutputArray rvec, OutputArray tvec, bool useExtrinsicGuess = false, SolvePnPFlags flags = SolvePnPFlags.Iterative)


This returns void, so I can't check for success. Since all overloads require InputArray types, I'll need to convert my arrays to Mat objects first, then call SolvePnP without a return value check.

I'm creating Mat objects from the point arrays...



csharp
using var objMat = Mat.FromArray(objPts);
using var imgMat = Mat.FromArray(imgPts);
Now setting up the camera matrix with focal length and principal point, along with a distortion coefficients matrix, then calling SolvePnP to compute the rotation and translation vectors. I need to check if the result was successful by verifying the output matrices aren't empty.

For converting the rotation vector to a rotation matrix, I'm using Rodrigues transformation, then accessing individual matrix elements as doubles. Finally, I'm projecting the 3D points back onto the image plane using ProjectPoints...

Now I'm verifying that Mat.FromArray properly handles the different data types—Point3f arrays should create CV_32FC3 matrices for object points, Point2f arrays should create CV_32FC2 for image points, and a 2D double array should create the CV_64F 3x3 camera matrix that these functions expect.

For accessing the results, I'm checking that rmat.At<double>(0, 0) works on a CV_64F 3x3 matrix and tvec.At<double>(0) works on a CV_64F 3x1 vector, which they should.

Since SolvePnP doesn't return a success value, I'll wrap the call in a try-catch to handle any OpenCVException that gets thrown if something fails.

Writing the CalibrationSolver implementation... Writing the calibration solver... result looks reasonable (rvec not empty).

Actually, there might be a simpler approach. OpenCvSharp does have a bool-returning SolvePnP if we look carefully. Let me look at what the error is telling us:


error CS1503: Argument 1: cannot convert from 'OpenCvSharp.Point3f[]' to 'OpenCvSharp.InputArray'
This means the ONLY overload available takes InputArray. So there's no IEnumerable<Point3f> overload in this version.

But wait, InputArray.Create(Point3f[]) SHOULD work to convert an array to InputArray. The earlier error said that version returned void. So the issue is:

The InputArray-based SolvePnP returns void (can't assign to bool)
There's no IEnumerable-based bool-returning overload
So I need to use the void version and skip the bool check. I'm wrapping the call in a try-catch to handle any errors, but I'm realizing that InputArray.Create might not actually support Point3f[] directly for some reason, which is why the conversion is failing in the first place. Still writing the solver...

This version of OpenCvSharp only exposes InputArray/OutputArray overloads — no convenience wrappers. I need to build Mat objects explicitly and skip the bool return (use try/catch instead):

Edit c:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\CalibrationSolver.cs
Added 13 lines
Now fix ProjectPoints to use Mats too:

Edit c:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\CalibrationSolver.cs
Added 1 line
Also remove the now-unused using OpenCvSharp alias for InputArray — actually InputArray is still needed nowhere now. Let me also drop the unused using System.Collections.Generic if it's only needed for IList (it is still needed). The file should be clean now. Let me verify the top:

Read c:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\CalibrationSolver.cs (lines 2-11)
Good. All usings are still needed (System for Math/Exception, Collections.Generic for IList, OpenCvSharp for Mat/MatType/Cv2/Point3f/Point2f). Build should pass now.

I want to implement a command "PMSetPhotoplaneTransparency" that changes the trancparency of a photoplane the command ask for what plane then a procentage 0-100 of what transparancy photoplane shuld have
The user wants a command to set the transparency of a photo plane. I need to:

Add a Transparency property to PhotoPlanePair
Update PhotoPlaneConduit.GetMaterial to use the transparency value
Create Commands/SetPhotoplaneTransparencyCommand.cs
Let me look at the relevant files to understand the current state.

The DisplayMaterial.Transparency property takes a value from 0.0 to 1.0. The command will ask for 0-100% and convert.

I need to:

Add Transparency (0.0–1.0) to PhotoPlanePair

Update GetMaterial to apply the transparency value from the pair and clear the material cache when it changes

Write the command to handle user input and update the transparency

Three changes: add Transparency to PhotoPlanePair, use it in the conduit material, create the command.

Edit c:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\PhotoPlanePair.cs
Added 3 lines
Now use it in GetMaterial:

Edit c:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\PhotoPlaneConduit.cs
Edit failed
Read c:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\PhotoPlaneConduit.cs (lines 95-119)
Edit c:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\PhotoPlaneConduit.cs
Modified
Now create the command:

Write c:\SRC\RHINO\VIEW\RhinoPhotoMatch\Commands\SetPhotoplaneTransparencyCommand.cs
72 lines
using Rhino;
using Rhino.Commands;
using Rhino.Input;
using RhinoPhotoMatch.Core;

namespace RhinoPhotoMatch.Commands
{
    /// <summary>
    /// PMSetPhotoplaneTransparency — sets the display transparency of a photo plane.
    /// 0 % = fully opaque (default), 100 % = fully transparent.
    /// </summary>
    public class SetPhotoplaneTransparencyCommand : Rhino.Commands.Command
    {
        public override string EnglishName => "PMSetPhotoplaneTransparency";

        protected override Result RunCommand(RhinoDoc doc, RunMode mode)
        {
            var plugin   = RhinoPhotoMatchPlugin.Instance;
            var registry = plugin.Registry;

            if (registry.Pairs.Count == 0)
            {
                RhinoApp.WriteLine("PMSetPhotoplaneTransparency: no photo planes in the registry.");
                return Result.Failure;
            }

            // Pick pair
            PhotoPlanePair? pair;
            if (registry.Pairs.Count == 1)
            {
                pair = registry.Pairs[0];
            }
            else
            {
                var names = new System.Collections.Generic.List<string>();
                foreach (var p in registry.Pairs) names.Add(p.Name);

                string pick = names[0];
                var res = RhinoGet.GetString(
                    $"Photo plane ({string.Join(", ", names)})", false, ref pick);
                if (res != Result.Success) return res;

                pair = registry.FindByName(pick);
                if (pair == null)
                {
                    RhinoApp.WriteLine($"PMSetPhotoplaneTransparency: no plane named \"{pick}\".");
                    return Result.Failure;
                }
            }

            // Prompt for percentage
            double current = pair.Transparency * 100.0;
            double pct = current;
            var getResult = RhinoGet.GetNumber(
                $"Transparency % for \"{pair.Name}\" <{current:F0}>",
                true, ref pct, 0.0, 100.0);

            if (getResult == Result.Cancel) return Result.Cancel;
            if (getResult == Result.Nothing) pct = current; // Enter = keep current

            pair.Transparency = pct / 100.0;

            // Invalidate cached material so it rebuilds with new transparency
            plugin.Conduit.InvalidateMaterial(pair.Name);

            doc.Views.Redraw();
            RhinoApp.WriteLine($"\"{pair.Name}\" transparency set to {pct:F0} %.");
            return Result.Success;
        }
    }
}
Done. The command prompts for the plane name (skipped if only one exists), then asks for a percentage 0–100. It stores the value on the pair, invalidates the material cache, and redraws. The conduit picks up the new transparency on the next frame.

well the photo is looks more washed out but the photoplane is not accualy transparent
The user is saying that when they change the transparency, the photo looks washed out (lighter/whiter) but it doesn't actually become transparent (you can't see through it to the background/geometry behind it).

This is a known issue with how DrawMeshShaded works in Rhino's display conduit. The DisplayMaterial.Transparency property affects the material's appearance in certain ways, but for actual alpha-blending transparency in a conduit, you need to handle it differently.

The issue is that DrawMeshShaded with a DisplayMaterial that has Transparency > 0 might just be lightening/washing out the color rather than making the mesh actually transparent (alpha-blended).

So I need to explore a few different approaches to get actual transparency working. The first option is to ensure I'm using the right blend state when drawing the mesh—proper depth sorting and the transparency property should theoretically handle alpha blending, but it might not be working as expected in the conduit context. Another possibility is that the transparency setting in DrawMeshShaded doesn't actually trigger alpha blending in Shaded mode and just affects how the material color is mixed. I could also try loading the image as a bitmap and modifying it directly to handle transparency at the pixel level.

The real issue seems to be about timing in the render pipeline—PostDrawObjects is where transparent geometry should be drawn so it renders after all opaque objects, which is probably why the transparency isn't showing up correctly right now. Let me think through whether the display material's transparency property is actually being applied or if something else is preventing it from working.

Actually, I think the "washed out" appearance might mean Rhino is blending the texture with white instead of doing true alpha compositing. I should try adjusting the diffuse color's alpha channel directly based on the transparency value instead.

Still writing color adjustment... I see the issue now—in a conduit's PreDrawObjects, the photo plane is being drawn before the scene geometry, so when transparency is applied, it's blending with the white viewport background rather than the actual geometry behind it, which is why everything looks washed out instead of genuinely transparent.

Moving the photo to PostDrawObjects would let it composite over the already-rendered geometry, but that might not work in the linked viewport depending on how Rhino handles the drawing order. The real question is whether DisplayMaterial.Transparency in Rhino's conduit actually supports true alpha blending or if it's just a viewport rendering limitation. The cleanest solution is to use DisplayMaterial.Transparency and conditionally switch between PreDrawObjects (when fully opaque, to keep the photo as background) and PostDrawObjects (when transparent, to composite correctly over the scene). The key insight is that PostDrawObjects should handle transparency properly since it renders after all geometry, so I can draw the linked viewport there when transparency is active. The real issue is that when transparency is applied in PreDrawObjects, the photo blends with the viewport background instead of compositing over the geometry. I need to switch to PostDrawObjects when transparency is greater than zero, so the alpha properly layers the photo on top of the already-rendered 3D model. Let me update the conduit to only use PreDrawObjects for fully opaque images, and PostDrawObjects for anything with transparency.

The issue: PreDrawObjects fires before geometry is rendered, so blending has nothing to composite against except the viewport background (white) — hence "washed out" but not transparent to the model.

The fix: when transparency > 0, move the linked-viewport draw to PostDrawObjects so it composites over already-rendered geometry.

Read c:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\PhotoPlaneConduit.cs
Edit c:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\PhotoPlaneConduit.cs
Added 7 lines
Now update the body of PostDrawObjects — the frustum/dot should only show for other viewports, not when drawing the transparent linked plane:

Edit c:\SRC\RHINO\VIEW\RhinoPhotoMatch\Core\PhotoPlaneConduit.cs
Added 1 line
Now when transparency is 0 the photo draws in PreDrawObjects as a background behind all geometry. When transparency > 0 it draws in PostDrawObjects so it alpha-composites over the already-rendered scene, giving true see-through transparency.

/compact
Error: Compaction canceled.

